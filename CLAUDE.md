# CLAUDE.md — 로앤 통합 플랫폼

**언어 지침(최우선): 사용자에게 보여주는 모든 응답·설명·요약·질문은 예외 없이 한국어로
작성한다.** 코드/커밋 메시지/식별자처럼 원래 영어가 관례인 항목, 그리고 사용자가 붙여넣은
영어 원문을 그대로 인용하는 경우는 예외다.

이 저장소에서 작업을 시작하기 전에 **`PROJECT_PLAN.md`를 먼저 읽어라.** 저장소 구조·
아키텍처·설계 결정·오픈 이슈의 단일 소스다. 그리고 **`AGENTS.md` 맨 아래 "작업 인수인계
로그"**를 확인할 것 — Codex를 포함해 다른 에이전트/세션이 최근에 무슨 작업을 했는지
append-only로 기록되어 있다.

이 파일이 따로 있는 이유: Claude Code는 세션 시작 시 `CLAUDE.md`를 자동으로 읽지만
`AGENTS.md`·`PROJECT_PLAN.md`는 자동으로 읽지 않기 때문이다 — 이 파일은 그 두 문서로 가는
안내판 역할만 한다. (Codex는 반대로 `AGENTS.md`를 자동으로 읽는다. 즉 이 두 파일이 있어야
Claude와 Codex 양쪽 다 같은 설계 문서를 세션 시작 시 자동으로 보게 된다.)

이 세션에서 의미 있는 작업(스캐폴딩, 신규 패키지/앱, DB 스키마, 외부 연동, 배포, 크론 변경
등)을 마쳤으면 **`AGENTS.md`의 "작업 인수인계 로그" 섹션에 형식에 맞춰 새 항목을 추가**할 것.
