# 로앤 통합 플랫폼 — 프로젝트 설계·구현 기준선 (v1.62)

> 이 문서는 새 로앤 홈페이지 + 새 ERP + 리걸플로/리걸프렌즈 연동을 하나의 플랫폼으로
> 묶기 위한 **저장소 구조·아키텍처 설계 초안**이다. 코덱스/클로드코드 세션이 번갈아
> 작업하는 것을 전제로 하며, 첫 커밋 시점에 이 문서를 기준선으로 삼는다.
>
> 2026-08-19 후보: 센트릭스 SMS/LMS는 발송 직원의 개인 전화 회선 대신 활성·인증된
> 대표 endpoint `070-4607-0588`을 사용한다. `telephony_messages`에는 실제 발송 직원과 함께
> 발신번호·회신 mailbox endpoint snapshot을 보존하고, 수신문자는 같은 mailbox·같은 고객번호의
> 수신시각 이전 최신 성공/결과불명 발송에만 `reply_mailbox_latest_outbound`로 연결한다. snapshot이
> 없는 과거 발송이나 다른 mailbox 발송은 번호만으로 추측하지 않고 `연결 확인 필요`와 관리자
> 알림으로 남긴다. SOLAPI JPG MMS는 기존 직원 endpoint 원장과 `02-555-7455` 발신을 유지하며,
> 활성·인증된 7455 대표 endpoint가 유일할 때만 회신 mailbox snapshot을 함께 기록한다.
> migration `0069_talented_meltdown.sql`과 gateway 후보만 만들었고 main 병합·운영 migration·배포·
> 추가 외부 발송은 수행하지 않았다.
>
> 2026-08-19 운영 canary: 센트릭스 대표 endpoint `070-4607-0588`의 RID를 제거한 뒤 그
> 계정을 명시적으로 선택해 SMS 1건을 발송했다. 고객 단말에는 `070-4607-0588`이
> 발신번호로 표시됐고, 전화 단말의 전원·인터넷이 연결되지 않은 상태에서도 같은 번호로 보낸
> 회신이 0588의 `getrecvsmslist`에 들어왔다. gateway는 수신 후 약 14초 안에 원 발송 건과
> `latest_outbound`로 연결하고 최신 발송 직원 알림을 만들었다. 따라서 개인 전화 회선은 통화에
> 유지하면서 센트릭스 SMS/LMS만 0588 공용 계정으로 보내고 같은 mailbox에서 회신을 받는
> 수직 경로는 운영에서 검증됐다. 실제 제품화 전에는 현재의 상대번호 전역 최신 발송 매칭을
> `회신 mailbox + 상대번호 + 발송시각` 기준으로 좁히고 발송 당시 회신 mailbox snapshot을
> 보존해야 한다. SOLAPI JPG MMS는 기존 `02-555-7455` 발신을 유지하되 그 번호의 실제
> 센트릭스 회신 mailbox를 별도로 snapshot한다. 소스·migration·배포는 아직 변경하지 않았다.
>
> 2026-08-19 운영 반영: Orca 완료 작업 6개를 소스
> `224e156619f4fab3b9d78f7ca9204e377de5b4c8`, 릴리스
> `20260819T035353Z-orca-integrated-ops-v3`로 홈페이지·ERP·gateway에 함께 배포했다.
> 암호화 RDS snapshot 뒤 migration `0068_centrex_inbound_message_deduplication.sql`을
> 적용해 운영 원장 69개, 수신문자 안정 unique index 1개, 잔여 중복 그룹 0을 확인했다.
> GA4 코드는 포함됐지만 운영 Measurement ID와 외부 속성 연결은 승인 게이트에 남겨
> 비활성이고 AdPilot·Windows bridge·외부 발송은 변경하지 않았다.
>
> 2026-08-19 통합 후보는 Orca 완료 작업 6개를 한 소스로 합쳤다. 홈페이지에는 동의 기반
> GA4 측정 코드를 포함하되 운영 Measurement ID와 외부 속성 연결은 승인 전까지 비활성으로
> 둔다. ERP에는 전화데스크 재통화, 무효 상담 복원 보강, 전 직원 담당자 변경, 소개·기존
> 상담명과 소개자 담당 문맥, 단순화한 내비게이션, 문자 미읽음 우선 표시를 반영한다.
> gateway와 migration `0068_centrex_inbound_message_deduplication.sql`은 변동 목록 순번을
> 수신문자 중복키에서 제거하고 기존 중복 원장과 직원별 읽음 상태를 보수적으로 병합한다.
> 영향 앱은 홈페이지·ERP·gateway이며 같은 `main` commit과 릴리스 ID로 배포한다.
>
> 2026-08-19 후보: `lawand-ga4-measurement-v1`을 홈페이지와 AdPilot 양쪽에 구현했다.
> 홈페이지 전역은 서버 런타임에서 검증한 Measurement ID가 있고 사용자가 서비스 개선
> 분석을 허용한 뒤에만 GA 스크립트를 불러온다. 맞춤형 광고·Google Signals와 세 광고
> 동의는 계속 비활성화한다. 수동 페이지뷰는 정식 origin·허용된 공개 pathname·통제된
> 캠페인 키만 보내고 실제 검색어·임의 query·fragment를 버린다. 사례 상세 slug는 공통
> 상세 경로로, 알 수 없는 경로는 `/_not-found`로 일반화한다. 직접 상담과 상담을 생성한
> 자가진단의
> `new`·`suspected_duplicate` 성공만 논리 제출당 `generate_lead` 후보로 두며 접수번호·
> 제출키·상담값은 보내지 않는다. 개인정보처리방침 초안과 푸터 재설정·철회 흐름도 실제
> 후보 코드에 맞췄다. 전체 운영 상담·계약·매출의 기준은 계속 gateway·ERP다.
> AdPilot v1.1.0은 최적화 역할과 상세 보고 목표 선택을 분리하고 `GA4 관측` 경계·범용
> 연결 문구를 적용했으며, 서로 다른 세션/랜딩 키워드 ID는 미귀속과 `conflictingEvents`로
> 진단한다. 상대 최신 커밋은 `59ac8c42a0512525fd9cde7c3a80bbca9ccaf917`이다. 양쪽 후보는
> 로컬 테스트·typecheck·lint·production build를 통과했지만 GA4 운영 속성·웹 스트림,
> 네이버 광고 계정과 운영 Measurement ID는 아직 만들거나 연결하지 않았고 main 병합·
> 운영 secret 변경·배포·실데이터 조회·광고 변경도 수행하지 않았다.
>
> 2026-08-19 후보: ERP 전화데스크 통화 상세 헤더에 기존 센트릭스 클릭투콜을 연결한다.
> 통화의 상담 또는 리걸프렌즈 고객 대상을 안전하게 식별할 수 있으면 상담 담당자 여부와
> 무관하게 인증 직원 누구나 버튼을 사용할 수 있고, 실제 발신은 실행 직원 본인의 활성 주
> 회선과 기존 중복 발신 방지·감사 원장을 그대로 사용한다. 전화 후처리의 자동문자 선택에는
> 템플릿 이름과 발송 상태만 남기고, 실제 개인 링크까지 포함하지 않아 불완전했던
> `발송될 내용 미리보기` 영역은 제거한다. gateway·DB·migration 계약은 변경하지 않는다.
>
> 2026-08-19 후보: ERP 상담데스크 신규등록의 기존고객·소개건 선택과 전화데스크 후처리의
> `신건상담으로 저장`에 같은 고객명 사내 구분 규칙을 적용한다. 선택한 동안 이름 입력칸 안에
> 수정할 수 없는 `_기존`·`_소개` 접미사를 바로 표시하고, 전화 후처리에는 동시에 선택할 수
> 없는 기존고객·소개건 선택을 추가한다. 저장 경계는 사용자가 과거 방식으로 접미사를 직접
> 입력했더라도 끝에 반복된 두 접미사를 제거한 뒤 현재 선택값 하나만 붙인다. 자동 접미사를
> 포함한 고객명은 기존 50자 제한을 유지하며 migration·운영 데이터 변경은 없다.
>
> 2026-08-19 보강 후보: 소개건은 새 고객 전화번호로 기존 담당자를 찾지 않고, 등록 때 선택한
> 소개자의 암호화 사건 snapshot에서 담당자명을 읽어 상담 목록에 `소개자 담당` 배지로 표시한다.
> 상세 화면과 신규등록의 검색·선택 문구도 기존 고객 담당과 소개자 담당을 구분한다. 전화데스크
> 후처리에서 소개건을 선택하면 소개자를 고객찾기에서 반드시 선택하게 하고, gateway가 고객·사건
> ID를 다시 검증한 뒤 소개자 사건·담당 snapshot을 상담 생성과 같은 트랜잭션에 보존한다. 기존
> 소개건도 이미 저장된 snapshot을 사용하므로 backfill과 migration은 필요하지 않다.
>
> 2026-08-19 후보: ERP 상단 내비게이션은 자주 쓰는 상담·고객 찾기·후기·전화·문자를
> 1차 메뉴로 유지하고, 모든 직원용 저빈도 기능인 전화번호부는 `더보기`, 관리자 전용
> 직원 관리는 별도 `관리` 메뉴로 분리했다. 하위 페이지에서는 상위 메뉴와 현재 항목을
> 함께 활성 표시하며 드롭다운은 바깥 클릭·Escape·페이지 이동 때 닫힌다. 메뉴 텍스트는
> 줄바꿈하지 않고 모바일에서는 기존처럼 아이콘 중심으로 축약한다.
>
> 2026-08-19 수정 후보: 센트릭스 수신문자의 영구 중복키에서 새 문자 수신 때마다 밀리는
> 목록 순번 `NO`를 제거하고 endpoint·발신번호 지문·수신 시각·본문 지문을 안정 키로
> 확정했다. worker의 선조회와 DB unique index가 같은 키를 사용해 순차·동시 재수집을 모두
> 막는다. migration `0068_centrex_inbound_message_deduplication.sql`은 배포 시점의 기존
> 중복을 전수 병합하며, 최신 발신 연결이 가장 강한 행을 보존하고 직원별 알림 사유와 읽은
> 시각을 합친 뒤 초과 수신문자 행을 제거한다. 마지막 읽기 전용 운영 표본은 중복 31그룹·
> 초과 56행이었고 로컬 PostgreSQL 트랜잭션 fixture에서 원장 병합·읽음 보존·재삽입 차단을
> 검증했다. 이 브랜치에서는 운영 migration·운영 데이터 수정·배포를 수행하지 않았다.
>
> 2026-08-19 운영 진단: `김충환3_테스트` 휴대전화 캡처와 운영 문자 원장을 읽기 전용으로
> 대조해 상담·끝번호·대표번호 연결은 정확하지만 수신문자가 중복 저장되는 결함을 확인했다.
> 센트릭스 `getrecvsmslist`의 `NO`는 새 문자에 따라 기존 값이 밀리는 목록 순번인데 현재
> provider identity에 포함돼 같은 시각·발신번호·본문이 다른 메시지로 재수집된다. 해당 대화는
> 실제 수신 5건이 16행으로 저장됐고 운영 전체는 중복 그룹 31개·초과 56행이다. 영구 중복키를
> endpoint·발신번호 지문·수신 시각·본문 지문 기준으로 교체해야 한다고 판정했다. 이 진단에서
> 운영 데이터 수정은 수행하지 않았다.
>
> 2026-08-19 후보: 직원별 문자 미읽음 수가 내비게이션에 남아도 기존 문자 대화 목록은
> 최근 시각만으로 정렬되고 미읽음 표시가 없어 대상 대화를 찾기 어려웠다. 문자 허브 조회가
> 현재 직원의 대화별 미읽음 수를 함께 계산해 미읽음 대화를 먼저, 그 안에서는 최신순으로
> 반환하고 목록에 건수를 표시한다. 대화 조회 성공 시에만 기존 원장의 해당 대화 미읽음을
> 읽음 처리하며 화면의 표시와 전역 내비게이션 숫자도 즉시 다시 동기화한다.
>
> 2026-08-18 운영 반영: Orca 완료 작업 6개와 migration `0066`·`0067`을 소스
> `b837751660172877c80d2cdcc59cd57293540c64`, 릴리스
> `20260818T090149Z-orca-integrated-ops-v2`로 gateway·ERP에 배포했다. 문자 SSE가 추가한
> 다섯 번째 영구 LISTEN 연결에 맞춰 운영 설정과 코드의 전용 풀 최소값을 5로 올렸으며,
> 최종 health는 request waiting 0/20·LISTEN waiting 0/5다. 홈페이지와 Windows bridge,
> 실제 전화·문자·쿠폰 발송, 별도 운영 원장은 변경하지 않았다.
>
> 2026-08-19 후속 후보에서는 리걸프렌즈 `무효`를 ERP 상담 상태가 아니라 특수 담당자
> 계정으로 바로잡았다. 상세 조회는 ERP 배정 유무와 관계없이 사건 연결을 반환하고,
> soft-delete가 아니며 현재 외부 담당자가 `lawandfirm_s999`이면 상담 상태가 `closed`여도
> 복원할 수 있다. 복원 요청은 별도 outbox 이벤트로 멱등 처리하며 외부 담당자 변경 성공
> 뒤에만 기존 ERP 배정을 실행 직원으로 갱신하거나 새 배정을 만들고 상담 상태를 `assigned`로
> 전환한다. 원래 상담·요청·사건 원장과 상태 이력을 보존하며 migration은 추가하지 않는다.
>
> 2026-08-19 후속 후보에서는 일반 담당자 변경도 현재 담당자·관리자에게만 제한하지 않고
> 상담 접근 권한이 있는 인증 직원 누구나 요청할 수 있게 한다. 대상 직원의 활성 멤버십·
> 리걸프렌즈 계정 검증, 외부 변경 성공 뒤 ERP 확정, pending·무효·종결 충돌 방지는 그대로
> 유지한다. ERP 처리 현황의 담당자명과 고정 폭 변경 버튼은 같은 행의 중앙선에 맞춘다.
>
> 2026-08-18 통합 후보는 Orca 작업 6개를 한 소스로 합쳤다. 전화 후처리 자동문자와 상담완료
> 후기 요청은 직원이 최종 발송 여부를 선택하고 기존 성공·처리 중 이력을 안전하게 기본 해제한다.
> 수신전화는 서울·대전·부산·미분류 지역을 토스트와 브라우저 알림에서 먼저 구분하며, 무효로
> 넘긴 리걸프렌즈 사건은 인증 직원이 기존 멱등 담당자 변경 원장을 통해 상담으로 복원한다.
> 후기 목록은 실제 쿠폰 `sent` 원장만 발송완료로 표시한다. 직원 생성 상담의 등록자를 직접
> 보존하고 전화 후처리 신건에 필수 거주지역을 받으며, 대표번호 수신문자는 최근 발송 직원과
> 상담 담당자 또는 활성 관리자에게 배정해 직원별 미읽음·SSE·토스트·브라우저 알림을 제공한다.
> 독립 브랜치에서 겹친 migration 번호는 상담 등록자 `0066_consultation_request_creator.sql`,
> 수신문자 알림 `0067_goofy_mongoose.sql`의 단일 snapshot/journal 체인으로 확정했다.
>
> 2026-08-18 후보: 외부 수신전화가 울릴 때 대표번호·센트릭스 원회선을 우선하고 개인
> 회선은 활성 소유 직원의 주 소속을 사용해 `서울`·`대전`·`부산`·`미분류`로 분류한다.
> 대표번호는 02-555-7455·02-555-7465·070-4607-0588을 서울, 042-484-0488·
> 042-485-0488을 대전, 051-502-1919·051-505-1909를 부산으로 고정한다. 근거가 없거나
> 직원 소속이 충돌하면 추정하지 않고 미분류로 둔다. ERP 실시간 토스트는 색상 지역 배지를,
> OS 브라우저 알림은 제목 맨 앞의 `[지역]` 표기를 사용해 담당 여부보다 지역을 먼저
> 인지하게 한다. migration·운영 데이터·배포는 변경하거나 수행하지 않는다.
>
> 2026-08-18 수정 후보: ERP 직원 신규등록과 전화데스크 후처리 신건 생성의 등록자를
> `consultation_requests.created_by_user_id`에 직접 보존하고 상담 요청 이력에 표시한다.
> 기존 직원 생성 건은 요청 시각과 정확히 일치하는 초기 직원 상태이력이 있을 때만 역채움한다.
> 전화 후처리 신건은 거주지역을 받지 않은 채 리걸프렌즈 필수 intake 계약으로 전달되어
> `invalid_stored_data`가 발생하던 결함을 수정했다. 후처리 화면과 공통 계약에서 거주지역을
> 필수로 받고 암호화 intake에 저장하며, 과거처럼 필수 intake가 누락되면 일반 저장 오류가
> 아니라 `invalid_consultation_intake`로 원인을 명시한다. migration은
> `0066_consultation_request_creator.sql`이며 운영 데이터·배포는 이 후보에서 변경하지 않는다.
>
> 최신 운영 반영(2026-08-18): 브라우저 알림 누락 복구·다중 탭 안정화, 신규 상담 전달사항,
> 후기 완료 화면 어텐션, 개인 문자 템플릿 자동발송, 기프티쇼 비즈 통제 발송을 소스
> `3c53cb46ac49c464e41b2085ba9ee28f2c7cf89c`, 릴리스
> `20260818T051852Z-integrated-notifications-memo-sms-gifts-v1`로 홈페이지·ERP·gateway에
> 함께 배포했다. migration `0064`·`0065`와 기프티쇼 운영 비밀 6개를 반영했고 Windows
> bridge와 기존 통화·문자·쿠폰 원장은 변경하지 않았다.
> 최종 health에서 상담 알림 snapshot이 영구 LISTEN 전용 풀을 잘못 사용해 대기를 누적시키는
> 문제를 발견했다. snapshot 조회를 요청 풀로 분리한 gateway 소스
> `430081bf864c542a7d2b4369c9a6ef2feeee8d47`, 릴리스
> `20260818T054421Z-consultation-listener-pool-hotfix-v1`을 즉시 후속 배포해 LISTEN 대기 0을
> 연속 확인했다. 스키마·운영 데이터·ERP·홈페이지·Windows bridge는 이 hotfix에서 바꾸지 않았다.
> 2026-08-18에는 후기 상세의 모바일 쿠폰 발송 상태를 브라우저 임시 문구가 아니라
> `review_gift_coupon_deliveries` 운영 원장에서 다시 읽는 계약으로 보강했다. 발송 내역을
> 확인하기 전에는 버튼을 잠그고, 체크 완료 뒤에는 명확한 활성 색상과 포인터를 표시한다.
> 이미 `prepared`·`sent`·`unknown` 원장이 있는 후기는 gateway와 DB unique index 양쪽에서
> 재발송을 막으며 ERP에는 상품명·발송 시각·주문번호와 재발송 불가 상태를 지속 표시한다.
> 별도 migration이나 운영 원장 수정 없이 소스 `263f6de5a5cb5352c930306c7f78bec0dce1aee1`,
> 릴리스 `20260818T062700Z-gift-coupon-delivery-state-v1`로 ERP·gateway에 운영 반영했다.
> 2026-08-18 후보에서는 대표번호 수신문자를 최근 발송 직원과 현재 상담 담당자에게
> 우선 배정하고, 연결할 수 없는 번호는 활성 관리자에게 배정하는 직원별 미읽음 원장을
> 추가했다. ERP 문자 메뉴 숫자·토스트·브라우저 알림은 같은 대상 원장을 사용하며 문자
> 대화를 열면 해당 직원의 그 대화만 읽음 처리한다. 실시간 SSE에는 전화번호와 본문을
> 싣지 않고 직원별 권한 확인 뒤 상세 API에서 고객 표시명과 이동 경로만 반환한다.
> migration은 `0067_goofy_mongoose.sql`이다.
> 후기관리 목록은 각 후기의 쿠폰 발송 원장을 함께 판정해 실제 `sent` 원장이 있으면
> `발송완료`, 그 밖의 미발송·실패·취소·결과 확인 중 상태는 `발송대기` 배지로 표시한다.
> 목록 카드별 추가 요청은 만들지 않는다.
> 2026-08-18 최초 복원 경로는 기존 담당자 변경 원장을 재사용해 진행 중 상태와 기존 ERP
> 배정을 요구했으나, 다음 날 실제 `closed`·미배정 무효 사례를 확인해 위 v1.56 계약으로
> 교정했다.
> 2026-08-18 후보에서는 전화 후처리의 개인 자동문자를 무조건 발송하지 않고 결과별 체크박스로
> 명시적으로 선택한다. 같은 상담·사건과 발송 조건의 성공 또는 처리 중 이력이 있으면 기본
> 체크를 해제하고 시각·상태를 표시하며, 최초 또는 확정 실패는 기본 체크한다. `상담완료`에는
> 후기관리 기본 템플릿 `상담을 받은 뒤`와 개인 후기 링크 발송을 같은 규칙으로 추가하고,
> 템플릿 출처와 본문 미리보기를 후처리 화면에 명시한다. migration은 없다.
> 2026-08-18에는 후기 제출 완료 화면이 새 DOM commit 뒤 감사 메시지 영역으로 스크롤되고
> 완료 제목에 포커스되도록 정리했다. 홈페이지·ERP의 주요 화면 전환도 새 페이지 최상단 또는
> 관련 콘텐츠 영역으로 스크롤·포커스를 이동하되, 검증 실패와 과거 열람 중 갱신은 현재 위치를
> 보존하는 것을 전역 UX 기준선으로 확정했다.
>
> 2026-08-18 운영 반영: ERP 상담페이지의 직원 신규등록과 전화데스크 통화 후처리의
> `신건상담으로 저장`에 선택 입력 `전달사항`을 추가했다. 두 경로 모두 최대 2,000자의
> 같은 계약을 사용하고, 내용은 기존 상담 요청 `intake`와 함께 AES-GCM 암호화해 보존한다.
> 상담 상세의 고객 핵심정보 카드 하단에는 가장 최근에 기록된 전달사항을 줄바꿈 그대로 표시한다.
> 별도 평문 컬럼과 migration은 추가하지 않는다.
>
> 2026-08-18 운영 반영에서는 직원별 문자 템플릿에 담당자배정·부재 및 무응답·통화중·
> 담당자 연결 요청·거절 자동발송 조건을 하나씩 연결한다. 같은 직원의 한 조건에는 템플릿
> 하나만 허용하고, 상담하기 담당 배정과 전화 후처리 저장을 멱등 문자 요청으로 연결한다.
> 담당자 연결 요청은 템플릿 변수 입력을 요구하지 않고 확정된 30분 재연락 일정과 담당자명을
> 본문 뒤에 고정 형식으로 붙인다. 자동발송이 설정된 후처리 결과에서는 수동 문자 동작을
> 발송 예정 안내로 교체해 중복 발송을 막는다. migration은 `0064_real_alex_power.sql`이다.
>
> 2026-08-18에는 승인된 기프티쇼 비즈 상용 API를 후기 상세의 모바일쿠폰 화면에 연결하는
> 운영에 반영했다. 승인 계정의 실제 상품 목록에서 메가MGC커피 더블 아아 세트,
> 네이버페이 포인트 1만원, 배달의민족 모바일상품권 3만원의 상품코드·가격·판매 상태를
> 확정했다. 발송 직전 상세 재검증, 한 후기당 활성 1건, 요청 멱등키·25자 이하 거래 ID,
> 암호화 수신번호·감사 원장, 응답 유실 시 동일 거래 ID 취소를 migration
> `0065_review_gift_coupons.sql`과
> gateway에 구현했다. 운영 비밀은 gateway에만 두며 실제 배포 검증에서는 외부 발송이나
> 과금을 만들지 않았다.
>
> 상태: 홈페이지 구현 진행. 배포·데이터 보관 기본 방향과 개인회생·파산 홈페이지
> 제품·UX 기준선, 정식 도메인, 홈페이지·리걸프렌즈 역할 경계를 확정하고 `/bank`
> 모바일 우선 홈페이지, `/bank/personal-rehabilitation/eligibility` 신청자격과
> `/bank/compare` 개인회생·개인파산 비교, `/bank/personal-rehabilitation/process`
> 개인회생 절차·기간, `/bank/personal-bankruptcy/eligibility` 개인파산 신청자격
> 및 `/bank/personal-rehabilitation/documents` 개인회생 필요서류,
> `/bank/personal-rehabilitation/repayment` 개인회생 변제금,
> `/bank/personal-bankruptcy/process` 개인파산 절차·기간과
> `/bank/personal-bankruptcy/documents` 개인파산 필요서류 랜딩페이지 초안을
> 구현했으며, `/bank/situations/collection-and-seizure` 독촉·압류 대응 상황
> 랜딩페이지와 `/bank/situations/investment-debt` 주식·코인 채무 상황
> 랜딩페이지, `/bank/situations/self-employed` 자영업자 개인회생 상황
> 랜딩페이지까지 연결했다. 이후 `/bank/personal-rehabilitation`,
> `/bank/personal-bankruptcy`, `/bank/situations` 카테고리 허브와 그룹형 전역
> 메뉴를 추가해 상세 URL을 유지한 채 탐색 구조를 정리했다. 회생·파산 비교는
> 후기·사례와 분리한 독립 1차 메뉴로 제공한다. 기존 Cafe24 KBoard의 회생·파산 후기
> 3,403건을 전용 PostgreSQL 원장으로 이관했고, 삭제 43건과 개인정보 패턴 검수 대기
> 1건을 제외한 3,359건을 `/bank/reviews`에서 분야·작성 당시 단계·고객 경험 키워드별로
> 탐색한다. `/bank/reviews/write`에서는 기존 고객이 별점 없이 분야·작성 당시 단계·
> 경험 키워드·후기 원문을 남긴다. 휴대전화와 원문은 공개 원장과 분리해 암호화하고
> 자동 공개하지 않으며 운영 검수 대기로 접수한다. `/about`은 네 가지 질문에 답하면서
> 방문자의 관심 기준에 맞춰 로앤의 일하는 방식과 확인 가능한 기록을 살펴보는 소개
> 페이지다. 답은 저장·전송하지 않고 공개 후기 원장의 실시간 집계, 실제 사무소·법인
> 정보, 사람의 판단과 자동화의 경계를 함께 공개한다. `/bank/consultation`에는 빠른 상담
> 요청과 상세 상황 남기기, 실제 접수 완료 흐름을 구현했다. 카카오톡 채널 1:1 채팅은
> 이 두 접수 방식 안의 세 번째 모드로 섞지 않고 상담 요청 옆의 독립 보조 채널로
> 연결했다. 상담 접수정보는 상담
> 요청일로부터 1년 보관한다. 필수 거주 시·도는 관할·지역별 업무 특성의 1차 확인,
> 담당 상담 배정과 지역별 상담·캠페인 운영 분석에 사용하고, 연락정보·상담 원문 없이
> 요청별 광고 귀속과 결합한 집계만 분석에 제공하는 기준을 확정했다. `apps/erp`, `apps/gateway`,
> `packages/core`, `packages/db`의 첫 골격과 상담·중복·광고 귀속·방문 여정·outbox
> 스키마 및 이벤트 계약 v1을 구현했다. 로컬 PostgreSQL에는 영구 개발 DB `lawand_dev`와
> migration·runtime·DBeaver 조회 역할을 분리해 마이그레이션을 적용했다. 홈페이지
> same-origin POST부터 gateway의 AES-GCM 암호화·HMAC 중복 판정·여정/귀속·상태/outbox
> 단일 트랜잭션, ERP 상담 목록·상세까지의 첫 실제 수직 흐름도 연결했다. ERP에는
> 초대 전용 직원 가입·로그인·서버 세션·역할과 상담 PII 조회 감사 v1을 연결했다.
> 2026-08-04에는 ERP 상담 목록을 단순 테이블에서 배정 대기·내 담당·확인 필요·오늘
> 접수 지표와 검색·필터를 갖춘 작업 큐로 재구성했다. 상담 상세는 고객 전화·출처·중복
> 판정·담당자·다음 행동을 첫 화면에 모으고, 요청 이력·자가진단 입력과 고객에게 실제로
> 표시한 유사사건 다섯 건·외부 실행 원장을 아래에서 순서대로 확인하게 했다. 직원 ID를
> 목록 응답에 포함해 동명이인과 무관하게 `내 담당`을 판정한다. 전화번호가 리걸프렌즈
> 고객찾기의 삭제되지 않은 사건 고객과 완전히 일치하면 목록·상세에 `기존고객` 배지를
> 표시해 재상담임을 접수 단계에서 바로 알린다. 다만 같은 상담의 `상담하기`가 새로 만든
> `legalfriends_case_links.case_idx`는 이 판정에서 제외하고, 제외 뒤에도 과거 사건이
> 남을 때만 `기존고객`으로 표시한다. 목록의 기존고객 배지에는 그 과거 사건들에서 확인한
> 주·부 담당자명을 중복 없이 함께 표시하고, 우측 담당자는 이번 ERP 상담의 현재 배정만
> 계속 나타낸다. 담당자명은 기존고객 판정과 같은 일괄 디렉터리 조회 결과를 재사용해
> 상담별 추가 조회를 만들지 않는다. 이번 상담의 신건 생성 성공은 고객 이력 판정과 분리한
> `리걸프렌즈 등록 완료` 배지로 표시한다. 상담 작업 큐에서는 직원이 신규 고객을
> 직접 등록하거나 `기존고객`·`소개건`을 선택해 리걸프렌즈 고객찾기 결과를 연결한다.
> 세 경로 모두 암호화 상담 원장과 개인정보 없는 `consultation.requested` 이벤트를 같은
> 트랜잭션으로 만들며, 신규 접수 알림과 미배정 `상담하기` 흐름을 그대로 사용한다.
> ERP 전역에는 초기
> 깜빡임을 막는 라이트·다크 테마와 모바일 카드형 목록, 전화·복사 동작, 하단 고정
> `상담하기`를 적용해 390px급 화면에서도 핵심 상담 업무를 이어갈 수 있게 했다.
> 새 상담과 상담 상태 변경은 gateway의 인증된 SSE로 ERP에 즉시 전달한다. PostgreSQL
> outbox INSERT 트리거가 개인정보 없는 이벤트 식별자만 `LISTEN/NOTIFY`로 알리고,
> ERP 브라우저는 same-origin 서버 프록시를 통해 스트림을 구독한 뒤 이벤트가 올 때만
> 최신 목록을 다시 읽는다. 연결·재연결 시 한 번 동기화해 중단 구간의 누락을 보정하며,
> Redis나 주기적 HTTP 폴링은 사용하지 않는다.
> 2026-08-05에는 LG U+ 고급형 센트릭스 A타입 클릭투콜의 로컬 출시 후보를 구현했다.
> ERP는 전화번호가 있는 상담에서 인증 직원의 활성 주 회선으로 발신 명령을 만들고, 개인정보 없는
> `telephony.call.requested` outbox와 별도 통화 원장을 같은 트랜잭션으로 저장한다.
> gateway 전용 워커는 고객 번호를 발신 직전에만 복호화해 POST body로 전달하고,
> 계정 단위 직렬 처리와 30초 진행 중 명령 재사용, 불명확한 결과의 자동 재시도 금지로
> 중복 발신을 막는다. 회선 연결 도구는 `userinfo`의 실제 070번호·내선이 입력값과
> 일치할 때만 DB를 변경한다. 처음 전달받은 로그인은 다른 회선을 반환했지만, 이후
> 정정된 API 로그인은 요청 회선·내선과 정확히 일치했다. 로컬 직원 주 회선 연결까지
> 완료했고 비밀번호 원문·SHA-512 값은 저장하지 않았다. 로컬 통제 발신 5건에서는
> `callhistory`를 요청 시각·마스킹 번호로 대사해 연결 여부와 전체·연결·호출 시간을
> 모두 복원했다. 통화가 연결되면 ERP에서 후처리 화면을 열 수 있어 메모·다음 일정을 먼저
> 저장할 수 있고, 종료 뒤 실제 결과를 다시 확인하거나 수정한다. 인증 직원 누구나 고객 상담·
> 음성사서함·무응답·거절·통화 중·발신 취소·재상담 필요 중 실제 결과를 확정하며,
> 선택값과 직원·
> 시각을 감사 원장에 남긴다. 센트릭스는 사람과 음성사서함, 통화 중과 발신 취소를 각각
> 같은 상태로 반환할 수 있어 이 세부 분류는 자동 추정하지 않는다. 운영 RDS migration은
> 적용됐고 2026-08-06에 Secrets Manager 자격증명과 워커도 활성화했다. 실제 운영
> 클릭투콜 canary와 변경 예정 비밀번호의 secret 갱신은 남아 있다.
> 2026-08-06에는 별도 임시 Windows Server 2022 x64 EC2에서 공식 32비트 OpenAPI OCX를
> WOW64로 등록하고 실제 수신·받기 canary를 완료했다. 통제된 휴대전화 수신 한 건에서
> `RINGEVENT(ISDIAL=0) → Answer() → CHANNELLIST → CHANNELOUT`이 순서대로 수신됐고,
> 실제 센트릭스 전화기가 자동으로 스피커폰을 열어 양방향 통화되는 것을 사용자가
> 확인했다. 따라서 `Answer()`는 PC 음성 스트리밍이 아니라 물리 전화기의 받기 동작을
> 제어한다. 진단 HTA는 다음 수신 한 번만 받도록 제한하고 발신번호를 끝 4자리만
> 기록했으며 센트릭스 비밀번호는 파일·DB·SSM·Git에 저장하지 않았다. 같은 날 진단 HTA를
> 운영에 재사용하지 않는 전용 Windows bridge 1단계를 구현했다. .NET Framework 4.8 x86
> WinForms STA·ActiveX host·메시지 루프, 회선별 단일 프로세스, 재접속 backoff, Windows
> 자격 증명 관리자, 마스킹 로그, interactive logon 작업 스케줄러를 적용했다. 실제 EC2에서
> `HOST_READY → LOGIN_RESULT(성공) → RING_EVENT → CHANNEL_OUT`을 확인했고, 자동 받기는
> 넣지 않았다. gateway 인증·이벤트 멱등 원장과 ERP 표시·명시적 받기는 다음 단계다.
> 같은 날 수신 연동 2단계로 bridge→gateway 이벤트 수직 흐름을 운영 canary에 배포했다.
> Windows bridge는 수신 이벤트를 현재 사용자 DPAPI 암호문 큐에 먼저 보존하고 HTTPS
> 요청마다 bridge ID·Unix 시각·난수 nonce·본문 SHA-256의 HMAC-SHA256 서명을 붙인다.
> gateway는 5분 시각창과 고정 bridge→endpoint 매핑을 확인한 뒤 발신번호를 AES-GCM
> 암호화·HMAC 지문화하고 provider call ID·event ID·nonce 원장으로 재시도를 멱등 처리한다.
> 운영 RDS에는 별도 `telephony_inbound_calls`·`telephony_inbound_events`를 추가했으며
> 상담과 연결되기 전 통화도 독립적으로 보존한다. 배포 전 암호화 스냅샷을 확보했고,
> gateway와 Windows 프로세스가 정상인 상태에서 실제 신규 수신 한 건의 end-to-end
> 원장 canary도 완료했다. 약 12.9초 울린 뒤 발신자가 끊은 통화가 연결 이벤트 없이
> `ringing → ended(HCAUSE=16)` 두 이벤트로 남았고, 발신번호 암호문·nonce·HMAC 지문의
> 길이와 서버 복호화·지문 재계산이 모두 일치했다. 첫 전송에서 발견한 Windows 기본
> proxy 경로 의존과 센트릭스 sibling leg ID 불일치는 bridge v0.2.1에서 직접 HTTPS·
> TLS 1.2 및 같은 prefix의 인접 sequence 매칭으로 고쳤다. 보존된 DPAPI 큐를 201로
> 전달하고 같은 종료 이벤트 재전송이 200·원장 2건 유지로 끝나는 것까지 확인했다.
> 같은 날 수신 3단계로 운영 ERP 전역 수신전화 표시를 배포했다. 수신 이벤트 INSERT가
> 커밋되면 개인정보 없는 event ID·call ID·상태·시각만 PostgreSQL `NOTIFY`로 보내고,
> gateway의 전용 `LISTEN` 연결과 인증된 SSE, ERP same-origin 프록시를 거쳐 모든 직원
> 화면의 상단 수신 바를 갱신한다. 실제 표시는 전체 발신번호·내선·회선 담당자와
> `수신전화/통화 중/통화 종료`만 포함한다. 스트림 연결·재연결 때 권한 있는 스냅샷을
> 다시 읽어 누락을 복구하며 주기적 polling은 하지 않는다. 운영 RDS 스냅샷과 migration
> `0027_telephony_inbound_sse_notifications.sql`, gateway·ERP 릴리스
> `20260806T020118Z-centrex-inbound-step3`을 적용했다. 커밋 후 바로 삭제되는 비식별 canary로
> `sync → changed`, ERP 프록시 200, SSE·스냅샷 PII 0건과 원장 잔존 0건을 확인했다.
> 이후 실제 신규 수신을 사용자가 ERP에서 확인했다. 최신 건은 약 13.6초 울린 뒤 연결 없이
> `ringing → ended(HCAUSE=16)`로 자연 종료됐고 bridge 프로세스 1개·DPAPI 큐 0건을
> 유지했다. 사용자 운영 기준에 따라 인증된 ERP 스냅샷은 번호를 복호화해 전체 표시하되,
> DB는 AES-GCM 암호화 상태를 유지하고 SSE·로그에는 번호를 넣지 않는다. 같은 시각 여러
> 통화가 오면 통화 ID별 카드로 모두 반환·표시하도록 고정하고 릴리스
> `20260806T022927Z-centrex-inbound-full-number`를 gateway·ERP에 배포했다.
> ERP `상담하기` 본인 담당 배정과 리걸프렌즈 등록·알림톡 실행 요청 outbox까지
> 구현했다. 공개 상담 POST는 홈페이지 서버만 gateway에 전달할 수 있고 IP 원문을
> 저장하지 않는 전화·네트워크 다단계 rate limit과 정상 멱등 재시도 예외를 적용했다.
> 리걸프렌즈 실행 요청에는 선점 임대·재시도·시도별 실행 원장과 ERP 상태 표시를
> 연결했고, 관리자가 직원별 리걸프렌즈 로그인 ID와 숫자형 `member_idx`를 초대·직원
> 화면에서 함께 관리한다. 신건 등록은 `createForLawnV2`에 배정 직원의 `member_idx`를
> 보내고, 성공 응답의 `case_idx`·`case_id` 또는 숫자형 `data`를 사건 연결 원장에
> 보존한다. 2026-07-30 실제 V2 canary에서 사건 `202130`이 생성됐고 별도 담당자 변경
> 없이 최초부터 김충환(`lawandfirm_s`, `member_idx=138`)에게 배정됐다. 이후 담당자
> 변경에는 저장한 사건 ID와 기존 `member_id` 기반 `changeManager`를 사용한다.
> 2026-07-31 사용자 승인으로 로컬 개발 gateway의 리걸프렌즈 워커를 다시 활성화했다.
> 외부 멱등성 계약은 아직 확인되지 않았으므로 응답 유실·timeout의 확인 필요 원장을
> 계속 운영하고, 운영 배포의 안전성 확인 항목은 별도로 남긴다. Solapi는 승인된
> 법무법인 로앤 카카오 채널과 상담 접수·담당 배정 템플릿을 연결했다. 새 상담이 최초
> 생성되면 접수 알림톡을, ERP에서 담당자가 배정되면 담당자 배정 알림톡을 각각 독립
> outbox로 발송한다. 문자 대체발송은 사용하지 않고 솔라피 그룹·메시지 ID만 별도 원장에
> 보존한다. 2026-07-30 실제 canary 두 건이 모두 최종 `COMPLETE/4000/수신 완료`로
> 확인됐다. 2026-08-04에는 별도 Solapi 자격증명을 Git 제외·권한 600인 로컬 gateway
> 환경파일에 연결하고 알림톡 워커를 활성화했다. 리걸프렌즈·알림톡 워커가 함께 시작되고
> gateway health가 정상이며, 초기화된 외부 실행 대기열이 0건이라 과거 테스트 상담의
> 소급 발송·등록이 없음을 확인했다. 2026-08-04 운영 gateway의 탄력적 송신 IP는
> `3.36.255.226`으로 확정하고 운영 secret을 별도로 분리했다. Solapi 키의 허용 범위는
> 사용자 지시에 따라 아직 `0.0.0.0/0`이며 정식 도메인 전환 전 해당 EIP 하나로 제한한다.
> 카카오 챗봇 운영 연결 실험에서는 고객의 자유 메시지가 봇에 의해 자동
> 읽음 처리되고 채널 상담원 화면에 정상적으로 나타나지 않는 운영 위험을 확인했다.
> 따라서 운영 채널에서 챗봇을 해제하고 기존 채팅방 리스트 메뉴를 복구했다. 현재
> 자동화 범위는 홈페이지의 카카오 CTA로 한정한다. 고객이 홈페이지에서 카카오 버튼을
> 누르면 client-only 모달이 `이름 또는 카카오톡 표시명`과 광역 거주지역을 필수로 받고,
> 휴대전화 번호는 `선택 · 입력하지 않으셔도 돼요`로 명시해 부담 없이 선택 입력하게 한다.
> 제출 직전에는 채팅방을 열기만 해서는 상담원에게 접수되지 않으며
> `홈페이지에 입력한 이름은 {입력 이름}입니다. 상담 요청합니다.`라는 첫 메시지를 보내야
> 한다는 고대비 안내를 항상 표시하고, 제출 버튼도
> `카카오톡 열고 상담 메시지 보내기`로 명시한다. 확인 시
> same-origin POST가 필수값과 선택 전화번호를 암호화한 ERP 상담을 만든 뒤 기존 1:1 채팅
> URL로 이동시킨다. 카카오 채팅방 진입 성공·고객 식별자는 일반 채널 링크로 받을 수
> 없지만, 직원은 고객 입력 이름으로 메시지를 찾아 목록에서 바로 `상담하기`를 누른다.
> 이 동작은 채팅 확인과 본인 담당 배정을 한 트랜잭션으로 처리한다. ERP 이름은
> `표시명_<접수번호 8자리>_플친`으로 암호화 저장하며, 메시지가 없는 진입 건은
> `미진입·무효`로 종결한다. 선택 전화번호는 실제 번호를 암호화해 ERP에 표시하고, 미입력 때만
> `010-0000-0000 · 미수집`으로 표시한다. 접수·배정 알림톡은 만들지 않지만,
> `상담하기`는 입력 번호를 사용하고 미입력 때만 외부 payload에 `010-0000-0000`을 합성해 고객이 선택한 지역과 로그인 직원의
> `member_idx`로 리걸프렌즈 신건을 등록한다. 배정 전 무효는 무효 담당자
> `lawandfirm_s999`·`member_idx=1824`로 바로 신건 등록하고, 배정 후 무효는 생성된 사건의
> 담당자를 같은 무효 계정으로 변경한다. 네이버 예약은 공식 IMAP 확정 메일 내용만으로 전화번호 없는
> ERP 상담을 만든다. 메일의 마스킹 이름·예약시각·상품·요청사항과 상세 링크를 보존하고,
> 전화번호는 실제 `NULL`, ERP에서는 `010-0000-0000 · 미수집`으로 표시한다. 네이버
> 로그인 세션·2단계 인증·CAPTCHA에 의존하는 브라우저 상세수집은 운영 신뢰성이 낮아
> 2026-07-31 사용자 결정으로 전부 철회했다. Playwright·브라우저 프로필·웹 로그인
> 자격증명·텔레그램 연동과 관련 DB 스키마도 제거했다. 다음 단계는 IMAP 기준점 이후
> 실제 신규 예약 canary, 카카오 운영자 canary, 리걸프렌즈 외부 멱등성 확인, Solapi
> 최종 결과 자동 수신과 개인정보 고지 승인이다. 리걸프렌즈 성공사례 원천
> `CB.TblCBCase` 9,598행은 2026-07-31 로컬 MySQL의 별도 `CB` 스키마와 Git 밖의
> 권한 제한 백업으로 복제·검증했고, 2026-08-03에는 로컬 PostgreSQL `lawand_dev`의
> 별도 `CB` 스키마에도 같은 35개 컬럼·9,598행을 이관했다. 이 원천에는 이름·전화·
> 사건번호가 포함되므로 홈페이지나 자가진단이 직접 조회하지 않는다. 2026-08-04에는
> 리걸프렌즈 RDS의 `CONTENT.TblCaseMemo`를 로컬 PostgreSQL
> `lawand_dev`의 비공개 `CB.TblCaseMemo`로 별도 복제했다. 이 원천은 계속 갱신되므로
> 각 실행은 일관된 스트림 스냅샷을 새 임시 테이블에서 검증한 뒤 교체한다.
> 같은 RDS의 `CONTENT.TblMoClientStatement`도 18개 컬럼·9,402행을 로컬 PostgreSQL
> `lawand_dev`의 비공개 `CB.TblMoClientStatement`로 복제했다. `idx` 기본키와
> `Case_idx`·`phone` 검색 인덱스를 보존하며, 전화·주소·채무상담 원문이 포함될 수 있어
> 홈페이지·gateway 런타임에는 연결하지 않는다. 2026-08-05에는 로컬 PostgreSQL의
> 검증된 세 `CB` 테이블을 운영 RDS의 동일한 비공개 `CB` 스키마로 이관했다.
> `TblCBCase` 9,598행, `TblCaseMemo` 202,772행, `TblMoClientStatement` 9,402행의
> 행 순서 기반 논리 해시·키 범위·최종 수정시각이 로컬과 운영에서 일치한다.
> 2026-08-06에는 수신전화 고객 해석의 리걸프렌즈 차선 원천으로 로앤 사무소
> `Office_idx=56`만 선별한 `CB.TblCSClient`·`CB.TblCase`·`CB.TblMember`를 로컬과 운영
> RDS에 추가했다. 동일한 MySQL consistent snapshot 기준 고객·사건 각 60,947건과 담당자
> 69건이며 로컬·운영 행별 논리 해시가 일치한다. 고객은 이름·전화 검색 필드, 사건은
> 수신 snapshot은 상담데스크 전화 HMAC 일치 건을 먼저 찾고, 없을 때만 `CB` 원천을
> security-definer 최소 조회 함수로 검색한다. gateway에는 이 함수 실행만 허용하며
> `CB` 테이블 직접 권한은 계속 주지 않는다. ERP에는 상담데스크 일치 시 고객·상태·담당자와
> 상세 이동 링크를, 차선 일치 시 고객명·사건 유형·진행 상태·담당자를 표시한다.
> 사건은 유형·상태·주/부 담당자와 표시 필드, 담당자는 식별자·이름·직책만 보존한다.
> 다른 사무소 데이터와 회원 비밀번호·생년월일·개인 연락처, 사건 계좌정보는 복제하지
> 않는다. 운영 RDS migration과 gateway·ERP 릴리스
> `20260806T031115Z-inbound-directory`까지 배포했다. 이후 실제 수신 원장을 같은 시각의
> 운영 snapshot으로 재현해 상담데스크 우선 일치와 고객명 `김충환3_테스트` 반환을
> 확인했다. 당시 열린 ERP 탭은 고객 해석 배포 전 JavaScript bundle을 계속 실행해
> 통화 카드만 표시하고 고객 정보를 갱신하지 못한 것으로 판정했으며, 새 릴리스 뒤 한 번의
> 강력 새로고침이 필요하다.
> 같은 날 명시적 `전화 받기` 운영 흐름도 완성했다. 회선 담당 직원이 아직 `ringing`인
> 자기 회선 통화에서만 20초 유효한 `answer` 명령을 만들 수 있고, 활성 명령은 통화별
> 하나로 멱등 처리한다. Windows bridge는 750ms 간격의 HMAC 서명 pull로 명령을 받고
> WinForms STA 메시지 루프에서 OCX `Answer()`를 호출한 뒤 성공·실패 결과를 서명해
> 돌려준다. 요청·배포 시도·결과·직원·시각은 `telephony_inbound_commands`와 감사 원장에
> 남으며, 정확한 HTTP method·path·body hash와 5분 시각창·nonce 재사용 방지·고정
> bridge/endpoint 연결을 모두 검증한다. 자동 수신과 PC 음성 스트리밍은 하지 않는다.
> 운영 RDS 스냅샷 `lawand-prod-pre-centrex-answer-20260806` 뒤 migration
> `0029_powerful_captain_stacy.sql`을 적용하고 gateway·ERP를 릴리스
> `20260806T035011Z-centrex-answer`, Windows bridge를 v0.3.0.0으로 배포했다. 세 런타임과
> HTTPS·gateway health가 정상이고 재시작·CloudWatch ALARM·잔존 명령은 모두 0건이다.
> 첫 운영 재시험에서 고객 해석은 정상이나 `staff_telephony_bindings`가 0건이라 버튼의
> 회선 소유자 조건을 만족하지 못한 것을 확인했다. 운영의 유일한 활성 직원 김충환과
> 활성 대표전화 내선 4591을 주 회선·활성 상태로 연결하고
> `telephony.centrex_endpoint.linked` 감사 원장을 남겼다. 이후 수신 snapshot은 이 직원의
> 내부 ID를 회선 소유자로 반환하므로 같은 ID로 로그인한 ERP에서만 `전화 받기`를
> 표시한다. 직원·회선이 늘어나기 전 관리자용 회선 배정 화면을 별도 구현해야 한다.
> 직원관리에는 내선이 아닌 `070` 전체 센트릭스 회선번호를 추가했다. 초대 생성 시
> 선택적으로 입력하고 초대 대상자가 읽기 전용으로 확인한 뒤 가입을 완료하면
> `staff_profiles.centrex_line_number`에 숫자 11자리로 보존한다. 하이픈 입력은 서버에서
> 제거하며 070이 아니거나 전체 11자리가 아니면 core·DB 양쪽에서 거부한다. 기존 직원은
> 관리자 화면에서 번호를 수정·해제할 수 있고 변경 여부와 마지막 4자리만 감사 원장에
> 남긴다. 전체 번호는 직원 원장의 지정값이며 `staff_telephony_bindings`를 자동 생성하거나
> 전화 제어 권한을 바꾸지 않는다. migration `0030_outgoing_garia.sql`은 기존 활성 주 회선
> binding이 있는 직원 한 명의 전체 번호를 안전하게 백필했다. 운영 RDS 스냅샷
> `lawand-prod-pre-staff-centrex-line-20260806` 뒤 gateway·ERP 릴리스
> `20260806T045120Z-staff-centrex-line`을 배포했고, 운영 프로필–binding 일치 1건과 받기
> 명령 0건을 확인했다. 2026-08-07에는 직원 원장의 센트릭스 값을 전체 회선번호와 내선번호
> 한 쌍으로 확장하고, 직원관리에서 회선의 현재 비밀번호까지 함께 받아 U+ `userinfo`가
> 반환한 전체 회선·내선과 정확히 일치할 때만 endpoint와 주 회선 binding을 생성·교체하도록
> 운영 배포했다. 비밀번호 원문은 요청 메모리에서만 사용하고 즉시 SHA-512로 변환하며,
> endpoint ID를 AAD로 한 AES-256-GCM 암호문만 전용 인증 테이블에 저장한다. 일반 viewer와
> PUBLIC은 이 테이블을 조회할 수 없고 기존 Secrets Manager 자격증명은 DB 값이 없는 동안의
> 무중단 읽기 fallback으로 유지한다. 일치 검증에 실패하면 프로필·endpoint·binding을 전혀
> 바꾸지 않으며, 두 값을 함께 비우면 전화 제어 배정도 해제한다. 직원 원장은 희망 소유자
> 정보, endpoint·binding은 검증된 실제 제어 권한이라는 내부 분리는 유지하지만 직원관리
> UI에서는 회선·내선·비밀번호 검증, 실제 endpoint와 gateway bridge 배정 상태를 한곳에서
> 관리한다. migration `0033_icy_starfox.sql`과
> `0034_smooth_pandemic.sql`은 운영 RDS 스냅샷
> `lawand-prod-pre-centrex-credentials-20260807` 뒤 적용했고 gateway·ERP 릴리스
> `20260807T011028Z-centrex-staff-credentials`를 배포했다. 운영 화면은 직원 지정값
> 07046074535·내선 4535와 기존 실제 제어 endpoint 07046074591·내선 4591의 차이를
> `배정 불일치`로 정확히 표시했다. 이후 운영 직원 지정값과 주 회선은 다시 4591로
> 복구됐다. 직원 한 명은 활성 Windows bridge 하나만 점유하고 회선을 바꿀 때 같은 bridge
> 슬롯을 재설정하는 원클릭 모델을 확정했다. migration `0035_natural_greymalkin.sql`의
> `staff_telephony_bridge_assignments`는 직원·bridge 각각 활성 1개만 허용하고 현재·대기
> endpoint, heartbeat, 실제 로그인 결과를 보존한다. 직원관리 저장은 U+ `userinfo` 검증과
> 클릭투콜용 SHA-512 암호문 저장 뒤, raw 비밀번호를 DB·로그·Secrets Manager에 남기지 않고
> bridge HMAC secret으로 암호화한 단기 명령을 기존 bridge에 전달한다. bridge v0.5.0은 활성
> 통화가 없을 때만 Windows 자격 증명과 endpoint 설정을 교체하고 실제 OCX 로그인 회선·내선이
> 일치해야 성공으로 보고한다. 실패·시간초과에는 이전 자격증명·endpoint를 복구하고 ERP의
> 직원 원장·binding도 이전값으로 보상한다. 15초 제한 heartbeat로 온라인 여부를 별도
> 판정하며 실제 로그인 검증 전에는 `브리지 설정 대기`, 성공 뒤에만 `전체 전화 연결 완료`다.
> 운영 스냅샷 `lawand-prod-pre-centrex-oneclick-20260807` 뒤 migration과 ERP 릴리스
> `20260807T022953Z-centrex-oneclick`, gateway 최종 릴리스
> `20260807T023907Z-centrex-oneclick-status`, Windows bridge v0.5.0.0을 배포했다. 이후
> 관리자가 직원관리에서 4535 회선·내선·현재 비밀번호를 저장했고 같은 bridge 슬롯의 실제
> OCX 로그인, ERP 클릭투콜로 4535 물리 전화기 호출, 4535 수신 실시간 표시와 ERP 받기까지
> 확인했다. 따라서 직원별 원클릭 연결은 실제 운영 canary를 통과했다.
> 이후 다른 회선으로 전환할 때 직원관리에는 `centrex_network_error`가 표시됐지만 Windows
> 안전 로그에는 새 회선 `LOGIN_RESULT(STATUS=1)`이 뒤늦게 도착하는 순서 역전을 확인했다.
> 원인은 회선 교체를 위해 호출한 `DisconnectServer()`의 비동기 `NetworkError`를 진행 중인
> 새 로그인 실패로 즉시 확정한 것이었다. bridge v0.5.1은 프로비저닝 중 network error를
> 비결정 신호로만 기록하고 재접속을 예약하며, 실제 `LoginResult` 또는 제한시간만 성공·실패의
> 최종 판정으로 사용한다. Windows x86 self-test 11개를 통과한 v0.5.1.0을 canary 서버에
> 배포했고 작업 Running, 프로세스 1개, 응답 프로세스 1개, DPAPI 큐 0건과 현재 회선 로그인
> 성공을 확인했다.
> 같은 날 직원 약 50명을 직원별 Windows 서버 없이 수용하도록 한 Windows 서버 안의
> 다중 bridge 풀로 확장했다. migration `0038_mute_wild_pack.sql`은 bridge 배정의 직원을
> nullable로 바꾸고 `idle` 상태와 소유권 제약을 추가한다. Secrets Manager registry에는
> 기존 검증 회선 1개와 논리 유휴 슬롯 50개의 bridge ID·placeholder endpoint·무작위 HMAC
> secret만 두며 센트릭스 계정은 넣지 않는다. gateway는 직원관리 저장 시 U+ `userinfo`를
> 먼저 검증한 뒤 PostgreSQL advisory lock 아래 최근 45초 heartbeat가 있는 유휴 슬롯 하나를
> 원자적으로 점유한다. raw 비밀번호는 암호화한 40초 명령으로만 전달되고 Windows Credential
> Manager에 저장된다. DB 저장이나 로그인에 실패한 신규 예약은 endpoint가 없는 조건을 다시
> 확인해 즉시 `idle`로 반환하므로 슬롯이 `failed`에 고착되지 않는다.
> Windows bridge v0.6.2는 인스턴스별 설정·로그·DPAPI 큐·mutex·작업 스케줄러를 격리한다.
> 전체 50개 작업 정의를 설치하되 감독기가 배정된 모든 슬롯과 유휴 5개만 실행한다. 실제
> 프로세스는 기존 4591, 새로 자동 배정된 1208, 유휴 5개로 7개이며 전체 working set 약
> 169.3MB, 큐 0건이었다. 1208은 U+ REST 검증, 유휴 슬롯 자동 점유, 실제 OCX
> `LOGIN_RESULT(STATUS=1)`, endpoint·주 binding, ERP `connected/online/credentialConfigured`
> 까지 운영 canary를 통과했다. OCX가 전체 070 로그인 ID를 `NotFound(-1)`로 반환하는 회선만
> 내선 PBX ID로 한 번 재시도하되 최종 회선·내선 일치 검증은 그대로 유지한다.
> 운영 RDS 스냅샷 `lawand-prod-pre-centrex-bridge-pool-20260807` 뒤 gateway·ERP 릴리스
> `20260807T090502Z-centrex-bridge-pool-v2`와 Windows v0.6.2.0을 배포했다. 풀은 총 51개
> 배정 원장, 연결 2개, 온라인 유휴 5개, 소유권 이상 0건이며 gateway·ERP·Caddy 재시작 0,
> error journal·CloudWatch ALARM·진행 중 전화 명령 0건이다. 설치용 Windows IAM 권한과
> 일회성 작업은 완료 후 제거했다. 현재 서버에서 warm 5개는 충분하지만 50개 전원이 실제로
> 배정되기 전에는 실부하 메모리 canary를 하고 필요하면 Windows 인스턴스 사양을 높인다.
> 2026-08-10에는 배정 직원 6명과 실제 업무 수·발신이 섞이는 조건에서 다중 bridge 운영
> 안정화를 배포·검증했다. bridge v0.7.0은 배정 슬롯 전부와 유휴 warm 5개를 정확히 맞추는
> 감독기, 1분 주기의 SYSTEM health monitor, gateway heartbeat 파일, 영구 거부 이벤트의
> 암호화 dead-letter 격리를 추가했다. 앞선 orphan 이벤트 하나가 뒤 정상 이벤트까지 막던
> DPAPI 큐 적체를 발견해 400/404/409/422 응답은 1분 동안 재시도한 뒤 격리하고 후속 이벤트를
> 계속 전송한다. 당시 암호문은 삭제하지 않고 별도 archive로 보존했으며 최종 활성 큐와
> dead-letter는 0건이다.
> migration `0039_chemical_captain_flint.sql`과 릴리스
> `20260810T012042Z-centrex-stability-v1`은 격리 슬롯 상태, 관리자의 온라인 유휴 슬롯 원자
> 재배정·기존 슬롯 reset, 직원관리의 `정상/연결 중/연결 실패/브리지 오프라인` 구분을
> 운영에 반영했다. CloudWatch에는 배정 bridge 오프라인, 로그인 실패, DPAPI 큐 적체,
> 감독기 이상, warm pool 부족 경보를 만들었고 큐 적체 실제 ALARM→OK 전환을 확인했다.
> 다만 SNS 구독이 없어 사람에게 보내는 통지는 수신처를 정한 뒤 별도로 연결해야 한다.
> 같은 t3.medium에서 10개 10분, 25개 10분, 50개 30분의 5초 간격 canary를 수행했다.
> CPU p95는 각각 59%/61%/58%, 최대는 76%/100%/100%, 최소 여유 메모리는
> 1411.63/1185.99/939.47MB였고 프로세스 유실이나 60초 지속 부족은 없었다. 50개 최대
> working set 1347.29MB, private memory 1208.95MB로 768MB 중단선은 넘지 않았지만 여유가
> 171.47MB뿐이므로 실제 배정이 50개에 가까워지기 전에 t3.large 상향을 우선 검토한다.
> 유휴 슬롯 프로세스를 강제 종료한 canary는 작업 스케줄러가 38초 안에 새 PID로 복구했다.
> bridge 프로세스와 감독기는 OCX의 interactive desktop 요구 때문에 Administrator 로그온
> 작업이며, SYSTEM health monitor만 부팅 시 무인 복구된다. `AutoAdminLogon`은 도입하지
> 않았고 평문 암호도 없다. 실제 재부팅 canary에서 2026-08-10 13:49 KST 부팅 뒤 SYSTEM
> monitor는 자동 실행됐지만 로그인 전 프로세스 0, 배정 6개 오프라인, warm 0, 감독기
> 비정상을 감지했고 배정 오프라인·warm 부족·감독기 이상 경보가 모두 `ALARM`이 됐다.
> 14:10 KST 관리자가 RDP로 로그인하자 logon trigger만으로 감독기와 배정 6개+warm 5개,
> 총 11개 프로세스가 복구됐고 큐·dead-letter 0, 모든 경보 `OK`를 확인했다. 한 회선은 약
> 2분 뒤 일시 `NotFound(-1)`이었지만 기존 backoff가 47초 뒤 재로그인에 성공해 2회 연속
> 로그인 실패 경보에는 걸리지 않았다. 따라서 평문 자동 로그온 없이도 `부팅 감지·경보 →
> 관리자 RDP 로그인 → 전체 자동 복구` 운영 절차는 검증됐지만, 매 재부팅 뒤 관리자 로그인이
> 필요한 한계는 유지한다. RDP 창은 로그아웃하지 말고 연결만 끊어 interactive session을
> 보존한다. 운영 실행 파일은 v0.7.0.0이고 빌드·설치 단계의 Authenticode 강제 경계는
> 구현했으나 서버에 조직용 코드 서명 인증서가 없어 현재 배포본은 아직 `NotSigned`다.
> 같은 날 한 직원 회선이 다른 장소의 중복 센트릭스 로그인으로 재접속되면서, 재접속 전
> 수신 통화는 ERP 원장에 `connected`로 남고 재로그인 뒤 직접 발신이 별도 활성 통화로
> 표시되는 문제를 확인했다. bridge v0.7.1은 네트워크 재접속·수동 연결 해제·회선 재설정·
> 프로세스 종료 전에 메모리의 활성 수신·발신을 각각 `ended` 보정 이벤트로 DPAPI 큐에
> 넣고 나서 상태를 비운다. 로그인 성공 시 남은 상태와 3분 지난 수신 ring도 같은 방식으로
> 보정한다. 외부 전화번호가 아닌 4자리 내부 leg는 ring payload 검증이 끝난 뒤에만 활성
> 상태를 만들도록 순서를 바꿔 고아 `connected/ended` 이벤트를 차단했다.
> gateway는 bridge와 U+ callback의 새 ring을 모두 endpoint advisory lock 아래 직렬화한다.
> 새 통화를 만들기 전에 같은 회선의 다른 `ringing/connected` 원장을
> `SUPERSEDED_BY_NEW_CALL`과 비식별 합성 종료 이벤트로 닫아, bridge 보정이 네트워크에서
> 유실돼도 회선당 활성 통화를 하나로 유지한다. 뒤늦은 실제 종료 이벤트는 합성 원장의
> 종료 원인을 실제 provider cause로 보강한다. 기존 김지안 회선의 수신 ghost 한 건은 확인된
> 재접속 시각으로 `BRIDGE_RECONNECT_RECOVERY` 종료 이벤트를 남겨 복구했다. gateway 릴리스
> `20260810T073937Z-centrex-active-call-v1`과 Windows bridge v0.7.1.0을 운영 배포했으며,
> gateway 78개·core 55개 테스트, 전체 typecheck·lint·production build, bridge x86
> self-test 16개와 로컬 DB ingress 통합 검증을 통과했다. 운영 최종 대사에서 회선별 활성
> 중복 0, 배정 11개+warm 5개 전부 온라인, 큐·dead-letter 0, 로그인 실패 0을 확인했다.
> 배포 전에 생성된 409 격리 암호문 6건은 삭제하지 않고 인스턴스별
> `gateway-dead-letter-archive`에 보존했고 CloudWatch 5종 경보는 모두 `OK`로 복귀했다.
> 실행 파일은 여전히 `NotSigned`다.
> 재택 U+ 비즈콜 앱은 같은 4535 회선 수신을 휴대전화에서 받지만 Windows OCX 이벤트가
> 없으므로 기존 bridge 전용 수신 바와 전화데스크에는 나타나지 않았다. 운영에서 실제 누락
> 시각을 U+ REST `getinboundcall`로 대사하자 `FAILED`, `CANCEL` 이력이 존재해 망 단위
> 통화 원장은 확보 가능했다. 공식 `setringcallback`은 수신 즉시 고객 서버를 HTTP로만
> 호출하고, `getinboundcall`은 종료 뒤 `ANSWERED/NO ANSWER/CANCEL/BUSY/FAILED` 상태와
> 시각·통화시간을 제공한다. gateway는 긴 비밀 `.html` 경로 하나만 HTTP로 받고 나머지는
> 기존 HTTPS redirect를 유지한다. 콜백의 발신번호는 즉시 AES-GCM 암호화·HMAC 지문화하며
> 수신 회선·내선·허용 필드를 검증하고 URL·로그·SSE에 원문을 남기지 않는다. 같은 회선·
> 발신번호·짧은 시각창의 Windows bridge 이벤트는 advisory lock 아래 기존 콜백 통화에
> 병합한다. 15초 이력 보정은 종료 상태를 확정하고 콜백이 누락된 비즈콜 통화도 전화데스크에
> 추가한다. ERP는 이를 `U+ 앱/망 수신`으로 구분하며 물리 bridge가 없는 통화에는 동작하지
> 않는 `전화 받기` 버튼을 표시하지 않는다. 기존 U+ `clickdial`은 실제 canary에서 비즈콜
> 앱을 울리지 않았고 공개된 앱 deep link·원격 발신 API도 확인되지 않았으므로 ERP
> 클릭투콜은 물리 전화기 제어로 유지하고 비즈콜 발신은 앱에서 직접 수행한다.
> 암호화 RDS 스냅샷 `lawand-prod-pre-centrex-bizcall-20260807`을 available까지 확인하고
> gateway·ERP 릴리스 `20260807T034220Z-centrex-bizcall`을 배포했다. 시작 즉시 4535의
> callback 등록이 성공했고 U+ 이력에만 있던 통화 4건을 보강했다. 같은 날 기존 물리 bridge
> 통화 1건은 그대로 한 건이며 같은 발신번호·분 단위 중복은 0건이다. 인증된 전화데스크
> canary는 200과 `U+ 앱/망 수신` 4건을 반환했고 임시 세션을 0건으로 정리했다. HTTP의
> 일반 경로는 301, 잘못된 callback 경로는 404, 비밀 callback 경로의 불완전 요청은 400이며
> 실제 경로 원문은 출력하지 않았다. gateway·ERP·Caddy active, 컨테이너 재시작·최근 error
> journal·CloudWatch ALARM은 0이고 Windows bridge 작업도 Running·프로세스 1개다. 이후
> 여러 실제 비즈콜 앱 수신을 canary하자 통화 중 callback은 0건이고 `channelstatus`도 계속
> `NO CHANNEL`이었으며, 종료 뒤에만 `getinboundcall`의 `CANCEL/NO_ANSWER/ANSWERED`가
> 생성됐다. U+에 저장된 callback의 회선·EIP·비밀 경로·포트·ring 종류는 모두 정확했다.
> 고대 callback 클라이언트의 Host 헤더 누락 가능성을 확인해 Caddy HTTP listener를 특정
> IP host에서 `:80`으로 바꾸고, IP Host·임의 Host·HTTP/1.0 Host 없음이 모두 비밀 경로로
> 전달되도록 교정했다. 일반 경로는 계속 301이고 앱·gateway 재시작은 0이다. 그러나 교정 후
> 실제 `ANSWERED` canary도 callback 없이 종료 54초 뒤 이력으로만 생성됐다. 따라서 공식
> `setringcallback`과 `channelstatus`는 AI비즈콜 앱 leg를 관측하지 않으며 서버 REST만으로
> 앱의 벨 시점을 얻을 수 없다고 확정한다. 현재 운영은 종료 이력·전화데스크 기록은 정상이고,
> 실시간 표시는 Android 알림 브리지 또는 U+가 제공하는 별도 기업 webhook/동시착신처럼
> 모바일 leg를 노출하는 추가 경로가 필요하다.
> 2026-08-11에는 U+ 수신 콜백의 밀리초 수신 시각보다 `getinboundcall`의 초 단위 시작
> 시각이 같은 초 안에서 앞설 때, `connected_at >= ringing_at` 제약에 걸려 `ANSWERED`
> 종료 이력이 반복 실패하고 전화데스크가 `ringing`에 머무는 문제를 교정했다. 이력 보정은
> 콜백·provider 중 이른 시각을 호출 시작으로, 이미 bridge가 관측한 연결 시각 또는 두 시작
> 시각 중 늦은 값을 연결 시각으로 사용해 시간 순서를 보장한다. bridge가 보낸 실제 연결
> 시각은 U+ 이력으로 덮어쓰지 않는다. 한 이력의 DB 오류도 같은 회선의 뒤 이력 처리를
> 중단하지 않으며, 오류 로그는 개인정보 없이 provider 상태와 DB 제약 분류만 남긴다.
> ERP의 U+ 앱/망 수신은 실시간 `connected`를 관측할 수 없으므로 처음부터 `호출 중`으로
> 단정하지 않고 `수신 상태 확인 중`으로 표시한다. 종료 이력이 2분 넘게 없으면
> `이력 반영 지연`과 자동 재확인 상태로 바꿔 장시간 실제 벨이 울리는 것처럼 보이지 않게
> 한다. 첫 SSR은 현재 시각을 쓰지 않고 mount 뒤 시계로만 지연 표시를 판정한다.
> 2026-08-07 사용자 결정으로 비즈콜은 앱의 벨 시점 실시간 표시·ERP 받기·ERP 원격 발신을
> 현재 범위에서 제외한다. 비즈콜 앱에서 직접 한 발신과 앱으로 받은 수신이 종료 뒤
> 전화데스크에 빠짐없이 합쳐지는 것을 완료 기준으로 삼고, 모바일 브리지·기업 webhook·
> 동시착신은 향후 실제 업무 필요가 생길 때만 다시 검토한다.
> 같은 날 전화데스크 후처리와 재통화 업무 수직 흐름을 운영 배포했다. 통화 사실 원장과
> 사람이 확정한 결과를 분리해 `telephony_call_aftercare`에 상담완료·재상담필요·부재 및
> 무응답·통화중·담당자 연결 요청·거절·법원 등 관공서·채권자 등·잘못 걸린 전화·기타의
> 열 가지 결과, 암호화 메모·기타 설명, 확정 직원·시각과 연결 상담을 저장한다.
> `telephony_follow_up_tasks`는 미완료 업무를 한 후처리당 하나만 허용하고 담당자·30분 단위
> 미래 시각·완료/취소 원장을 보존한다. 재상담필요·부재 및 무응답·통화중·담당자 연결 요청·
> 거절은 ERP에서 재통화 체크가 기본값이며 해제할 수 있다. 고객 담당자가 있으면 기본값으로
> 제시하고 복수 담당자는 선택하며, 담당자가 없으면 활성 직원 중 반드시 선택한다.
> 전화데스크 재통화 업무 큐는 로그인 직원을 기본 담당자 필터로 사용하고, 미완료 업무가
> 있는 다른 담당자 또는 전체 담당자로 전환해 업무 분배 상태를 함께 확인한다.
> 2026-08-11에는 상담데스크의 최신 50건·전화데스크의 최신 100건 고정 조회를 서버
> 페이지네이션으로 교체했다. 두 목록은 한 페이지 20건을 기본으로 20·50·100건을 선택하고,
> 전체 기간 또는 한국시간 오늘부터 7일간의 개별 날짜, 과거 특정일·시작일~종료일을
> `[시작, 다음 날 00:00)` 범위로 조회한다. 범위를 벗어난 페이지는 마지막 유효 페이지로
> 보정하며 SSE 재동기화도 현재 페이지·크기·필터·기간을 유지한다. 상담 현황은
> 전체·배정 대기·내 담당·확인 필요·오늘 접수, 전화 현황은 전체·수신·ERP 발신·직접 발신·
> 진행 중을 선택 기간 전체의 서버 집계로 표시하고 각 카드를 실제 목록 필터로 사용한다.
> 2026-08-12에는 전화데스크 출처 필터를 서버 조회 뒤 응답 분류와 브라우저 현재 화면에서
> 각각 다시 검증하도록 보강했다. `ERP 발신`과 `직접 발신`에는 정확히 같은 출처만 남고,
> `진행 중`은 대기·호출·통화 중인 원장만 표시한다. root가 연결된 관측 통화의 진행 상태는
> 과거 관측 행이 아니라 화면에 표시하는 root의 상태를 우선해 집계한다. 현재 페이지 검색은
> 전화번호·고객명 전용으로 좁히고, 통화 담당자는 재통화 업무와 같은 별도 드롭다운에서
> 선택한다. 2026-08-13에는 통화 담당자 후보를 현재 페이지가 아니라 선택 기간·출처의 전체
> 결과에서 만들고, 담당자 조건도 전체 결과에 먼저 적용한 뒤 총 건수·마지막 유효 페이지와
> 현재 페이지를 계산하도록 교정했다. ERP 발신은 실제 발신 직원, 수신·직접 발신은 회선
> 담당자, 내선은 참여 직원을 기준으로 한다. 다크 테마의 네이티브 담당자 선택 목록도 배경과
> 글자색을 함께 지정해 운영체제 기본 흰 배경에 밝은 글자가 겹치지 않게 한다.
> 2026-08-14에는 대표번호로 들어온 뒤 직원 내선으로 착신·당겨받기 된 실제 통화가 전화
> 원장에서 빠진 사례를 조사했다. 대표 문자 수신함 7개는 모두 활성 endpoint와 검증된
> 인증정보를 갖고 있었지만 직원 binding이 0건이라, 직원 binding만 읽던 수신 관찰기가
> callback 등록과 `getinboundcall` 보정에서 전부 제외했다. 실제 누락 통화는 대표번호
> `02-555-7455`의 U+ 이력에 고객번호·시작·148초 연결·종료가 있었고, 당겨받은 직원 bridge의
> `CHANNEL_LIST/CHANNELOUT` 종료시각과 일치했다. 큐 적체나 제공자 이력 누락은 아니었다.
> 수신 관찰 대상은 이제 활성·인증된 대표 endpoint를 직원 binding 없이도 포함하되, 개인
> endpoint는 계속 활성 직원 binding을 요구한다. 따라서 대표번호 7개도 수신 callback과
> 24시간 이력 보정으로 고객번호가 있는 외부 root를 만들며 문자 동기화와 직원/Windows bridge
> 소유권은 그대로 분리한다. 당겨받기 최종 직원 자동 확정은 기존 exact provider 근거를
> 계속 요구하고, 근거가 없으면 대표 root를 임의 직원에게 귀속하지 않는다.
> 같은 날 후속 운영 읽기 점검에서 내선 7455·1919는 직원 개인 회선이 아니라 직원 binding이
> 없는 대표 endpoint임을 다시 확인했다. 따라서 owner가 없는 대표 endpoint는 ERP에서
> `대표번호 공용 회선`으로 표시하고, 개인 endpoint에만 `회선 담당 미지정`을 사용한다. 16:09
> 전후 대표 수신은 U+ root·개인 callback root·직원 bridge root가 exact 회선 증거 부족으로
> 분리된 사례였으며 bridge→gateway 유입은 수십 ms, queue/dead-letter는 0이었다. 1분 E2E
> summary도 정상 수집됐고, 한 브라우저 Notification 처리 표본만 10.48초 지연됐을 뿐 지속
> 적체나 이력 수집 장애는 아니었다.
> `***9684` 통화는 고객 leg가 실제 종료된 뒤 등록되지 않은 대상 내선 9971의 consultation
> leg만 `connected`로 남아 root가 `needs_confirmation`인 사례였다. 기존 상세·확정 API가
> root 자체가 `ended`이고 종료 participant leg가 있을 때만 담당자 선택을 열어 이 통화는
> 사람이 바로잡을 수 없었다. 앞으로 외부 root에서 고객 leg 종료 근거가 있고 활성 고객 leg가
> 없으며 마지막 신호 뒤 2분이 지나면, 인증 직원이 전화데스크 상세에서 활성 직원 전체 중 실제
> 최종 통화자를 선택할 수 있다. 확정 시 남은 비고객 활성 leg를 종료 근거 시각으로 닫고 root를
> `ended/confirmed`로 전환하되 `MANUAL_STAFF_CONFIRMATION` cause, 선택 직원·endpoint·실행자와
> 기존 상태를 `staff_resolved` 감사 관계에 남긴다. 이는 자동 추정이 아니며 실제 통화자를 아는
> 직원의 명시적 확정만 허용한다. legacy 수신 원장이 없는 외부 root도 ID로 상세를 열 수 있고,
> 상태바는 시작·마지막 신호 시각과 `담당자 확인` 동선을 표시한다. 실제 transfer 관계가 있을
> 때만 `호전환 확인 필요`, 그 외에는 방향별 `상태 확인 필요`로 구분한다. DB migration은 없다.
> migration `0046_small_cargill.sql`은 이 조회의 최신순·날짜 범위를 위해 상담 요청시각,
> 클릭투콜 요청시각, 관측 통화 호출시각 단독 btree 인덱스를 추가한다.
> 재통화 업무 큐는 통화 이력의 페이지·날짜와 별도인 전체 열린 업무 원장으로 유지한다.
> 전화데스크 상세는 기존 같은 전화번호 상담 연결, 리걸프렌즈 고객명·담당자를 이용한
> 신건상담 생성, 전화데스크 단독 저장을 제공한다. 신건은 통화에서 직원이 기록했다는
> 개인정보 처리 근거를 별도로 남기며 고객 동의 시각을 만들지 않고, 기존 상담은 전화 HMAC이
> 같은 경우만 연결한다. 수신 바와 클릭투콜은 통화 종료 시 같은 공용 후처리 화면을 열고,
> 상담목록도 최신 후처리 결과를 같은 상태 배지로 조회한다. 메모·기타 원문과 전화번호는
> PostgreSQL `NOTIFY`·SSE에 넣지 않는다.
> 암호화 스냅샷 `lawand-prod-pre-phone-aftercare-20260807` 뒤 migration
> `0036_phone_desk_aftercare.sql`과 gateway·ERP 릴리스
> `20260807T055854Z-phone-aftercare`를 배포했다. 운영 합성 직접발신은 후처리 저장 200,
> 상세 200, 재통화 완료 200을 반환했고 통화·후처리·업무·감사·세션을 모두 정리했다.
> ERP same-origin 페이지·목록·상세·SSE도 모두 200과 `telephony.desk.sync`를 확인했고 임시
> 세션 잔존은 0건이다. 두 앱·Caddy는 active, 컨테이너 재시작·배포 뒤 error journal·
> CloudWatch ALARM은 0이며 Windows bridge v0.5.1.0도 작업 Running·프로세스 1개·큐 0이다.
> 최종 범위 대조에서 실물 전화기·비즈콜 앱의 미연결 직접 발신은 전화데스크 상세에서만
> 후처리할 수 있고 자동 창은 없음을 확인해 ERP를 한 번 더 보강했다. 전역 직원 바가
> `telephony.desk.changed`를 구독하되 페이지 진입 전 과거 종료 통화는 기준선으로만 기억하고,
> 로그인 직원 소유 회선의 새 `centrex_direct` 종료·미처리 통화만 공용 후처리 큐에 넣는다.
> 여러 통화가 동시에 끝나면 순서대로 열며 수신·클릭투콜과 같은 session key로 중복 창을
> 막는다. 최종 ERP 릴리스 `20260807T063043Z-phone-aftercare-direct`의 private S3 AES256
> 아티팩트 SHA-256은
> `92576babd191066cb5b15692e6d2551ef9c5888797433800660647bdc80e6f03`이다. 배포 bundle에
> 직접발신 observer가 포함됐고 ERP 페이지·목록·SSE 200과 sync, 임시 세션 0건을 확인했다.
> 같은 날 후처리 사용성도 운영 보강했다. 후처리 모달은 sticky 헤더의
> `backdrop-filter` containing block 밖인 `document.body` portal에서 열고 내부 스크롤을
> 항상 맨 위로 초기화해 첫 항목부터 보이게 한다. 기존 상담 일치 건은 등록일·최근 요청일·
> 현재 담당자를 표시하고, 상담데스크 우선 일치 여부와 무관하게 같은 번호의 비공개
> 리걸프렌즈 디렉터리를 한 번 더 조회해 최근 사건의 유형·상태·담당·법원·등록일·갱신일을
> 함께 보여준다. `lawand_app`은 확장된 security-definer 함수 실행만 가능하며 `CB` 직접
> 권한은 계속 없다. 재통화 일정은 홈페이지 상담 요청과 같은 평일 날짜 카드·다른 날짜·
> 08:00~19:00의 30분 구간 카드로 펼쳐지고, 상세에서 저장하면 전화데스크 목록으로 이동한다.
> 상세 상단 목록 링크의 공용 버튼 CSS도 링크 요소에 맞게 복구했다. 암호화 스냅샷
> `lawand-prod-pre-phone-aftercare-ux-20260807` 뒤 migration
> `0037_phone_desk_directory_context.sql`과 gateway·ERP 릴리스
> `20260807T072916Z-phone-aftercare-ux`를 배포했다. 인증 운영 canary는 목록 28건 중
> 상담·리걸프렌즈 양쪽 일치 통화의 상세·페이지를 200으로 읽고 사건 8건의 확장 필드를
> 확인했으며 임시 세션은 0건으로 정리했다.
> 2026-08-12 후처리를 새로 작성할 때 통화 결과는 가장 빈번한 `상담완료`를 기본 선택하되,
> 이미 저장된 결과를 수정할 때는 기존 값을 그대로 표시한다. 결과 종류와 무관하게 후처리
> 안에서 기존 문자 작성기를 열 수 있고, 재통화 업무 큐에는 고객명·전화번호와 `문자보내기`·
> `센트릭스 전화하기`를 함께 표시한다. 두 액션은 전화번호 원문을 브라우저 요청으로 다시
> 보내지 않고, 연결 상담 또는 정확히 일치한 리걸프렌즈 고객·사건 식별자만 전달해 gateway가
> 삭제 사건·현재 전화번호·직원 권한을 기존 경계에서 재검증한다. 원시 번호만 있는 통화는
> 고객명·번호를 업무 식별용으로 보여주되 두 액션을 비활성화한다. migration
> 통합 과정에서 `0053_phone_follow_up_actions.sql`로 재번호화한 migration은 정확 일치 resolver에 `client_idx`를 추가해 재통화
> 업무에서도 고객 찾기와 같은 안전한 문자·발신 대상을 만들며, 운영 반영 전에는 `0051`과
> gateway·ERP를 같은 릴리스로 배포해야 한다.
> 이후 회선 소유자로 로그인한 사용자가 실제 수신의 ERP `전화 받기`를 눌렀고, 물리
> 전화기 스피커폰과 양방향 통화를 확인했다. `ringing → connected`는 정상 기록됐지만
> 통화 종료 뒤 ERP가 `통화 중`을 유지했다. Windows 로그에는 `CHANNELOUT(HCAUSE=16)`이
> 도착했으나 최초 수신 ID와 전화기 쪽 연결 channel ID의 prefix·sequence가 모두 달라
> bridge v0.3.0의 최초 ID·인접 leg 비교에서 버려진 것이 원인이었다. bridge v0.3.1은
> `CHANNELLIST`의 양쪽 ID를 활성 통화에 보존하고 어느 쪽 종료도 최초 수신 통화의
> `inbound.ended`로 전달한다. 연결된 통화에는 무응답용 3분 제한을 적용하지 않아 장시간
> 통화도 종료된다. Windows x86 빌드·8개 self-test 뒤 v0.3.1.0을 배포했고 프로세스 1개,
> 로그인 성공, DPAPI 큐 0건을 확인했다. 누락된 실제 종료는 원본 provider 시각·원인으로
> 같은 HMAC gateway 경로에서 복구해 운영 원장을 `ringing → connected → ended`로
> 완결했으며 DB를 직접 수정하지 않았다.
> 이어 실물 센트릭스 전화기 직접 발신 canary에서 `RINGEVENT(ISDIAL=1)`의 `CALLERID`가
> 상대 번호이고 `AGENT`가 내선이며 `INEXTEN`은 비어 있음을 확인했다. 약 8.6초 호출 뒤
> 연결되고 약 16.4초 통화 후 `CHANNELOUT(HCAUSE=16)`으로 끝났다. bridge v0.4.0은 이를
> `outbound.ringing → outbound.connected → outbound.ended`로 HMAC gateway에 전달한다.
> 호환성을 위해 기존 `telephony_inbound_calls/events` 테이블 이름은 유지하되 migration
> `0031_groovy_stellaris.sql`로 `direction`을 추가해 센트릭스 관측 통화 원장으로 확장했다.
> 기존 행은 모두 `inbound`이며 발신 번호도 같은 AES-GCM 암호화·HMAC 검색 지문 경계를
> 사용한다. 발신 이벤트는 수신 전용 PostgreSQL 알림과 ERP 상단 수신 바에서 제외한다.
> ERP 클릭투콜도 같은 OpenAPI 발신으로 관측될 수 있으므로 이후 전화데스크는 회선·번호
> 지문·시각으로 `telephony_calls` 명령 원장에 연결된 관측 발신을 한 건으로 표시하고,
> 연결되지 않은 관측 발신을 `센트릭스 직접 발신`으로 관리한다. OpenAPI에는 실물 전화기와
> U+ 비즈콜 앱을 구분하는 문서상 필드가 없으므로 비즈콜 실제 canary 전에는 단말 종류를
> 추정하지 않는다. 운영 암호화 스냅샷 `lawand-prod-pre-centrex-outbound-20260806` 뒤
> gateway 릴리스 `20260806T054920Z-centrex-observed-outbound`와 Windows bridge v0.4.0.0을
> 배포했다. 두 런타임은 정상이고 post-deploy 실물 발신·비즈콜 canary가 남아 있다.
> 같은 날 통합 전화데스크를 구현했다. 센트릭스 관측 수신, ERP
> 클릭투콜, 실물 전화기·U+ 비즈콜 앱을 포함하는 미연결 센트릭스 직접 발신을 하나의
> 시간순 목록에서 조회하되 원본 원장은 합치지 않는다. migration
> `0032_brown_ronan.sql`의 `telephony_call_observation_links`가 같은 회선·상대번호 HMAC
> 지문과 요청 대비 -5초~+120초 시각창에서 시각 차가 가장 작은 미연결 클릭투콜 명령과
> 관측 발신만 1:1로 연결한다. migration 시점의 기존 원장 backfill은 양쪽 상호 최근접
> 조건까지 적용한다. 연결된 두 원장은 읽기 모델에서 한 행으로 접고, 연결되지 않은 관측
> 발신은 단말 종류를 추정하지 않고 `센트릭스 직접 발신`으로 표시한다. 전화데스크는
> 전체 전화번호, 고객·사건·담당자 해석, 회선·내선, 상태·호출/통화 시간, 상담 상세
> 이동을 제공하며 수신·ERP 발신·직접 발신 필터와 검색을 지원한다. DB commit 뒤 별도
> 개인정보 없는 `lawand_telephony_desk_events` 알림과 인증 SSE로만 갱신하고 전화번호는
> 권한 있는 snapshot 응답에서만 복호화한다. 로컬 실제 DB 통합 검증에서 수신 2건과
> 직접 발신 1건, 기존 클릭투콜 명령에 연결된 관측 발신의 단일 행 표시를 확인한 뒤
> 임시 원장과 연결을 모두 정리했다. 이후 운영 암호화 스냅샷
> `lawand-prod-pre-phone-desk-20260806`을 available까지 확인하고 migration `0032`를
> 적용했다. 기존 운영 관측 발신과 클릭투콜 명령은 모두 0건이라 backfill 변경은 없었고,
> 연결 테이블·제약 5개·알림 trigger 3개·migration hash와 `lawand_app`의 SELECT/INSERT
> 최소권한을 확인했다. private S3 AES256 아티팩트 SHA-256
> `02a0d8f40fd50c5b7531d7bc53f57d98051959b1ebb19764ea409504f01018c5`의 gateway·ERP
> 릴리스 `20260806T072225Z-phone-desk`를 배포했다. 인증 canary에서 운영 `/phone-desk`와
> same-origin 목록 API가 200, SSE `telephony.desk.sync`가 수신됐고 임시 세션은 0건으로
> 정리됐다. 기존 수신 6건이 통합 목록에 보이며 연결·클릭투콜은 0건이다. 673px 운영
> Chrome에서 전체 번호·고객·담당자·통화시간, 실시간 연결, 필터 빈 상태를 확인했다.
> console 403 세 건은 모두 설치된 Monica 확장 프로그램의 `background.js` 요청이며 ERP·
> gateway·hydration 오류는 없다. 두 앱·Caddy는 재시작 0회, error journal과 CloudWatch
> ALARM은 0건이고 Windows bridge v0.4.0.0도 프로세스 1개·DPAPI 큐 0건이다. 이어 사용자의
> 명시적 승인으로 현재 자격증명을 다시 `userinfo` 검증해 실제 070 회선과 내선 4591이
> 일치함을 확인하고, 비밀번호 원문은 저장하지 않은 채 SHA-512 값만 운영 gateway
> Secrets Manager의 `office-main-4591` 키에 넣어 클릭투콜 워커를 활성화했다. 같은 릴리스
> gateway를 재기동한 뒤 워커 시작 로그, 운영 컨테이너의 `userinfo` HTTP 200, 내부·외부
> health, gateway·Caddy active와 재시작 0회, error journal·CloudWatch ALARM 0건을
> 확인했다. 활성화 전후 클릭투콜 명령·대기 이벤트·전송 시도는 모두 0건이라 실제 발신은
> 없었다. 운영 ERP 클릭투콜 canary는 사용자가 사무실에 복귀한 뒤 수행한다. 사용자가
> 센트릭스 비밀번호를 변경하면 다음 발신 전에 운영 secret의 SHA-512도 반드시 함께
> 갱신해야 하며, 비즈콜은 직원 회선 등록 뒤 별도로 확인한다.
> 2026-08-11 통합 통화 활동 v2 제품 정책을 확정했다. 외부 수신·센트릭스 직접 발신·ERP
> 클릭투콜은 모두 로그인 직원 전체의 상단 카드에 동일하게 표시한다. 일반 내선은 참여자만
> 보되 수신자에게 토스트와 브라우저 알림을 보내고, 외부 고객 호전환은 하나의 고객 통화
> root 아래 A/고객, A/B 상담, B/고객 leg를 연결한다. 중간 A leg 종료는 전체 종료로 보지
> 않고 마지막 고객 leg가 끝날 때만 종료하며 후처리는 최종 고객 통화자에게 한 번만 연다.
> 외부 수신 알림은 고객·회선 담당 여부와 무관하게 활성 직원 전체를 대상으로 하므로, ERP에
> 로그인하고 알림 권한을 허용한 직원은 모두 수신 즉시 네이티브 알림을 받는다. 한 브라우저의
> 여러 ERP 탭은 기존 leader lease로 알림 한 건만 표시한다. 리걸프렌즈 담당자는 이름이 아니라
> `Member_idx`·`sub_member_idx` → `staff_external_accounts.external_member_idx` → ERP 직원 ID로
> 해석한다. 인증 same-origin snapshot은 전화데스크의 전체 번호·사건 상세를 유지하되 전역
> 알림 카드와 Notification은 고객명·고객 담당자·상담/사건 진행상태만 제공한다. 원문은
> 개인정보 없는 변경 신호 뒤 인증된 snapshot에서만 조회한다. 상세 계약과 내선·
> 무조건/통화 후 호전환·실패 복귀 canary 순서는 `docs/CENTREX_CALL_ACTIVITY_V2.md`가
> 기준이다. 실제 식별자 관계 확인 전에는 제공자 채널명만으로 보류·전환 완료를 추정하지
> 않는다. bridge v0.7.2 후보는 gateway 전송과 기존 내부 leg 거부를 유지한 채
> `internal/external` 상대 종류와 `sip/pjsip/local/local_xfer` 채널 종류만 비식별 로그에
> 추가한다. 2026-08-11 운영 v0.7.2 통제 canary에서 4591→1208 일반 내선은 양쪽
> `RING_EVENT`가 같은 root ID를, `CHANNEL_LIST`가 같은 adjacent channel ID 쌍을 보냈고
> 상대 suffix도 서로의 내선과 일치했다. 내부 4자리는 기존처럼 gateway 전송 전 거부되어
> 운영 원장을 만들지 않았다. 통화 후 호전환에서도 A/B 상담 leg는 양쪽에서 같은 별도 ID
> 쌍과 `internal/sip`으로 연결됐지만 외부 고객 root/channel ID와는 provider group이
> 달랐고 `local_xfer` 채널은 관측되지 않았다. A의 외부 connected channel 종료가 현재
> bridge에서 `inbound.ended`를 발생시켜 운영 원장을 16:53:34 KST에 닫았으나, B의 마지막
> `CHANNEL_OUT`은 19.13초 뒤였다. B 로그에는 외부 고객 root·masked suffix가 없고 A/B
> 상담 channel ID와 sentinel source만 있어 현재 정보만으로 B/고객 최종 leg를 외부 root에
> 결정적으로 연결할 수 없다. 따라서 A leg 종료와 고객 root 최종 종료를 아직 구분할 수
> 없으며, 이 통화 후 호전환 fixture는 명시적 transfer correlation 없이는 기존 원장을 통합
> 통화 활동 상태머신의 근거로 사용할 수 없는 미해결 사례로 유지한다.
>
> 같은 날 무조건 호전환 성공 fixture에서는 최초 외부 root가 B에도 다시 나타났고,
> `line=4591`·`agent=1208`인 B `RING_EVENT`의 고객 지문이 A와 일치했다. 이어 B
> `CHANNEL_LIST`가 그 외부 root와 B/고객 최종 leg를 직접 연결해 이 시나리오는 결정적으로
> 상관할 수 있었다. 그러나 현재 gateway는 B endpoint와 원수신 회선이 다른 이벤트를
> `incoming_line_mismatch` 409로 거절하고, 뒤이은 connected/ended도 orphan 409로 거절해
> 정확히 3건을 1208 dead-letter로 보냈다. 그 결과 A 원장은 실제 B 최종 종료보다 33.259초
> 먼저 닫혔다. 암호화 원본 3건은 timestamp archive에 hash 검증 후 보존하고 active DLQ에서만
> 격리해 target queue/dead-letter를 0으로 복구했으며 관련 경보도 OK를 확인했다. 이 보호를
> 전체 완화하지 않는다. 호전환 전용 수용 경계는 동일 외부 root·고객 지문·원수신 회선·
> B agent·외부 root와 B final leg를 잇는 `CHANNEL_LIST` 증거를 모두 만족해야 한다.
>
> 실패·복귀 fixture에서는 외부 root와 connected channel이 유지된 상태에서 양쪽에 같은
> internal consultation root의 A→B `RING_EVENT`가 왔고 B `CHANNEL_LIST`는 없었다. B
> 미응답 뒤 상담 leg 종료 cause 207/16이 관측됐지만 이는 fixture 값일 뿐 의미를 일반화하지
> 않는다. 취소 과정에서 외부 channel의 중간 종료는 없었고, 12.10초 뒤 같은 외부 channel의
> 최종 종료만 gateway가 수용했다. 오류와 DLQ는 0이었다. 제공자의 명시적 return 이벤트가
> 없으므로 bridge가 활성 외부 root 문맥에서 consultation attempt/returned correlation
> 이벤트를 생성해야 제품이 `호전환 시도 중`·`복귀`를 구분할 수 있다.
>
> 구현 계약은 고객 통화 root와 개별 call leg를 분리하고 transfer correlation evidence를
> 보존하는 것이다. root는 마지막 customer leg가 끝날 때만 종료하며 A leg 종료로 닫지 않는다.
> 후처리는 최종 고객 통화자에게만 한 번 열고, 일반 내선은 참여자에게만 알리되 호전환 대상은
> `전달된 고객 전화` 정책을 적용한다. 일반 내선은 참여자 연결이 결정적이고 무조건 호전환은
> 외부 root와 B final leg 연결이 결정적이며 실패·복귀는 bridge-local 문맥으로 판별 가능하다.
> 통화 후 호전환 성공의 B/customer final leg는 passive event만으로 외부 root와 결정적으로
> 연결할 수 없어 여전히 미해결이다. cause 또는 시간 근접만으로 상태·상관관계를 추정하지 않는다.
> 2026-08-11에는 이 계약의 출시 후보 구현을 완료했다. bridge v0.8.0 후보가 기존 외부
> 수·발신 v1과 함께 내선·호전환 `RINGEVENT`·`CHANNELLIST`·`CHANNELOUT`을 v2 관측으로
> 전달하고, migration `0045_safe_zarek.sql`이 고객 call root·개별 leg·provider
> root/channel/source 식별자·transfer relation·원본 관측을 보존한다. gateway는 동일 외부
> root·고객 지문·원수신 회선·대상 agent·provider root→adjacent 관계가 모두 맞는
> 무조건 호전환만 확정하고, U+ callback/history가 중간 A leg를 종료하더라도 활성 B/customer
> leg가 있으면 root를 닫지 않는다. 통화 후 호전환 final leg 근거가 없으면
> `호전환 확인 필요`로 남기고 최종 통화자와 후처리를 추측하지 않는다. 공유 회선도 provider
> 근거 없이 임의의 한 직원을 최종 통화자로 지정하지 않는다. 외부 수·발신 공용 카드와
> 참여자 전용 내선 카드, 전달·복귀 상태, 정확한 `Member_idx` 담당자 알림, 토스트·브라우저
> Notification과 탭 간 중복 방지를 ERP에 연결했다. 개인정보 없는 NOTIFY/SSE는 통화 ID만
> 전달하고 전체 번호·고객·사건·담당자·회선은 인증 snapshot에서만 복호화한다. 임시 로컬
> DB 복제본 수직 검증과 전체 테스트·lint·typecheck·production build, Windows x86 compile·
> self-test를 통과했다. 2026-08-11 통합 릴리스
> `20260811T104143Z-integrated-call-messaging-v2`로 migration `0045_safe_zarek.sql`,
> `0046_small_cargill.sql`, `0047_wandering_maximus.sql`과 gateway·ERP·Windows bridge
> v0.8.0을 운영에 함께 반영했다. 기존 후처리 35건은 동일 UUID의 새 call root로 유실 없이
> 승격했고 재통화 10건을 보존했다. 운영 원장은 root/leg 295쌍·연결 누락 0, 후처리 source
> 위반 0이며 대표 문자 mailbox 7개도 오류 없이 동기화된다. 활성 통화·실행 명령·회선 중복·
> DPAPI queue/dead-letter 0에서 전환했고 gateway·ERP·Caddy 재시작 0, 센트릭스 5종을 포함한
> CloudWatch ALARM 0을 확인했다. 남은 acceptance는 실제 전화를 새로 만들지 않은 이번 배포와
> 분리해 일반 내선·무조건/통화 후 호전환·실패 복귀 네 시나리오와 통제 문자 회신을 수행하는
> 것이다.
> 2026-08-12에는 Windows bridge v0.8.0의 v2 `call.ringing` 직렬화 결함을 확인했다.
> `incomingLineNumber`가 `callerNumber`와 같은 조건문 안에 있어, caller 원문을 싣지 않는
> v2 수신·내선 관측에서 필수 수신 회선만 JSON에서 빠졌고 gateway가 HTTP 400으로 거부했다.
> v1 병행 원장은 정상이라 고객 통화 자체와 종료 상태는 보존됐지만, 해당 구간의 일부 외부
> 통화는 새 root/leg 연결이 없었다. 같은 날 4591 통제 수신은 Windows가 실제 ring을
> 즉시 관측했지만, 이 400 파일이 FIFO 선두에서 1분간 재시도되면서 정상 v1 ring과 뒤이은
> 종료 이벤트까지 막았다. U+ 종료 이력은 끊은 뒤 약 16초에 먼저 종료 원장을 만들었고,
> bridge ring은 약 68초 뒤 도착해 기존 구현이 이미 종료된 U+ 이력을 찾지 못한 채 별도
> 13초 원장과 후처리를 만들었다. 즉 브라우저 렌더 지연이 아니라 이벤트 단위 영구 오류의
> head-of-line blocking과 지연 ring 상관 범위 부족이 결합한 장애였다.
> v0.8.2는 수신 회선을 독립 직렬화하고 C#·core 양쪽에 inbound v2 계약 회귀
> 테스트를 둔다. 이벤트 단위 400/404/409/422는 재시도 중에도 후속 큐 파일을 계속 보내고,
> 인증·rate limit·gateway 장애인 401/429/5xx와 네트워크 실패만 현재 배치를 중단한다.
> gateway는 동일 endpoint·고객 지문·시작시각 5초 안의 유일한 U+ callback/history 원장을
> 종료 상태여도 재사용하며 실제 provider 식별자를 root에 보존한다. 15초 넘게 늦거나 ring
> 근거가 없는 수신은 카드에 지연 상태를 표시하고 이미 무효일 수 있는 `전화 받기` 버튼을
> 숨긴다. health monitor는 대기 중 재시도 큐
> `QueueDepth`와 영구 거부 `DeadLetterDepth`를 별도 metric으로 발행하되 기존
> `DpapiQueueDepth` 합계도 호환용으로 유지한다. migration
> `0048_centrex_v2_ringing_recovery.sql`은 먼저 동일 회선·고객 지문·시작 2초·종료 3초 안의
> 유일한 종료 이력/bridge 쌍만 합치고, 동일 분류·상담·확정자이며 메모·재통화가 없는 중복
> 후처리만 하나로 정리한다. 이어 병행 v1 원장의 암호문·AAD와 UUID를 그대로 사용해 빠진
> external root/customer leg/provider 식별자를 멱등 복구하고, 기존 v2 leg가 있으면 중복
> 생성하지 않고 연결하며 후처리 원천도 root로 원자적으로 승격한다. 필수 필드가 이미 빠진
> 과거 암호화 dead-letter는 재처리로 복구하지 않고 migration 대조 뒤 hash를 보존한 archive로
> 이동한다. 로컬 운영형 fixture에서 중복 통화·이벤트 4건·동일 후처리·두 provider 식별자의
> 보존과 migration 2회 멱등성을 검증했다. 2026-08-12 통합 복구 릴리스
> `20260812T015203Z-centrex-ringing-recovery-v1`로 migration `0048`, gateway·ERP와
> Windows bridge v0.8.2.0을 함께 운영 반영했다. migration은 미연결 v1 17건을 root/leg에
> 연결하고 엄격히 일치한 중복 통화 1쌍과 중복 후처리 1건만 정리했으며 source 제약 위반과
> 미연결 원장은 0이다. v0.8.0이 남긴 암호화 dead-letter 32건은 복호화·재처리하지 않고
> 15개 bridge별 SHA-256 manifest와 함께 archive로 이동했다. 새 `QueueDepth`·
> `DeadLetterDepth` 경보와 기존 합계 경보는 모두 `OK`이고, pool은 설치 51·배정 19·실행 24·
> warm 5, 오프라인·로그인 실패·활성 queue/dead-letter 0이다. gateway·ERP 인증 smoke와
> 전화데스크 첫 페이지 계약을 통과했고 임시 세션은 0건으로 정리했다. 별도 과제로 남은
> 08:19 KST의 내부 통화 root 1건은 실제 활성 관측이 없는 기존 orphan이라 이번 migration이나
> 배포에서 추정 종료하지 않았다.
> 같은 날 상단 바에 실제 2건보다 `발신통화중` 2건·`발신중`·`수신중` 4건이 보이는 후속
> 장애를 운영 원장과 비식별 Windows 이벤트로 다시 대조했다. U+ `CHANNELOUT`은 실제 종료
> ID를 보내면서 `SRCUNIQUEID=NONE`도 함께 보냈고, 기존 bridge/gateway가 `NONE`을 정상
> provider 식별자로 저장해 여러 endpoint의 종료 이벤트가 이전 통화에 잘못 붙었다. 또한
> 병행 v1 원장은 정상 종료돼도 이미 연결된 v2 root/leg에 terminal state를 전파하지 않았고,
> v2 내부 통화는 bridge 재접속 전 메모리 보정과 3분 미연결 만료의 대상이 아니었다. snapshot도
> 모든 비종료 root를 12시간 노출해 세 결함이 상단 ghost 카드로 합쳐졌다. 08:19 내부 root에는
> root `.3081116`→channel `.3081117`→실제 종료 `.3081118`의 동일 prefix·인접 sequence
> 증거가 남아 있어 시간이나 HCAUSE 추정 없이 정확히 종결할 수 있음을 확인했다.
> 운영 v0.8.3은 `0/NIL/NONE/NULL/UNKNOWN`을 provider sentinel로 폐기하고, 외부 v1뿐
> 아니라 모든 v2 ring/channel도 메모리에서 추적해 미연결 3분 timeout·재접속·명시적 해제·
> 프로세스 종료 시 정확한 v2 `call.ended` 보정 이벤트를 내구 큐에 남긴다. gateway는 exact
> 식별자를 활성 leg 우선으로 찾고, 동일 endpoint·같은 prefix·인접 sequence가 유일한 활성
> leg에만 sibling 종료를 연결한다. 병행 v1의 connected/ended는 동일 root/leg에 즉시
> 동기화하되 다른 활성 customer leg가 있는 호전환 root는 닫지 않는다. 인증 snapshot은
> `ringing`을 3분까지만 반환하는 마지막 UI 안전망도 둔다. migration
> `0049_centrex_terminal_state_recovery.sql`은 sentinel 관측 64건·식별자 7건을 정규화하고,
> 잘못 연결된 종료 11건을 분리한 뒤 유일한 provider 종료 증거, 동일 root/leg의 v1 terminal
> state, 관계 없는 3분 초과 ringing만 순서대로 복구한다. 운영 읽기 dry-run 기준 현재 영향은
> 유일한 종료 증거 4건·v1 종료 3건·순수 ring timeout 1건이며 실제 활성/호전환과 모호한
> 후보는 제외된다. 로컬 운영형 fixture에서 08:19·잘못 연결된 외부 종료·v1 동기화·timeout·
> 호전환 보존·모호 후보 비병합을 검증했고 gateway 테스트, DB schema 검사, Windows x86
> self-test 20개를 통과했다. 2026-08-12 통합 릴리스
> `20260812T063358Z-integrated-worktrees-v1`에서 migration `0049`·gateway·bridge를 함께
> 운영 반영했다. 사용자의 명시적 승인으로 활성 통화를 배포 차단 조건으로 쓰지 않았고,
> migration은 provider 종료 증거가 있는 ghost만 종결했으며 남은 connected root는 추정
> 종료하지 않았다. 적용 뒤 sentinel 관측·식별자와 CloudWatch ALARM은 0이다.
> 같은 날 ERP 상단 통화 활동은 수신·발신·내선을 합친 전체 카드 중 `lastEventAt`이 가장
> 최근인 한 건만 기본 표시하고, 두 건 이상이면 총 건수와 `모두 펼치기/최근 1건만 보기`
> 제어로 상단 높이를 줄이거나 전체 활동을 확인한다. 상담 작업 큐의 거주 시·도는 전화번호·
> 상담 방식 뒤의 보조 문구가 아니라 고객명 옆 위치 아이콘의 고대비 배지로 승격하며,
> 누락도 별도 경고 배지로 드러낸다. 모든 활성 직원 역할은 기존 개인정보 없는
> `consultation.requested` SSE를 전역 직원 바에서 구독한다. 새 상담 ID만 받은 뒤 현재
> 로그인 세션이 same-origin 최소 알림 조회로 고객 입력 이름·거주지역·채널과 현재
> `상담하기` 가능 여부만 읽는다. 페이지가 보이는 탭에는 이름·지역을 강조하고 채널 배지를
> 둔 카드형 20초 토스트를, 권한과 ERP 설정을 모두 켠 브라우저에는 같은 핵심정보의
> Notification을 보내며 시스템 알림도 20초 뒤 닫는다. 접수번호와 전화번호는 알림 전용
> 응답·카드·Notification에서 제외한다. 카드 행동은 `상담하기` 하나이며 이미 배정된
> 재요청처럼 직접 배정할 수 없을 때만 같은 자리에 `상담 확인`을 표시한다. 시스템 알림은
> 브라우저 제약상 CSS를 적용하지 않고 OS 기본 UI를 사용하되, 고객명 제목과 광역지역·채널
> 본문, LAW& 아이콘·배지를 표시한다. 서비스 워커 persistent Notification을 지원하고 아직
> 배정 가능한 상담이면 단일 `상담하기` 액션이 인증된 same-origin 배정 API를 실행한 뒤
> 상세로 이동하며, 미지원 환경은 알림 본체 클릭으로 상세에 이동한다. 다른 직원이 먼저
> `consultation.assigned`를 만들면 열려 있는 ERP 카드와 persistent/page Notification을
> 즉시 닫는다. 상담 ID별 tag로 앞선
> 알림을 최신 요청으로 교체하고 event ID로 닫힘 타이머가 새 알림을 잘못 닫지 않게 한다.
> 이 서비스 워커는 표시·클릭 처리만 담당하며 Push 구독·서버 발송은 아직 없으므로 ERP 탭을
> 완전히 닫은 상태의 전달을 보장하지 않는다. 8초 다중 탭 leader lease와 event ID로 한
> 브라우저에서 중복을 막는다. 상단 알림 제어는 최초 `알림 켜기`에서 브라우저 권한을
> 요청하고, 허용 뒤에는 별도 ERP 설정으로 `알림 켜짐 ↔ 알림 꺼짐`을 실제 전환한다.
> DB·gateway 실시간 이벤트 payload에는 이름·전화·지역을 추가하지 않고, 최소 알림 조회도
> 유효한 직원 세션을 gateway에서 다시 확인한다.
> 실사용에서 목록 갱신용과 알림용 SSE가 별도 연결이라 목록만 갱신되고 알림 연결이
> 대기할 수 있는 결함을 확인했다. 두 소비자는 모듈 단일 `EventSource`를 공유하며,
> 보이는 탭은 leader 여부와 무관하게 토스트와 권한 허용 Notification을 모두 실행한다.
> `localStorage` 접근 실패도 Notification API 실행을 막지 않는다.
> 이 ERP 변경은 같은 날 긴급 단독 릴리스
> `20260812T061230Z-erp-consultation-alerts-v1`로 운영 반영했다. main에 함께 존재하던
> v0.8.3·migration `0049`·gateway·Windows bridge 후보는 이번 배포에서 제외해 운영 DB와
> 전화 런타임을 변경하지 않았다. 이후 같은 날 위 통합 릴리스에서 나머지 워크트리와 함께
> 운영 반영했다.
> 같은 날 실제 4591→1208 내선, 4591 수신 후 1208 통화 후 호전환, 4425 수신을 1208이
> 당겨받은 세 통화를 운영 원장과 비식별 bridge 관측으로 읽기 대조했다. 일반 내선은 이미
> v2 root/leg에 정확히 저장됐지만 전화데스크가 legacy 수·발신 원장만 읽어 목록·상세·후처리에서
> 누락됐다. 전화데스크를 root 기준 canonical read model로 바꿔 일반 내선도 한 원장으로
> 표시하고 `직접 발신`과 `진행 중` 사이에 `내선` 필터를 추가한다. 내선 후처리는 고객 상담과
> 연결하지 않고 `내선 통화 완료/내부 확인 필요/내선 미연결`과 메모·내부 확인 업무만 저장하며
> 인증 직원 누구나 입력할 수 있다.
> 4425/1208 건은 단순 중복이나 호전환이 아니라, 1208 `CHANNEL_LIST`가 4425의 활성 외부
> root와 exact 일치하고 adjacent 고객 channel의 종료도 1208에서 확인된 센트릭스 당겨받기다.
> 같은 root의 U+ 연결 이력과 bridge timeout 원장을 root 한 건으로 접고, 호전환 관계가 없는
> 다른 endpoint의 exact root→channel 증거일 때만 `call_picked_up` leg/relation으로 확정한다.
> 원수신 4425 leg는 `CALL_PICKED_UP`으로 끝내고 현재·최종 endpoint를 1208로 옮기며, 늦은
> 4425 timeout 종료가 이를 덮어쓰지 못하게 이미 종료된 leg의 terminal 이벤트를 멱등 처리한다.
> 통화 후 호전환은 A/B endpoint의 같은 consultation provider root/channel 중 한쪽 exact
> 종료를 반대쪽 mirrored leg에도 동기화한다. 모든 관측 leg가 끝났지만 B/customer passive
> 근거가 없는 경우 통화 상태는 `ended`, 상관 상태는 `needs_confirmation`으로 분리해 상단
> ghost 카드는 사라지게 한다. 종료된 통화는 전화데스크 상세에서 실제 최종 통화자를 직원이
> 선택해 `staff_resolved` 감사 관계와 final actor를 저장한 뒤 결과를 확정한다. 연결 중에는
> 최종 통화자 확정 전에도 인증 직원이 메모·다음 일정을 먼저 저장할 수 있다.
> enum migration `0051`은 Drizzle이 모든 pending migration을 한 트랜잭션에 적용하는 계약에
> 맞춰 기존 enum을 전체 값의 새 타입으로 원자 교체한다. 따라서 같은 migrate 실행의
> exact-evidence 복구 migration `0052`가 `call_picked_up`을 안전하게 사용할 수 있다.
> `0052`는 운영에서 exact provider 증거가 있는 당겨받기 관계 11건을 복구했고, 후속
> `0053`은 전화번호 해석 함수가 검증된 리걸프렌즈 `client_idx`를 함께 반환하게 해
> 전화데스크 후처리·재통화 큐의 문자와 발신 대상을 서버에서 다시 확인한다.
> 같은 통합 릴리스에는 상담 작업 큐의 직원 직접 신규·기존고객·소개건 등록, 신규 상담
> 실시간 카드와 즉시 `상담하기`, 전화데스크 서버 검색·출처/담당자/기간 필터와 내선 전용
> 후처리, 고객 문자·상담 연결, SOLAPI MMS 대표 발신번호, 홈페이지 자가진단 원·만원 확인을
> 함께 포함했다. `20260812T115219Z-integrated-all-worktrees-v2`로 홈페이지·ERP·gateway와
> migration `0051..0053`을 운영 반영했으며 Windows bridge는 코드 영향이 없어 v0.8.3을
> 유지한다.
> 2026-08-13에는 홈페이지에서 같은 고객이 상담·자가진단을 반복 제출하는 경우를 배정
> 전·후로 구분하는 상담 묶음 규칙을 구현했다. 최초 릴리스는 7일 안의 정규화 전화번호와
> 정규화 이름이 모두 같을 때만 묶었으나, 후속 v1.33에서 채널·입력 이름과 관계없이 같은
> 정규화 전화번호의 마지막 요청이 7일 이내이고 미종결이면 같은 상담 흐름으로 묶도록
> 기준을 바꿨다. 일반 상담·자가진단·직원 신규등록은 대표 `consultation`에 새
> `consultation_request`로 붙이고, 별도 원장으로 생성되는 선택 전화번호 카카오 접수는
> `consultation_groups`의 대표 상담 아래 연결한다. 미배정
> 건은 작업 큐에 `배정 전 재요청`, 배정 뒤 건은 `담당 상담 재요청`으로 표시하고 후자는
> 현재 담당자에게만 ERP 실시간·브라우저 알림을 보낸다. ERP 목록에는 대표 상담 한 건만,
> 상세에는 구성 상담·채널별 모든 요청과 접수번호를 표시하며 입력 이름이 다르면 가족·공용
> 번호 확인 경고를 보낸다. 직원은 접수번호로 전화 없는 플친 상담을 수동 연결하거나 잘못
> 묶인 구성원을 별도 상담으로 분리할 수 있고 모든 묶기·분리·대표 변경을 감사 원장에 남긴다.
> 서로 다른 전화번호·담당자·리걸프렌즈 사건은 수동 연결을 거부하고, 소프트삭제·미진입 무효
> 건은 자동 후보에서 제외하며 묶인 건은 먼저 분리해야 삭제·무효 처리할 수 있다. 담당 배정,
> 리걸프렌즈 신건, 최초 접수 알림톡은 묶음 전체에서 한 번만 실행한다. 요청별 암호화 원문과
> 광고 귀속은 합치지 않고 그대로 보존한다. migration `0057_consultation_groups.sql`은 활성
> 대표·구성원·append-only 변경 원장과 app/viewer 최소 권한을 추가한다.
> ERP에는 홈페이지 접수 연락처가 리걸프렌즈에만 존재하는 경우의 명시적 처리 게이트도
> 추가했다. 담당 배정 전에 `기존 사건 문의/기존 고객의 새 사건/연락처를 함께 쓰는 다른
> 사람` 중 하나를 선택한다. 기존 사건 문의는 검증된 `client_idx·case_idx` snapshot과 선택
> 감사를 저장하고 리걸프렌즈 신건 등록을 생략하며, 나머지 둘만 새 사건 등록을 계속한다.
> migration `0054`는 재요청 판정 enum, 처리 선택 원장과 개인정보 없는 재요청 알림 종류를
> 추가한다. 2026-08-13 릴리스 `20260813T002157Z-repeat-consultation-handling-v1`에서 암호화
> RDS 스냅샷을 먼저 확보하고 `0054`를 gateway보다 선행 적용한 뒤 gateway·ERP를 같은
> 소스로 운영 전환했다. 운영 migration 원장은 55개이며 `0054` 해시는 Git과 일치한다.
> 홈페이지·Windows bridge는 이번 코드 영향이 없어 직전 릴리스를 유지한다.
> 같은 날 간헐적인 ERP 5xx와 센트릭스 받기 지연을 조사해 CPU·메모리·디스크가 아니라
> gateway의 단일 PostgreSQL 연결 풀 대기를 공통 원인으로 확인했다. 요청 풀은 기본 20개,
> 세 `LISTEN/NOTIFY` 전용 풀은 기본 4개로 분리하고 풀별 사용·대기 최고값을 1초 간격으로
> 관측해 60초 CloudWatch 사용자 지표와 요청 풀 대기 경보로 보낸다. 빈 센트릭스 bridge의
> 받기 명령 DB 확인은 750ms마다 수행하지 않고 bridge별 10초 복구 점검으로 제한하되,
> ERP가 실제 받기 명령을 커밋하면 같은 프로세스의 힌트로 다음 bridge 요청이 즉시 DB를
> 확인한다. ERP 서버의 같은 세션·같은 목록/스냅샷 동시 요청은 진행 중인 한 요청만 공유하고
> 완료 결과를 캐시하지 않는다. 전화데스크와 수신 스냅샷의 상담·리걸프렌즈 고객 해석 및
> 최신 받기 명령은 행별 조회 대신 일괄 조회한다. 리걸프렌즈 전화번호 후보 SQL은 배열이
> PostgreSQL record로 컴파일되던 `unnest` 경로를 없애고 개별 파라미터 `VALUES`를 사용한다.
> 이 안정화 변경은 통합 릴리스
> `20260813T041933Z-integrated-consultation-kakao-v1`에서 gateway 운영 secret의 요청 풀 20개·
> LISTEN 풀 4개·CloudWatch 지표 활성값, DB 풀 대기 경보와 함께 운영 반영했다. 무관한 스택
> 변경에서도 최신 AMI SSM 별칭 재해석이 세 EC2 교체를 일으킬 수 있음을 변경 세트에서 발견해
> 첫 변경 세트를 실행하지 않고 폐기했으며, 현재 검증된 ARM64 AMI를 명시적으로 고정한 뒤 새
> 경보 하나만 추가하는 변경 세트를 실행했다. 운영 health는 요청 풀 waiting 0, LISTEN 연결
> 3개와 max 4를 반환하고 DB 풀 대기 metric 최대값과 전체 CloudWatch ALARM은 0이다.
> 같은 날 실제 동시 조회에서는 전화번호 디렉터리의 다건 `VALUES`가 각 번호마다
> `resolve_inbound_phone_directory`를 실행하며 RDS CPU를 반복적으로 99%까지 사용했고,
> gateway 요청 풀 대기가 최대 523개로 늘어 ERP server error가 발생했다. gateway·ERP
> 재기동만으로 부하가 재발해 사용자 승인 아래 운영 RDS를 2 vCPU `db.t4g.small`에서
> 4 vCPU `db.t4g.xlarge`로 즉시 상향했다. CloudFormation 변경 세트는 RDS 클래스 한 항목만
> 교체 없이 수정했고 스택·실제 RDS 모두 xlarge로 일치한다. 전환 뒤 요청 풀 대기 0과
> gateway·ERP HTTPS 200을 확인했다. 앱·migration·운영 데이터·통화 원장은 바꾸지 않았다.
> 다건 전화번호 해석의 함수 반복 호출 자체는 후속 쿼리 최적화 대상으로 유지한다.
> 같은 날 전화 수신 알림이 간헐적으로 통화 종료 뒤에야 보이던 현상을 다시 조사했다.
> 실제 ring 이벤트 자체는 gateway에 1초 안에 도착했지만, 전화데스크 진입·전역 상태바의
> 전체 통화 원장 조회가 누적 원장 전부를 조인·복호화한 뒤 고객 정보를 해석하고 마지막에
> 페이지를 자르는 구조여서 RDS CPU와 요청 풀을 소진했다. 전화데스크는 이제 SSR·mount·SSE
> 시 과거 원장을 자동 조회하지 않는 검색형 화면이다. 직원이 고객명/전화번호·최대 31일·
> 출처를 지정해 검색할 때만 서버가 먼저 날짜·검색 조건으로 후보 ID 20/50/100건을 자른 뒤
> 그 후보만 상세 조인·복호화·고객 해석한다. 기존 재통화 업무 큐도 최초 진입이나 실시간
> 이벤트에서는 읽지 않고 명시적 검색 응답에서만 함께 불러와 기존 완료·문자·발신 흐름을
> 보존한다. 동일 전화번호 고객 해석은 gateway 프로세스 안에서 15초 동안 single-flight로
> 공유하되 개인정보를 로그나 새 테이블에 저장하지 않는다.
> 전역 ERP 화면은 누적 전화 상태 카드를 렌더하지 않고 신규 ringing의 짧은 카드와 네이티브
> 브라우저 알림만 유지한다. 외부
> 수신 알림은 활성 직원 전원에게 가며, 현재 직원의 상담·리걸프렌즈 담당 고객이면 제목과
> 본문 첫 줄에 별표로 강조한다. 전화데스크에서만 현재 외부 수·발신은 전 직원 회선 기준으로
> 표시하고 내선은 해당 직원 문맥에 한정한다. 이 실시간 경로는 최근 진행 통화 snapshot만
> 읽으며 누적 원장이나 재통화 큐를 읽지 않는다. 이 최적화는 RDS xlarge 상향과 별개로
> 쿼리 증폭의 근본 원인을 줄이는 애플리케이션 기준선이며, 두 변경을 함께 유지한다.
> 2026-08-14에는 전화데스크에서 일반 내선이 장시간 `통화 중`으로 남는 현상을 운영 DB와
> Windows bridge 로그를 읽기 전용으로 대조했다. 비종료 internal root 6건 중 5건은 같은
> provider root의 양쪽 leg 가운데 수신 leg에 `HCAUSE=16` 종료가 이미 기록됐지만 발신 leg와
> root가 `connected`로 남은 종료 동기화 결함이었다. 나머지 1건은 발신 endpoint의 ring/channel
> 관측만 있고 상대 endpoint·종료 관측이 전혀 없어 강제 종료할 근거가 없었다. endpoint와 bridge
> heartbeat·queue는 모두 정상이어서 전역 지연이나 오프라인 문제는 아니었다. 앞으로 일반 내선과
> 상담 leg는 같은 root·kind·provider call ID인 한쪽의 실제 terminal cause를 반대쪽에도
> 동기화하고, 종료된 leg/root에는 뒤늦은 `CHANNEL_LIST`가 와도 connected 상태를 다시 열지
> 않는다. bridge가 호전환 직전 외부 문맥을 비운 경우에는 시간 근접이 아니라 내선을 건
> outbound endpoint에 유일하게 연결 중인 customer leg가 있을 때만 상담 통화 root로
> 보강한다. 양쪽 중 어느 쪽에도
> 종료 근거가 없는 단일 활성 leg 내선은 DB에서 추정 종료하지 않으며, 3분 뒤 실시간 카드에서는
> 제외하고 전화데스크 원장에는 `확인 필요`로 표시한다. migration
> `0058_centrex_internal_terminal_recovery.sql`은 같은 internal root/provider의 종료 leg가 있는
> 기존 분리 상태만 복구하고 한쪽짜리 원장은 건드리지 않는다. 이 변경은 아직 운영에 배포하지
> 않았으며 gateway와 ERP를 같은 릴리스로 전환하기 전에 `0058`을 먼저 적용한다.
>
> 2026-08-14 최근 통화의 체감 지연을 gateway·RDS·Windows bridge 로그로 대조했다. 정상
> Windows v2 ring의 gateway 유입은 p50 약 0.08초·p95 약 0.17초였고 요청 DB pool 대기는
> 일부 1초 구간에만 최고 26개로 나타난 뒤 다음 표본에서 회복됐다. 반복된 센트릭스 로그인
> 공백은 직원 PC의 기존 IQ200이 같은 센트릭스 계정으로 로그인해 bridge와 충돌한 원인임을
> 사용자 확인으로 특정했고, 직원에게 IQ200 삭제를 요청했으므로 이번 코드 범위에서는 별도
> 재로그인 변경을 하지 않는다.
> 대신 전화 실시간 경로는 provider `occurredAt`부터 gateway SSE write, ERP snapshot과
> 전화데스크 다음 paint 또는 브라우저 Notification 표시 완료까지 비식별 E2E 지연을 잰다.
> 인증된 SSE 연결마다 10분짜리 무작위 delivery ID만 발급하고 기존 다중 탭 leader가 전화
> snapshot을 갱신한 뒤 call state·표시 방식·브라우저 monotonic 처리시간만 ACK한다. 전화번호·
> 고객·직원·회선 식별자는 payload·metric·구조화 로그에 넣지 않는다. ACK는 delivery ID
> capability와 내부 API 키·ERP 세션 cookie 존재를 확인하되 세션 DB를 다시 조회하지 않아
> 직원 수만큼 DB query가 증폭되지 않는다. gateway는 원천→SSE, SSE→브라우저 준비,
> 원천→브라우저 준비, 브라우저 내부 처리의 원시 millisecond 표본을 1분 단위 CloudWatch
> `Lawand/Gateway` metric으로 묶고, p50·p95·max 구조화 summary와 2초 이상 첫 slow 표본을
> 개인정보 없이 남긴다. 상세 해석과 운영 query는 `docs/TELEPHONY_REALTIME_LATENCY.md`가
> 기준이다. 이 계측 후보에는 DB migration이 없으며 gateway와 ERP를 같은 릴리스로 배포한
> 뒤 실제 수신·발신으로 표본을 확보한다.
>
> 2026-08-14에는 외부 수신의 ERP 카드와 Notification을 고객명·고객 담당자·상담/사건
> 진행상태만 강조하는 20초 알림으로 축약했다. 현재 직원 소유의 office bridge 회선에서
> 아직 울리는 원본 수신만 `수신하기`를 노출하고, 서비스 워커 액션과 ERP 카드가 기존의
> 인증·소유권 검증 answer API를 호출한다. 다른 직원 회선은 `다른 회선`으로 표시하되 원격
> 당겨받기 버튼은 만들지 않는다. 센트릭스의 현재 `Answer()` 계약은 자기 회선의 물리 전화기
> 받기만 제어하며, 원장에 기록되는 `당겨받기`는 전화기에서 사람이 실행한 사후 관측이기
> 때문이다. 누군가 받아 root가 `connected`로 바뀌거나 종료되면 카드와 Notification을
> 20초 전에 즉시 닫는다. 알림을 끌 때는 열려 있는 LAW& persistent/page 알림도 닫는다.
> 두 ERP 인앱 알림은 내용뿐 아니라 시각 문법도 분리한다. 신건 상담은 초록 세로 강조선·
> 서류 아이콘·밝은 업무 카드로 차분하게 표시하고, 수신전화는 파란 고대비 실시간 헤더·
> 원형 전화 아이콘의 울림 신호·원형 고객 표시·파란 수신 버튼을 사용한다. 수신전화의
> `내 회선/다른 회선`과 `내 담당 고객` 배지는 본문 맨 위에 독립 배치해 전화 카드임을
> 먼저 인식한 뒤 수신 가능 여부를 바로 판단하게 한다. 움직임 축소 설정에서는 울림과
> 카드 진입 애니메이션을 모두 중단한다. OS가 그리는 Notification은 CSS를 적용할 수 없으므로
> 제목 맨 앞을 상담 `📝`, 수신전화 `📞`로 구분한다.
> 같은 날 후속 구현에서는 직원 `신규등록`도 홈페이지와 같은 7일 중복 판정을 적용했다.
> 일반 신규도 v1.33부터 이름과 관계없이 같은 정규화 전화의 활성 상담에 요청을 붙인다.
> 새 상담에만 최초 접수 알림톡을 만들며 반복 요청에는 중복 발송하지 않는다.
> 직원 신규등록 연락처가 리걸프렌즈에 있으면 홈페이지 접수와 같은 기존 사건·새 사건·공유
> 연락처 처리 게이트를 거친다. 관리자만 직원 신규등록으로 시작한 상담을 소프트삭제할 수 있고,
> 상담·요청·감사 원장은 보존한 채 종결해 목록·상세 개인정보를 블러 처리한다. migration
> `0056_staff_consultation_kakao.sql`은 삭제 시각·삭제 관리자와 종결 일관성 제약을 추가한다.
> 홈페이지 카카오 CTA는 이름과
> 광역 거주지역을 모두 필수로 받고, 실제 선택 지역을 암호화 intake와 리걸프렌즈 신건에 사용한다.
> 개인정보처리방침·이용약관의 수집 항목과 시행일도 2026-08-13 기준으로 함께 갱신했다.
> 같은 날 후속 구현에서는 카카오 모달에 `선택 · 입력하지 않으셔도 돼요`라고 명시한 휴대전화
> 입력을 추가했다. 비워 둔 접수는 기존 무전화 흐름을 유지하고, 입력한 `010` 번호만 정규화해
> AES-GCM 암호화와 HMAC 지문으로 저장한다. ERP와 리걸프렌즈 신건에는 실제 번호를 쓰며,
> 리걸프렌즈 기존 연락처와 일치하면 카카오 상담도 담당 배정 전에 기존 사건·새 사건·공유 연락처
> 처리 게이트를 거친다. 같은 `0056`은 카카오 채널의 전화 암호문·지문을 선택값으로 허용하며,
> 담당자 변경 원장과 실행 계약을 추가한 `0055_icy_harrier.sql` 뒤에 적용한다.
> 같은 통합 릴리스에서 `0055`·`0056`을 gateway보다 먼저 적용하고 홈페이지·ERP·gateway를
> 단일 소스로 전환했다. 운영 migration 원장은 57개이며 두 최신 해시는 Git과 일치한다.
> 상담 담당자 변경, 직원 상담 소프트삭제·중복/알림톡 처리, 카카오 선택 전화·지역/첫 메시지
> 안내, 서비스 워커 기반 상담 브라우저 알림이 모두 운영에 포함됐다. Windows bridge는 코드
> 영향이 없어 v0.8.3.0을 유지한다.
> 관리자 직원관리 목록은 이름·계정·소속·지역·부서·직책·역할과 업무 연동 상태를 먼저 훑을 수
> 있도록 한다. 기본 정보 수정과 센트릭스·리걸프렌즈 설정은 모두 기본 접힘이며, 업무 연동
> 요약에는 전화 연결 상태와 리걸프렌즈 연결 여부를 계속 표시한다. 관리자가 설정 보기를
> 명시적으로 열었을 때만 두 연동 편집 폼을 펼친다.
> 2026-08-14에는 전화번호 고객 해석에 활성 직원의 검증된 센트릭스 전체 회선번호를 추가했다.
> 해석 우선순위는 상담 묶음 → 직원 회선 → 리걸프렌즈이며, `07046074595`처럼 직원관리에서
> 검증·저장된 전체 번호가 들어오면 직원명과 내선을 표시한다. 비활성 직원·미연결 회선은
> 후보에서 제외하고 이 정보는 인증된 ERP snapshot에서만 반환하며 SSE에는 넣지 않는다.
> 상담 상세와 전화데스크의 문자·발신, 연결 뒤 후처리는 현재 상담·회선 담당자 여부와 무관하게
> 인증 직원 누구나 실행할 수 있다. 외부 명령은 실행 직원 자신의 활성 주 회선을 사용하고
> 문자·발신·후처리 감사 원장도 실제 실행 직원을 남긴다. 카카오 채널 상담은 채널명이 아니라
> 실제 전화 암호문·지문 존재 여부를 확인하며, 대표 상담 아래 묶인 구성 상담의 선택 전화번호도
> 대상에 포함한다. 번호가 없는 카카오 상담은 기존처럼 발송·발신을 거부한다. 후처리 화면은
> `connected`부터 열어 메모와 30분 단위 다음 일정을 저장하고, `ended` 뒤 결과를 다시 수정할 수
> 있다. 아직 울리는 중이거나 연결되지 않은 통화는 저장하지 않는다.
> 같은 날 후속 구현에서는 외부 수신 Notification과 ERP 실시간 수신 카드에 발신번호를 항상
> 표시한다. 기존 외부 수신 요약이 상담·리걸프렌즈만 분기해 이미 gateway가 식별한 직원 회선을
> `발신자 정보 없음`으로 낮추던 오류를 고쳐 직원명·내선을 그대로 사용한다. 직원 번호 해석은
> 직원 프로필의 검증 전체 회선과 활성 주 회선 endpoint 번호를 모두 후보로 삼아 지역 간 외부
> 경로에서 어느 번호가 발신번호로 관측되어도 같은 직원을 찾는다.
> 상담·직원이 아닌 기관·채권자 발신자는 공용 전화번호부에 표시 이름과 `원번호`, 선택
> `연결번호`를 저장한다. 이름과 두 번호 원문은 AES-GCM 암호화하고, 각 번호 HMAC 지문을 모두
> 조회 키로 사용하며 활성 연락처 사이의 교차 중복을 트랜잭션 잠금으로 차단한다. 해석 우선순위는
> 상담 묶음 → 직원 회선 → 전화번호부 → 리걸프렌즈다. 전화 후처리에서 현재 통화 번호가 원번호나
> 연결번호에 실제 포함된 경우에만 연락처를 함께 저장·수정하고, `법원 등 관공서`·`채권자 등`
> 결과를 고르면 저장 UI를 바로 연다. ERP `/phonebook`에서는 전 직원이 이름·두 번호를 검색하고
> 등록·수정·소프트삭제할 수 있으며 모든 변경은 직원 감사 원장에 남긴다. migration
> `0061_telephony_phonebook.sql`은 암호화 연락처와 최소 권한만 추가하는 로컬 출시 후보이며,
> 이 브랜치에서는 운영 migration·배포·실제 통화 canary를 수행하지 않는다.
> 연결 즉시 상세 페이지로 강제 이동하는 방식은 직원이 진행 중인 다른 ERP 작업을 잃을 수 있어
> 사용하지 않는다. 대신 실제 현재 회선 소유자 또는 통화 참여 직원의 보이는 브라우저 탭 한 곳에
> 통화 중 후처리 모달을 한 번 자동으로 연다. 모달에서는 메모·재연락 일정·문자를 바로 처리하고,
> 닫은 뒤에도 전화 카드의 `후처리 입력`으로 다시 열 수 있다. 같은 브라우저의 여러 탭은 통화
> ID별 localStorage 표식으로 중복 자동 모달을 막으며, 다른 직원의 화면은 자동으로 가리지 않는다.
> `Office_idx=56` 사건만 이름·전화·사건번호·원본 사건 ID 없이 추출한 자가진단
> 런타임 읽기 모델을 만들었다. 현재 1,759건(회생 1,342·파산면책 417)이며 gateway만
> 이 모델을 읽는다. `/bank/self-diagnosis`는 고객 상황과 유사한 다섯 건의 월 변제금·
> 예상 지출·추가생계비·총변제금·변제율·변제개월·주요 절차일을 보여주고 같은 입력을
> 암호화 ERP 상담으로 접수한다.
> 다만 이 구현은 로컬 출시 후보이며, 미성년 자녀 수 입력은 `0명`부터 `6명 이상`까지
> 버튼으로 선택하고 기존 숫자형 계약에는 6 이상을 6으로 보낸다. 과거 사건의 이용 근거·희소 조합 재식별 위험·
> 고객에게 표시한 다섯 카드의 비식별 스냅샷은 상담 intake에 함께 암호화해 저장하고 ERP
> 고객 상세에서 요청별로 다시 확인한다. 원본 사건 식별자·사건번호는 저장하지 않는다.
> 자가진단 히어로 제목은 다른 안내 페이지와 같은 데스크톱·모바일 타이포그래피 기준을
> 사용해 페이지별 시각 위계를 통일한다.
> 자가진단의 월평균 소득·담보 없는 채무·담보부 채무·예상 청산가치는 원 단위 입력을
> 유지하되 현재 금액의 만원·억원 표현을 입력칸 아래에 즉시 표시한다. 양수이면서 10만원
> 미만인 값은 다음 단계 이동 전에 `입력값 × 10,000`의 만원 단위 해석과 정확한 원 금액을
> 함께 보여 주고, 고객이 변환·원 단위 유지·재입력 중 하나를 명시적으로 선택하게 한다.
> `0원`은 정상 선택값이므로 확인 대상에서 제외한다. gateway 계약도 의심 금액을 원 단위로
> 유지할 때 확인한 필드명을 요구해 미확인 저액이 UI 우회로 접수되지 않게 한다.
> 2026-08-04 로컬 개발 DB의 테스트 상담 7건과 상담요청 7건 및 연결된 상태이력·알림톡·
> outbox를 단일 트랜잭션으로 초기화했다. 여정 분석 세션·이벤트, 네이버 IMAP 메일함
> 기준점, 직원·후기 원장은 보존했으며 운영 DB에는 적용하지 않는다.
> 결과 문구에 대한 책임 변호사 심사 전에는 운영 공개하지 않는다.
> 홈페이지 출시 고지의 첫 기준으로 `/privacy` 개인정보처리방침과 `/terms` 이용약관을
> 공개하고 전역 푸터·상담 동의·후기 동의에서 연결했다. 상담·후기·카카오 홈페이지
> 진입·네이버 예약·알림톡·리걸프렌즈 위탁까지 현재 구현된 처리 흐름과 1년 보관,
> 권리행사·파기·보안·자동화 경계를 문서화했다. 대표변호사이자 광고책임변호사는
> 김충환 변호사(변호사등록번호 15977)로 확정했으며 전역 푸터와 11개 법률 콘텐츠의
> 임시 책임자 문구를 같은 중앙 식별정보로 교체했다.
> `/bank/guides/costs`는 `개인회생 비용` 검색 의도의 독립 랜딩페이지다. 변호사 보수,
> 신청 인지대, 채권자 수에 따라 달라지는 송달료, 사건별 실비·예납금을 분리하고
> 2026-07-31 현재 전자제출 인지액 27,000원과 e-Post 1회 송달료 5,640원을 기준으로
> 채권자 수별 법원 기본비용 예시를 제공한다. 로앤의 실제 계약 기준 없이 업계 평균
> 수임료나 최저가를 만들지 않으며, 위임 범위·부가가치세·보정 대응·중도 정산·추가비용
> 승인 방식을 견적서와 계약서에서 확인하도록 안내한다.
> `/people`은 네 변호사의 현재 소속 사무소·등록번호·자격·학력과 대한변호사협회 등록
> 전문분야를 일반 취급 분야와 구분해 공개한다. 개인회생·개인파산의 법률 판단은 김충환
> 대표변호사가 담당하고, 법률컨설팅팀의 최초 상담·정보 정리·서류 발급 지원,
> 사건관리팀의 자료 취합·신청서 및 보정서 초안 준비, 변호사·의뢰인의 제출 전 확인을
> 실제 업무 순서로 설명한다. 2026-08-03에는 사용자가 제공한 NAS 원본 사진과 ERP
> `members` 읽기 전용 조회를 대조해, 현재 재직이며 공개 원본이 있는 실무 구성원 중
> 법률컨설팅팀·사건관리팀 43명을 서울·대전·부산 및 팀별로 추가했다. 공개 표기는
> 직책 대신 법률컨설팅팀 `컨설턴트`, 사건관리팀 `매니저`로 통일하고 구성원마다
> 2~4개의 고정 특징 태그를 표시한다. 휴직·퇴사 구성원 사진과 개인 연락처·내부 계정
> 정보는 공개 자산과 페이지에서 제외한다. 제공받은 사무실 이미지는 실제 시설 증명에
> 사용하지 않고 `공간 연출 이미지`로 표시한 보조 시각자료로만 사용하며, 방문 판단의
> 기준은 `/about#offices`의 실제 주소와 공식 연락처다.
> 전역 브랜드 표기는 `LAW&`와 `법무법인 로앤`을 하나로 묶은 고정 SVG 워드마크를
> 사용한다. 영문 세리프와 여백이 넓은 휴머니스트 한글 산세리프의 대비를 유지하되
> 앰퍼샌드와 분할선의 짧은 절개에만 로앤 그린을 사용하며, 헤더·모바일·푸터에서 같은
> 윤곽과 비례를 유지한다.
> 2026-08-04 서울 리전에 `lawand-prod` CloudFormation 스택을 생성해 homepage
> `t4g.small`, ERP `t4g.small`, gateway `t4g.medium` EC2와 탄력적 IP 세 개를 각각
> 분리 배포했다. 비공개 PostgreSQL 16.14 RDS는 암호화·7일 자동백업·삭제방지와
> 앱별 최소권한·공식 RDS CA `verify-full` 연결을 적용했고 CloudFormation 스택 종료
> 방지도 활성화했다. 공개 후기 3,403건, 자가진단 읽기 모델 1,759건, preview 사례
> 3건과 활성 직원 1명을 선별 이관했고, 2026-08-05에는 공개 사례 생성 원천인 비공개
> `CB` 세 테이블도 운영 RDS에 추가 이관했다.
> 상담·outbox·외부 실행 테스트 원장은 0건으로 시작한다. 세 worker와 EIP HTTP의 임시
> `sslip.io` HTTPS 301 전환·HTTPS 200, 실제 브라우저·재기동·CloudWatch 경보를
> 검증했다. 2026-08-12에는 Cafe24 대표 도메인 연결을 삭제하면 구 SSL까지 제거되는
> 계약을 확인해, 기존 DNS·호스팅을 그대로 살려 둔 채 계획된 Route 53 public hosted
> zone으로 네임서버를 전환했다. 기존 apex A·`revivetouch` A·wildcard CNAME·Daum
> MX·SPF를 모두 복제하고 홈페이지·ERP·API의 개별 EIP를 추가했다. 세 Caddy는 연결 중단
> 없는 reload로 정식 호스트를 활성화했고 apex·www·ERP·API의 Let's Encrypt 인증서를
> 발급했다. 새 EIP 고정 smoke에서 홈페이지 루트→`/bank`, ERP 로그인, gateway health와
> legacy fallback이 모두 200이며 구 EIP 고정 홈페이지도 200이라 DNS 전파 중 양쪽이
> 정상이다. 일부 로컬·통신사 resolver가 권한 NS 전환 전의 Cafe24 wildcard 응답을 계속
> 사용해 ERP를 구 apex로 보내는 인증서 오류를 차단하기 위해, 보존 중인 Cafe24 구 zone에도
> ERP A `3.34.72.9`와 API A `3.36.255.226`을 명시했다. Cafe24 네임서버 4곳이 두 레코드를
> TTL 1,800초로 반환하고 새 EIP의 Let's Encrypt 인증서와 ERP login·gateway health 200을
> 확인했다. 기존 apex·`revivetouch`·wildcard·MX·SPF와 호스팅·SSL은 변경하지 않았다.
> 2026-08-13에는 직원의 구 홈페이지 전용 접속점으로 Cafe24 무료 도메인
> `https://lawyerkch3.cafe24.com`을 활성화했다. 최초 302는 `.htaccess`가 아니라
> WordPress 4.7.2 멀티사이트의 고정 `DOMAIN_CURRENT_SITE=lawandfirm.com`이 알 수 없는
> Host를 네트워크 루트로 보내면서 발생했다. 무료 Host에서만 `sunrise.php`를 로드해 기존
> 사이트를 요청 단위 별칭으로 매핑하고, home·siteurl·쿠키·canonical과 HTML 및 정적
> CSS/JS의 하드코딩 절대 URL을 무료 도메인으로 바꾼다. DB의 주 도메인과 원본 정적 파일은
> 수정하지 않으며 `lawandfirm.com` Host에는 이 경계가 적용되지 않는다. 무료 도메인의
> 루트·`/lawnadmin`은 200, 존재하지 않는 경로는 현 Host의 404이고 모두 정식 도메인
> `Location`이 없다. 별칭 응답은 `X-Robots-Tag: noindex, nofollow, noarchive`로 검색 노출을
> 막는다. 공개 `lawandfirm.com`은 계속 AWS EIP `15.165.23.84`와 새 홈페이지 `/bank`를
> 사용한다. 구 WordPress가 노후 버전이므로 장기 보존 시 별도 인증 또는 접속 IP 제한과
> 업데이트·격리 계획이 필요하다.
> rollback은 NS 재변경이 아니라 Route 53 apex A를 구 `222.239.248.41`로
> 되돌리는 것을 1차로 한다. Secrets Manager의 ERP 공개 URL만 정식 주소로 갱신했으나
> 업무 통화가 이어져 gateway·ERP 프로세스 재시작은 무통화 시점으로 미뤘다. 실제
> EIP·접속점·도메인 전환·rollback 기준은 `docs/PRODUCTION_DEPLOYMENT_V1.md`가 운영 단일
> 진실원천이다.
> 같은 점검에서 도메인 전환 전부터 일부 v2 `call.ringing`이 gateway 400 뒤 Windows
> DPAPI dead-letter에 격리되는 별도 결함을 발견했다. queue는 0이나 dead-letter는
> 2026-08-12 10:05 KST 12건에서 10:14 KST 16건으로 증가했고 기존 수신 이벤트는 201로
> 계속 전달된다. 암호문을 삭제·재처리하지
> 않고 보존하며, 400 원인을 수정·검증한 뒤 통제된 재처리 여부를 결정한다.
> 2026-08-10에는 main 정리·자가진단 어텐션 UX·전체 검증을 마치고 홈페이지 출시 후보
> `20260810T064408Z-homepage-cutover-ready-v3`를 임시 EIP HTTPS에 배포했다. `/bank`의
> 공개 사례·후기는 이미지 빌드에 DB 비밀값을 넣지 않고 요청 시점에 읽으며, 첫 화면의
> 승인 사례 2건·후기 3건과 주요 상담·자가진단·목록 URL 200을 확인했다. 기존 WordPress
> 핵심 회생·파산 URL은 새 canonical로 한 번만 영구 이동하고, 아직 이관하지 않은 이혼·
> 보험·부동산 등은 정식 Caddy에서 기존 HTTPS origin으로 임시 전달하도록 검증했다.
> 당시 `lawandfirm.com` DNS는 `222.239.248.41`이었으며 책임 변호사의 자가진단 최종 출시 승인과
> Cafe24 DNS 접속 뒤에만 전환한다.
> 2026-08-05에는 `ai-agent-prod-01`의 공개 사례 생성 크론이 비공개 운영 RDS에
> 접근할 수 있도록 기본 VPC와 `lawand-prod` VPC를 peering하고 양방향 route를
> 추가했다. RDS 5432는 해당 서버의 사설 IP `172.31.2.38/32`만 추가 허용한다.
> `hub-cloudwatch-role`에 추가한 사례 생성 런타임 권한은 운영 database·gateway secret
> 읽기와 전용 로그·metric 쓰기로 제한했으며, 90일 로그 그룹과 실패 alarm을 구성했다.
> 운영 서버의 `inspect`와
> DB INSERT 없는 Luna/high `dry-run`이 모두 성공했다. published canary와 timer
> 활성화는 별도 운영 게이트로 남아 있고 timer는 계속 disabled/inactive다.
> 2026-08-14에는 ERP 후기관리 기본 흐름을 구현했다. 이전 공개 후기와 홈페이지 신규
> 검수 대기를 한 목록에서 보고, 상세에서 암호화 원문·작성 전화, 리걸프렌즈 고객 전체
> 이름·전화·사건 상태/법원/번호/담당자 최대 3명을 확인한다. 신규 후기는 개인 후기 요청
> 문자에 넣은 HMAC 서명 링크를 우선 연결하고, 링크가 없을 때만 전화번호가 정확히 한
> 고객 사건으로 해석되는 경우 자동 연결한다. 여러 후보·이전 후기는 직원이 고객찾기와
> 같은 검색으로 직접 연결하며 이름 유사도 자동 연결은 하지 않는다. 연결 당시 ERP 계정이
> 있는 사건 담당자 전원에게 비식별 SSE·브라우저 알림과 지속 `답글 필요` 배지를 제공하고,
> 모든 직원은 공개 제한 사유를 남기거나 공식 답글을 작성·수정할 수 있다. 공개 답글은
> 홈페이지 후기 카드에도 표시된다. 직원별 후기 요청 템플릿과 최대 30명 고객 선택은 기존
> 센트릭스 문자 outbox를 재사용하며, 응답 유실 재시도도 같은 발송 식별자를 유지한다.
> 통합 migration `0060_tranquil_dark_phoenix.sql`은 고객/사건 연결·담당자·답글·개인 템플릿·요청
> 원장과 공개 제한 감사 필드를 추가한다. 같은 상세에는 기프티쇼 비즈 상용 Key 심사용
> 모바일쿠폰 발송 UI를 실제 적용했지만 상품 조회·발송 API, 과금, 외부 전송은 승인 전까지
> 연결하지 않고 버튼을 잠갔다. 제출용 소개서·적용 화면 PNG는 `docs/submissions`에 둔다.
> 2026-08-14 완료 worktree 8개를 통합하면서 서로 겹치던 migration 번호는 일반 내선
> 종료 복구 `0058_centrex_internal_terminal_recovery.sql`, 문자 대화 직접 대상
> `0059_message_conversation_composer.sql`, 후기관리 `0060_tranquil_dark_phoenix.sql`의
> 단일 journal 체인으로 확정했다. 세 migration은 하나의 암호화 RDS snapshot 뒤 같은 gateway
> digest로 순서대로 적용하고, 전화·문자·후기 경계를 함께 쓰는 gateway·ERP와 공개 답글을
> 표시하는 홈페이지를 같은 릴리스 ID로 전환한다. Windows bridge와 기프티쇼 외부 API는 이번
> 릴리스 대상이 아니다.
> 2026-08-14 릴리스 `20260814T044148Z-integrated-telephony-messaging-reviews-v1`로 이
> 통합 경계를 운영 반영했다. GitHub Actions가 검증한 `main`에서 세 `linux/arm64` 이미지를
> private ECR에 게시했고, 암호화 RDS snapshot 뒤 같은 gateway digest로 `0058..0060`을 적용해
> migration 원장 61개와 새 테이블 6개를 확인했다. 홈페이지·ERP·gateway는 tag가 아닌 digest로
> 전환했다. 인증 smoke에서 발견한 후기 목록 raw SQL 시각의 문자열 반환 경계는 커밋
> `d6e0022b142bcf9b282a1ea25fcbe5f345379d32`와 gateway 전용 핫픽스 릴리스
> `20260814T052158Z-review-list-timestamp-hotfix-v1`로 보정했다. 실제 상담·리걸프렌즈·문자·
> MMS canary를 새로 만들지는 않았고 통화 원장도 강제 종료·보정하지 않았다.
> 2026-08-14에는 후기 요청 자동화 기준선을 보강했다. 모든 직원은 `상담을 받은 뒤`·
> `개시절차 진행 중`·`면책결정 이후`·`그 밖의 시점` 네 기본 슬롯을 항상 가지며 문구는
> 개인별로 수정하되 슬롯 이름·시점 키는 변경·삭제하지 않는다. 별도 템플릿은 이름·문구·
> 기본 시점을 정해 추가·수정할 수 있다. 고객찾기에서 선택한 대상은 검색 결과와 별개인
> 발송 목록에 고객명·전화·사건명·진행 상태로 누적된다. 발송 시 사건 유형과 템플릿 시점을
> 요청 원장에 snapshot으로 남기고, 고객별 HMAC 링크는 query가 아닌 URL fragment에 담아
> 서버·접근 로그와 referrer에 남기지 않는다. 홈페이지는 토큰을 POST body로 검증해 가린
> 공개 이름과 사건 분야·작성 시점을 미리 채운다. 전용 링크의 고객은 이름·전화번호를 다시
> 입력하지 않고 경험 키워드 1~3개·원문·필수 동의만 제출하며, gateway가 토큰에 연결된 현재
> 고객 사건의 휴대전화를 암호화 원장에 사용한다. 일반 공개 작성의 이름·전화 입력과 정확한
> 전화 일치 자동 연결은 그대로 유지한다. migration `0062_review_request_defaults.sql`은
> 네 기본 슬롯과 발송 당시 추천 문맥을 추가한다.
> 개인이 추가한 후기 요청 템플릿은 발송 이력 유무와 관계없이 ERP에서 삭제할 수 있다. 사용자
> 화면과 새 발송 선택에서는 즉시 제거하되 `deleted_at` 소프트 삭제로 과거 요청의 템플릿 참조를
> 보존하며, 삭제한 이름은 새 템플릿에 다시 사용할 수 있다. 고정 기본 슬롯 네 개만 삭제를 막는다.
> 이 경계는 `0063_review_request_template_soft_delete.sql`에 이어 적용한다.
> 2026-08-14 릴리스 `20260814T093947Z-integrated-phonebook-reviews-lawand-os-v1`로 공용
> 전화번호부·후기 요청 기준선·`LAW& OS` 브랜드와 관련 홈페이지·ERP·gateway 변경을 운영에
> 함께 반영했다. GitHub Actions `31787901184`이 검증한 소스
> `8f75bf5ce16dd4ebe2b2a17cc76db8b2679af6c7`의 세 `linux/arm64` ECR digest를 사용했고,
> 암호화 RDS snapshot 뒤 같은 gateway digest로 `0061..0063`을 적용해 migration 원장 64개와
> 활성 직원 43명의 기본 후기 슬롯 172개를 확인했다. 수정 대상 리걸프렌즈 dead event 한 건만
> 기존 1차 실패를 보존한 채 2차 시도로 재처리해 사건 연결·담당 배정·ERP 완료를 확인했으며,
> 리걸프렌즈 dead와 새 릴리스 dead는 0이다. 기존 통화 원장과 Windows bridge는 변경하지 않았고
> 실제 전화·문자/MMS·새 후기 링크 canary는 만들지 않았다.
> 2026-08-16에는 공개 후기 작성자 마스킹과 ERP 후기 요청 편집기 개선을 릴리스
> `20260816T035427Z-review-privacy-template-editor-v1`로 운영 반영했다. GitHub Actions
> `31924301099`이 main 소스 `d4b503934506b5f9c3b6206c039f424b77ebcc1e`를 검증하고 세
> `linux/arm64` 이미지를 게시했으며 gateway·ERP·홈페이지를 같은 릴리스 ID의 불변 digest로
> 전환했다. 홈페이지 공개 최신 후기 9건은 모두 `김○○ 고객` 계열로 마스킹되고 원 작성자
> 문자열 노출은 0건이다. 운영 ERP의 실제 Chrome에서 기본 4개·개인 2개 템플릿 그룹,
> 미리보기·기본 필드 잠금·미저장 발송 차단·새 템플릿 상태와 390×844 레이아웃을 확인했고
> hydration·console 오류와 임시 세션 잔존은 0이다. DB 변경이 없어 snapshot과 migration은
> 실행하지 않았고 템플릿·발송·통화 원장, Windows bridge와 외부 서비스도 변경하지 않았다.

---

## 0. 비전 — 고객 전 생애주기의 자동화

로앤 홈페이지 / 새 ERP / 리걸플로 / 리걸프렌즈를 하나로 묶어, 고객의 전 생애주기를
**최대한 사람 손이 안 타는** 비즈니스 로직으로 흐르게 한다.

예시 흐름 (비즈니스 프로세스의 앞단):

1. 고객이 **홈페이지에서 상담신청** (또는 회사에 전화 → 실제 운영으로 지킬 수 있는
   예상 연락 범위를 안내하고 문자/카톡 발송).
2. **ERP에 상담이 자동 등록**되고 브라우저 알림이 뜬다.
3. 상담을 맡고자 하는 상담자가 캐치 → 이 데이터가 **리걸플로로 등록**된다.
4. 고객이 상담내용을 남긴 경우, 그 상담의 **법적 쟁점과 수임전환 전략을 LLM으로 생성**.
5. 상담자가 ERP 클릭투콜로 전화를 건다 → 후속 단계에서 **ERP 실시간 STT** 진행
   (OpenAI 실시간 STT API).
6. STT가 진행되는 동안 **법적 쟁점·수임전환 전략을 실시간으로 계속 제안**.
7. 상담 완료 → **STT 자동 요약 + 의뢰인에게 메시지** 발송.

계약 이후 실제 사건이 진행되는 동안 고객과의 정보 공유·할 일·서류·상태 확인은
**리걸프렌즈 애플리케이션**이 담당한다. 새 ERP는 내부 업무와 자동화의 상태 원장이고,
홈페이지는 우선 **계약 전 정보 탐색·신뢰 형성·상담 전환**에 집중한다. 홈페이지 자체
회원가입·고객 포털은 초기 우선순위에서 제외한다.

이 문서는 앞단만 예시로 들었지만, **끝까지 모든 단계를 같은 방식(이벤트 기반 자동화)으로
설계**하는 것이 목표다.

### 0-1. 현재 제품 우선순위 — 개인회생·파산 홈페이지부터

지금은 고객에게 보일 새 홈페이지가 없는 상태이므로 내부 인프라만 먼저 만드는 순서가
아니라, **검색 가능한 공개 기반과 상담 수직 흐름을 먼저 세운다.**

첫 제품 범위는 개인회생·개인파산·면책 분야다. 검색의도별 정보 탐색, 기존 고객후기,
승인된 공개 사례, 익명 상황 정리, 상담 요청과 ERP 인계까지를 홈페이지의 주 역할로
설계한다. 계약 후 사건 정보는 리걸프렌즈로 인계한다. 상세 정보구조·모바일 레이아웃·
디자인·SEO·AI와 광고 규정 경계·연동 계약의 기준선은
[`docs/HOMEPAGE_BLUEPRINT_V1.md`](docs/HOMEPAGE_BLUEPRINT_V1.md), 콘텐츠 이전·랜딩페이지·
후기/사례 운영 기준은
[`docs/SEO_CONTENT_MIGRATION_V1.md`](docs/SEO_CONTENT_MIGRATION_V1.md)를 따른다.

시각 정체성은 **아이보리·잉크 중심의 차분한 로앤**으로 확정한다. 그린은 넓은 브랜드
면이 아니라 버튼·링크·체크·진행 상태처럼 사용자의 다음 행동을 알리는 신호색으로 쓴다.

---

## 1. 권장 저장소 구조 — 모노레포 하나

핵심 통찰: 이 프로젝트의 본질은 "화면 3개"가 아니라 **하나의 도메인 모델 위를 흐르는
이벤트 파이프라인**이다. 따라서 저장소 구조도 거기에 맞춘다. **모노레포 하나, 앱은 분리,
도메인·이벤트·DB·AI는 공용 패키지.**

```
projects/lawand/                  # 모노레포 루트 (pnpm workspaces + Turborepo)
├── apps/
│   ├── homepage/                 # 로앤 홈페이지 (Next.js) — 공개·SEO·상담신청 접점
│   ├── erp/                      # 새 ERP (Next.js) — 직원용 운영 화면
│   └── gateway/                  # 장수명 서버 (Node) — WebSocket/SSE, 큐 워커, 웹훅 수신
├── packages/
│   ├── core/                     # 도메인 모델 + 이벤트 카탈로그 (상담 상태머신 등)
│   ├── db/                       # 스키마·ORM 클라이언트 — DB의 단일 진실원천
│   ├── ai/                       # LLM 프롬프트·STT 파이프라인·모델 클라이언트
│   ├── integrations/             # 리걸플로·리걸프렌즈·카카오알림톡·전화(PBX) 어댑터
│   └── ui/                       # 공용 디자인 시스템 (shadcn 기반)
├── docs/                         # 설계 문서, 이벤트 카탈로그 문서
├── CLAUDE.md / AGENTS.md         # 첫 커밋부터 — Claude·Codex 교차작업 전제
├── pnpm-workspace.yaml
└── turbo.json
```

**스택**: 기존 운영콘솔(`ai-agents-mono`)에서 쓰는 조합을 그대로 재사용 전제 —
Next.js 16(App Router) · React 19 · TypeScript · Tailwind 4 · shadcn/ui · TanStack Query.
이미 손에 익었고, 실시간·AI 중심 요구사항에 TypeScript 생태계가 가장 잘 맞는다.

---

## 2. 이 구조를 떠받치는 핵심 결정

### 2-1. 모노레포인 이유 — 계약(타입)의 공유가 이 프로젝트의 정체성
상담·사건·고객이라는 같은 개념을 홈페이지, ERP, 워커, 알림이 전부 만진다. repo를 나누면
첫 주부터 타입이 복제되고 드리프트가 시작된다. 혼자(+에이전트들) 개발하는 구조에서 repo
분리의 이점은 없다.

### 2-2. 이벤트를 1급 시민으로 (`packages/core`)
위 흐름을 코드로 옮기면 이런 이벤트 체인이다:

```
consultation.requested
  → consultation.assigned
  → call.started
  → call.transcript.delta
  → consultation.completed
  → summary.sent
```

"사람 손 안 타는" 자동화의 성패는 이 **이벤트 정의를 처음부터 명시적으로 카탈로그화**하고,
DB에 이벤트를 남기고(**outbox 패턴**), 워커가 소비하는 구조를 잡느냐에 달려 있다.
이미 이 EC2에서 `debt_cert_events`, `transfer_events`로 하고 있는 패턴의 일반화판.

### 2-3. 실시간은 Next.js 밖으로 (`apps/gateway`)
OpenAI Realtime STT, 브라우저 알림 푸시, 상담 중 실시간 쟁점 스트리밍은 전부 **장수명
연결**이 필요한데 Next.js 서버리스 모델과 상성이 나쁘다. 작은 Node 서버 하나에
WebSocket/SSE + BullMQ(Redis) 워커를 모아두면, 홈페이지 상담신청 웹훅 수신처도 여기가 된다.

첫 운영 실시간 흐름은 상담 목록 SSE다. `outbox_events`의 `consultation.*` INSERT가
커밋될 때 PostgreSQL `NOTIFY`를 보내고, gateway의 전용 `LISTEN` 연결이 이를 인증된
SSE 구독자에게 전달한다. 브라우저는 gateway 비밀키를 가지지 않고 ERP Next.js의
same-origin route를 통해 스트림을 받는다. 이벤트에는 PII를 넣지 않으며, 수신 시와
재연결 시 ERP가 권한 있는 목록 API를 다시 읽어 상태를 보정한다. 현재 단일 gateway에는
PostgreSQL fan-out이면 충분하고, 다중 인스턴스·고부하가 실제로 생길 때 Redis를 추가한다.

센트릭스 클릭투콜도 gateway의 장수명 워커가 담당한다. ERP 브라우저는 상담 ID만 보내고,
gateway가 실제 전화 수집 여부와 실행 직원의 활성 주 회선을 확인한 뒤
`telephony.call.requested`를 만든다. 전용 워커는 한 번에 명령 하나만 처리하고 명령
사이에 최소 3초를 둔다. 이벤트·로그·전달 시도 원장에는 고객 번호를 넣지 않으며,
암호화된 상담 번호는 센트릭스 POST 직전에만 복호화한다. clickdial은 직원 전화기에 먼저
벨을 울리고 직원이 받은 뒤 고객에게 연결한다. 센트릭스 응답이 없거나 워커가 명령 중
중단되면 실제 발신 여부가 불명확하므로 `unknown`으로 끝내고 자동 재시도하지 않는다.
비밀번호 원문은 현재 직원관리 제출과 gateway 메모리에서만 사용하고 즉시 SHA-512로
변환한다. `userinfo`의 전체 회선·내선이 입력값과 정확히 일치할 때만 endpoint를 만들고,
SHA-512는 endpoint ID를 AAD로 한 AES-256-GCM 암호문으로 전용 인증 테이블에 저장한다.
일반 viewer와 PUBLIC은 이 테이블을 조회할 수 없고 gateway 런타임만 접근한다. 기존
Secrets Manager 자격증명 맵은 무중단 전환용 읽기 전용 fallback으로 유지하며 DB 값을
우선한다. 운영 계약과 활성화 게이트는
[`docs/CENTREX_CLICK_TO_CALL_V1.md`](docs/CENTREX_CLICK_TO_CALL_V1.md)를 따른다.
명령 전달 성공 뒤에는 같은 워커가 A타입 `callhistory`를 조회한다. 회선별로 마스킹된
수신 번호와 요청 시각이 맞는 종료 이력 하나를 연결하고, provider 상태·시작/종료·전체·
연결 시간을 저장한다. ERP는 종료된 최신 본인 통화에 미확정 결과가 있으면 자동 팝업을
열며, 인증 직원이 선택한 실제 disposition은 수정 가능한 감사 대상 업무 판단으로 보존한다.
센트릭스의 `OK`는 사람뿐 아니라 음성사서함 연결도 포함하고 `FAIL/0초`는 통화 중과
수신 전 발신 취소를 구분하지 못하므로 세부 disposition을 제공자 값만으로 확정하지 않는다.
상담 목록 응답에는 상담별 최신 확정 통화 disposition을 포함한다. 최신 결과가 무응답이면
`부재`, 명시적인 후속 연락 분류이면 `재상담 필요` 배지를 표시하고 둘 다 기존 `확인 필요`
지표·필터에 포함한다. 이후 더 최신 통화 결과가 확정되면 최신 결과를 기준으로 해소한다.

고객 문자 v1도 같은 gateway 직렬 명령 경계를 사용한다. 전화번호가 실제 수집된 상담에서
인증 직원은 설정된 공용 대표 endpoint `070-4607-0588`로 텍스트 전용 문자를 보내며,
직원의 개인 주 회선은 전화·Windows bridge에만 유지한다. 센트릭스 `smssend`에
SMS 80바이트·LMS 720바이트 한도를 적용하고,
JPG 이미지가 붙은 템플릿은 기존 SOLAPI 자격증명과 사전 등록 발신번호로 MMS를 보낸다.
템플릿은 `owner_user_id` 기준 직원 개인 설정이며, 기본 제공 템플릿 없이 만든 직원만
조회·수정·사용·삭제한다. 개인 템플릿은 별도 활성화 체크 없이 항상 상담 화면에 표시한다.
삭제해도 과거 발송의 본문·템플릿명·이미지 스냅샷은 보존하며 현재 템플릿 참조만 해제한다.
이미지 Base64는 저장하지 않고 SOLAPI
스토리지 파일 ID·미리보기 URL·크기·해상도만 보존한다. migration
`0047_wandering_maximus.sql`부터
발송 원장에도 이미지 URL snapshot을 남기고, 기존 MMS는 현재 템플릿의 파일 ID가 발송
snapshot과 같은 경우에만 URL을 보강한다. 따라서 템플릿을 나중에 수정해도 과거 문자에
다른 이미지가 표시되지 않는다. 고객 번호와 본문은 outbox·로그에
넣지 않고 발송 직전에 기존 암호문에서 복호화하며, 실제 보낸 본문과 제공자 접수 결과는 별도
문자 원장과 상담 상세에서 확인한다. ERP는 개인 템플릿 생성·수정·삭제, JPG 명함 첨부,
허용 변수 치환과 실시간 휴대전화 미리보기를 제공한다. 운영 적용·통제 발송 게이트는
[`docs/CENTREX_MESSAGING_V1.md`](docs/CENTREX_MESSAGING_V1.md)를 따른다.

현재 운영 배포 코드는 직원의 활성 주 회선을 SMS/LMS endpoint로 선택하지만,
2026-08-19 통제 canary로 공용 대표 endpoint `070-4607-0588`을 선택한 발송·표시·회신·ERP
연결 전 구간이 확인됐다. migration `0069_talented_meltdown.sql` 후보는 직원의 개인 endpoint
binding을 통화와 Windows bridge에 그대로 두고 센트릭스 텍스트 문자에만 별도의 활성·인증
공용 발신 endpoint 설정을 둔다. `telephony_messages.staff_user_id`는 실제 발송 직원을 계속 보존하고
`endpoint_id`만 0588 공용 계정을 가리킨다. 발송 시점에는 고객이 회신할 실제 mailbox endpoint와
표시 발신번호 snapshot을 함께 보존한다. 수신 매칭은 같은 mailbox·같은 상대번호의 수신시각 이전
성공 또는 결과 불명확 발송만 후보로 삼아 가장 최근 건을 연결한다. snapshot이 없는 과거 발송과
다른 mailbox 발송은 고객번호만으로 호환 연결하지 않는다. SMS 프로토콜에는 원 발송 ID가 없으므로
같은 mailbox·고객번호로 서로 다른 사건에 연속 발송하면 최신 발송 기준이라는 한계는 남는다.
현재 ERP `/messages`도 고객번호 단독이 아니라 Case_idx·상담 ID·직접 연락처 ID별 대화이므로,
고객번호 단일 대화로 합칠지는 동일 번호 공유 고객·복수 사건 병합 정책과 함께 별도 결정한다.
센트릭스 SMS/LMS와 달리 SOLAPI JPG MMS는 `02-555-7455`를 계속 표시하며, 후보 코드는
그 번호의 유일한 활성·인증 대표 endpoint를 회신 mailbox로 snapshot하되 매핑 실패가 기존 MMS
발송 자체를 막지는 않는다.

2026-08-11 대표 문자 수신함은 직원 개인 endpoint·binding·Windows bridge와
분리된 `representative` endpoint 7개를 migration `0044_sturdy_preak.sql`에 비활성
메타데이터로 등록한다. 현재 클릭투콜 발신 회선과 대표전화의 U+ 순차착신 설정은 바꾸지
않는다. 계정 비밀번호는 코드·문서·migration에 저장하지 않고 배포 뒤 TTY 전용 연결 명령에서
입력해 `userinfo`의 회선·내선 일치를 확인한 경우에만 기존 endpoint 문맥 AES-GCM 원장에
연결한다. 단일 gateway 수신 워커는 U+ `getrecvsmslist`를 대표 계정별로 직렬 조회하며 최신
첫 페이지와 과거 backfill을 번갈아 처리한다. 번호와 본문은 암호화하고 로그에는 endpoint ID,
건수와 오류 코드만 남긴다. 수신시각 이전의 같은 상대번호 최신 성공·전달 불명확 발신을 찾아 그 발신의
상담 또는 리걸프렌즈 `Case_idx`를 상속하며, 선행 발신이 없으면 고객을 추측하지 않고 미연결
원장에 둔다. ERP 전역 `/messages`는 Case_idx별 센트릭스 SMS/LMS 수·발신과 SOLAPI MMS
발신을 시간순으로 합치고, 기존 개인 템플릿 화면은 이 페이지의 모달로 옮긴다. 대표전화
착신 대상·순서와 근무/휴무 조건 라우팅은 후속 범위다. 운영 migration과 gateway·ERP는
릴리스 `20260811T035307Z-centrex-message-inbox-v1`로 배포했다. 인증 `/messages`와 API는
기존 대화 12개·비활성 mailbox 7개를 정상 반환한다. 이후 사용자가 TTY 전용 명령에서
대표 계정 7개를 모두 `userinfo` 검증 연결했고 5개 수신함은 정상 동기화됐다. 다만
`051-502-1919`와 `02-555-7455`의 과거 첫 페이지에는 U+가 일반 국내 전화번호가 아닌
`숫자 1자리 + w + 숫자 6자리` 형태의 `SRC`를 반환한 기록이 각각 1건·4건 섞여 있었다.
기존 파서는 이 기록 하나만 격리하지 않고 페이지 전체를 `invalid_response`로 실패시켰다.
출시 후보는 U+가 문자열로 정의한 비표준 `SRC`를 불투명 provider 식별자로 암호화·HMAC
보존하되 일반 전화번호와 별도 namespace를 사용하고 최신 발신 맥락 매칭에서 제외한다.
ERP에는 원문 대신 `발신번호 확인 필요`만 표시하며 같은 페이지의 정상 번호 메시지는 계속
수집한다. 이 수정은 통합 릴리스 `20260811T104143Z-integrated-call-messaging-v2`로 운영 반영했고,
기존 실패 상태는 다음 polling에서 자동 해제돼 대표 mailbox 7개가 모두 오류 없이 최근
동기화됐다. 실제 통제 회신 Case_idx canary는 별도로 수행한다.
같은 화면의 후속 출시 후보는 인증·역할 검사를 통과한 직원에게 대화 목록의 전체 고객
전화번호를 하이픈 형식으로 표시한다. DB 암호화, outbox·로그 비식별 계약과 조회 감사는
유지하며 비표준 U+ `SRC` 원문은 전체 번호로 취급하지 않는다. 대화 말풍선은 휴대전화와
비슷한 약 18~23자 폭과 15px급 본문으로 제한하고, MMS는 파일명 대신 발송 당시 이미지를
표시한다. 같은 선택 행을 다시 눌렀을 때 loading만 켜고 조회 효과가 재실행되지 않던 오류를
없애며, 빠른 대화 전환 중 늦게 끝난 이전 응답도 최신 선택을 덮어쓰지 못하게 요청 순서를
검증한다. 이 후속은 migration `0047_wandering_maximus.sql`과 gateway·ERP를 위 통합
릴리스로 반영했다. 기존 MMS 52건 중 파일 snapshot이 현재 템플릿과 같은 40건만 이미지 URL을
보강했고 비-MMS 오보강은 0건이다.
지연 운영 검증에서 gateway·ERP·Caddy와 외부 HTTPS, 재시작·error journal은 계속
정상이었지만 `lawand-slot-017` 하나가 성공 로그인 뒤 U+ `STATUS=-1(NotFound)` 재접속을
반복해 `lawand-centrex-login-failures` 경보가 ALARM으로 바뀌었다. 배정 원장 heartbeat,
프로세스, supervisor, DPAPI queue는 정상이고 사용자는 같은 직원의 다른 장소 로그인
가능성이 높다고 알려왔다. 따라서 배포 회귀보다는 외부 중복 로그인 충돌로 추정하고, 다른
로그인을 종료한 뒤 자동 재로그인과
경보 해제를 확인하며, 운영 중 슬롯을 강제 종료·재배정하지 않는다.

2026-08-14 `/messages` 대화형 작성 출시 후보는 휴대전화 문자 앱과 같은 탐색·작성 경계를
제공한다. 왼쪽 대화 목록과 오른쪽 선택 대화는 각각 최신 50개만 먼저 읽고, 목록 하단과
대화 상단 스크롤에서 불투명 cursor로 다음 50개를 이어 붙인다. 첫 선택과 새 문자 수신 뒤에는
가장 최근 말풍선을 보여주되, 직원이 과거 내용을 읽는 중이면 polling이 스크롤을 강제로
내리지 않는다. gateway는 과거처럼 수·발신 최대 500건을 먼저 조인·복호화하지 않고, SQL
CTE에서 전체 대화의 최신 원장·건수와 선택 대화 ID를 페이지 처리한 뒤 그 50건만 조인·
복호화한다. 목록 검색은 이미 불러온 대화에만 적용해 개인정보 검색어를 서버나 감사 원장에
추가하지 않는다.

선택 대화 하단의 고정 작성창은 개인 템플릿 또는 직접 입력, 200KB·1500×1440 이하 JPG
첨부, SMS/LMS/MMS byte 표시와 발송 상태 확인을 제공한다. `새 메시지` 모달은 번호 직접
입력, 로그인 직원의 최근 ERP 담당 상담, 기존 리걸프렌즈 검색 결과에서 수신자를 고른 뒤
같은 작성창을 재사용한다. 상담·리걸프렌즈 대상은 기존 gateway 재검증 경계를 유지한다.
직접 번호 대상은 migration `0059_message_conversation_composer.sql`의
`telephony_message_manual_contacts`에 정규화 번호 HMAC과 번호·표시명 AES-GCM만 보존하고,
수·발신 원장은 `manual_contact_id`로 연결한다. outbox·SSE·로그에는 번호·이름·본문을 넣지
않으며 직원 주 회선, 개인 템플릿 소유권, 센트릭스 직렬 SMS/LMS와 SOLAPI MMS 설정을 발송
시점에 다시 검증한다. 같은 번호는 하나의 직접 입력 대화로 이어지고 이후 대표 수신함 회신도
최신 성공·불명확 발신의 해당 대화를 상속한다. 전화데스크의 임의 통화 행에는 발송 버튼을
추가하지 않았고, `/messages`에서 직원이 번호와 내용을 명시적으로 확인한 요청만 새 직접
대상으로 만든다.

2026-08-14 후속 사용성 보강에서는 `/messages`의 좌측 대화 목록과 우측 말풍선 영역이
각자의 고정 Grid 행 안에서 실제로 줄어들 수 있도록 모든 중간 자식에 `min-height: 0`과
내부 overflow 경계를 명시한다. 선택적으로 나타나는 오류 배너 유무와 관계없이 말풍선은
항상 `minmax(0, 1fr)` 행, 작성창은 마지막 고정 행에 둔다. 데스크톱 문자 패널은
820~960px 높이, 모바일 대화 패널은 약 9:19.5의 휴대전화 세로 비율을 기준으로
780~900px 높이를 확보한다. 첫 대화 선택·대화 전환·발송 뒤에는 React가 최신 말풍선을
DOM에 반영한 레이아웃 단계에서 하단으로 이동하고, MMS 이미지가 늦게 로드돼도 직원이
최신 영역에 머무는 동안 하단을 유지한다. 직원이 과거를 읽는 중에는 15초 polling이
강제로 내리지 않으며, 상단에서 이전 50건을 붙인 뒤에는 기존 읽던 위치를 복원한다.
회사 수·발신 문자 원장은 관리자·정규직·아르바이트·별산·민원업체를 포함한 인증 직원
전체의 공유 업무함으로 유지하고 직원별 필터를 적용하지 않는다. 개인 문자 템플릿 소유권과
실제 발송 직원·열람 감사 원장은 공유 내역과 별도로 계속 보존한다.

2026-08-10에는 HERDR와 모든 `origin/worktree/*`를 전수 대조해 누락된 문자 브랜치를
`main`에 통합하고 고객 찾기 `0040` 다음의 migration `0041_late_talon.sql`을 운영에
적용했다. gateway·ERP 릴리스 `20260810T090235Z-customer-messaging-v1`에서 인증된 전역
템플릿 화면과 상담 상세 발송 UI를 확인하고, 사용자 지정 통제 수신자에게 센트릭스 SMS
한 건을 정상 담당자 API와 outbox를 통해 실제 발송했다. 제공자 코드 `0000`, delivery
HTTP 200, outbox published를 확인했으며 통제 상담은 감사 원장을 보존한 채 종료했다.
2026-08-10 SOLAPI 활성 발신번호 목록에서 운영 계정의 등록 번호 `010-****-1382`를 확인하고
`LAWAND_SOLAPI_MMS_SENDER`로 Secrets Manager와 실행 중 gateway에 적용했다. 이미지 MMS
발송 경계는 활성화됐으며 명함 JPG 실제 수신 canary는 남아 있다.
2026-08-12에는 인증이 완료된 대표번호 `02-555-7455`를 SOLAPI MMS 발신번호의 새 기준으로
확정했다. 출시 후보는 운영 secret 생성기가 과거 로컬·secret 값 대신 `025557455`를
명시적으로 기록하게 하며, main 통합 뒤 운영 Secrets Manager와 gateway 권한 600 환경파일을
같이 갱신하고 gateway를 전환해야 실제 MMS 발신번호가 바뀐다. 센트릭스 SMS/LMS 경로와
대표 문자 수신함 번호는 변경하지 않는다.
후속 migration `0042_bright_midnight.sql`은 기본 템플릿 세 건과 `is_active`를 제거하고
개인 템플릿만 허용한다. 템플릿 삭제 시 `telephony_messages.template_id`는 `SET NULL`로
해제하되 발송 당시 템플릿명·본문·이미지 스냅샷은 유지한다. 이 migration과 gateway·ERP
변경은 암호화 스냅샷 뒤 릴리스
`20260810T135657Z-profile-message-templates-v1`로 운영 배포했다. 운영 migration은 43개이고
기본 템플릿 0건·개인 템플릿 7건, 소유자 `NOT NULL`, `is_active` 제거, 과거 발송 FK
`SET NULL`과 역할별 권한을 확인했다. 같은 릴리스에 ERP `/profile` 내 정보·본인 센트릭스/
리걸프렌즈 연결·현재 비밀번호 확인 기반 변경과 최소 직원 초대도 함께 배포했다.

수신전화는 REST polling이 아니라 Windows bridge가 센트릭스 OpenAPI의 `RINGEVENT`,
`CHANNELLIST`, `CHANNELOUT`을 장수명 연결로 받아 gateway에 전달한다. 2026-08-06 실제
canary로 64비트 Windows Server의 WOW64에서 서명된 32비트 OCX와 `Answer()`가 동작하고,
받기 시 실제 센트릭스 전화기 스피커폰이 열리는 것을 확인했다. 일반 x86 COM 인스턴스만
만든 session 0 smoke test에서는 control site 초기화 전 `IsConnected()`가 실패했으므로,
전용 bridge는 .NET Framework 4.8 x86 WinForms STA·ActiveX control host·메시지 루프를
명시적으로 갖춘 실행 파일로 구현했다. interactive logon 작업 스케줄러에서 장수명 연결과
재접속을 유지하고, 비밀번호는 같은 Windows 사용자의 자격 증명 관리자에만 둔다. 실제
회선에서 로그인 결과의 내선·회선 suffix를 검증하고, 약 10초 수신 후 `RING_EVENT`와
`CHANNEL_OUT(HCAUSE=16)`까지 확인했다. 진단용 HTA와 상시 자동 받기는 운영 구성으로
사용하지 않는다.

bridge는 회선별 단일 연결과 재접속 backoff, provider unique ID 기반 이벤트 멱등성,
gateway 상호 인증, 로컬 암호 원문·발신번호 원문 로그 금지를 지킨다. `RINGEVENT`의
발신번호는 gateway가 권한 있는 서버 경계에서만 정규화·조회하고, ERP 실시간 SSE에는
전화 이벤트 ID만 보낸 뒤 인증된 스냅샷이 전체 번호를 표시한다. 상세 API는 상담데스크
우선·리걸프렌즈 비공개 동기화 원천 차선으로 고객을 해석한다. `Answer()`는 담당자가 ERP에서 명시적으로 누른 회선에만
보내고 중복 클릭·이미 종료된 ring·다른 직원 회선을 거부한다. canary의 한 번 자동 받기는
기능 검증을 위한 예외이며 운영 정책이 아니다. 세부 검증과 임시 자원은
[`docs/CENTREX_INBOUND_CANARY.md`](docs/CENTREX_INBOUND_CANARY.md)에 기록한다.

bridge→gateway v1 인증은 mTLS 클라이언트 인증서 대신 HTTPS 서버 인증과 애플리케이션
HMAC을 결합한다. bridge별 32바이트 secret은 Secrets Manager와 해당 Windows 사용자의
Credential Manager에만 두고 일반 gateway 환경 비밀과 분리한다. 서명 문자열은 버전,
HTTP method, 고정 path, bridge ID, 10자리 Unix 시각, 16바이트 nonce, 전송 JSON의
SHA-256을 줄바꿈으로 결합한다. gateway는 ±5분 밖 요청·본문 변조·알 수 없는 bridge를
인증 전에 거부한다. bridge는 gateway 200/201을 받기 전 DPAPI 큐를 삭제하지 않으며
같은 event ID를 순차 재전송한다. gateway 원장은 같은 event ID의 다른 payload와 같은
nonce의 다른 이벤트를 충돌로 거부한다. raw 전화번호는 이 TLS 요청과 gateway 암호화
경계에서만 존재하고 DB·브리지 로그·gateway 로그·응답·SSE에는 남기지 않는다.
실제 무응답 canary에서는 `RINGEVENT`와 `CHANNELOUT`의 unique ID가 같은 prefix에서
sequence만 1 차이 나는 서로 다른 leg로 왔다. bridge는 무응답 수신이 3분 이내일 때만
정확히 같은 ID 또는 같은 prefix의 인접 sequence를 동일 통화로 인정한다. 받기 뒤에는
`CHANNELLIST`의 양쪽 ID를 모두 추적하고 연결된 통화의 종료에는 3분 제한을 적용하지 않는다. Windows의
공용 proxy 설정에 이벤트 전달을 맡기지 않고 gateway 고정 HTTPS 주소로 직접 연결하며
TLS 1.2를 명시한다. 이 보강 뒤 기존 종료 이벤트의 멱등 replay는 약 3.6초 안에 200으로
끝났고 DPAPI 큐는 0건, 운영 원장은 통화 1건·이벤트 2건을 유지했다.

ERP 전역 수신 표시는 `telephony_inbound_events` INSERT의 commit 뒤 PostgreSQL
`lawand_telephony_inbound_events` 채널로 알린다. payload는 event ID·inbound call ID·
이벤트 종류·발생시각만 포함하고 번호·provider call ID·암호문·지문을 포함하지 않는다.
gateway는 별도 `LISTEN` 연결을 인증된 직원 SSE로 fan-out하고, ERP는 내부 키를 브라우저에
노출하지 않는 same-origin 프록시를 사용한다. 연결 직후와 source 재연결 때 `sync`를 보내
권한 있는 스냅샷을 다시 읽는다. 스냅샷은 울림 3분, 연결 12시간, 종료 후 20초 범위만
반환해 누락된 종료 이벤트로 오래된 울림이 계속 보이지 않게 하면서 장시간 통화는 유지한다.
브라우저에는 인증된 same-origin 스냅샷으로 복호화한 전체 번호·내선·활성 회선 담당자를
보내고, 로그인한 직원과 담당자가 같으면 `내 전화`로 표시한다. 동시에 울리는 통화는 조회
개수를 임의로 자르지 않고 통화 ID별 카드로 모두 표시한다. DB 암호화·검색 지문·마스킹
보조 컬럼은 유지하고 번호는 `NOTIFY`·SSE·애플리케이션 로그에 넣지 않는다. 고객·상담
해석은 다음 단계의 감사 가능한 조회 API로 분리한다.

리걸프렌즈 차선 조회 원천은 전체 SaaS RDS를 그대로 복제하지 않는다. MySQL
`CONTENT.TblCase.Office_idx=56` 사건을 기준으로 `CONTENT.TblCSClient`를 조인하고,
`ACCOUNT.TblMember.Office_idx=56` 담당자만 하나의 repeatable-read consistent snapshot으로
읽는다. `scripts/import-legalfriends-phone-directory.mjs`는 PostgreSQL `CB` 스키마의
세 임시 테이블을 먼저 적재하고 행별 해시·사건-고객 1:1·전화 검색 정규화·사무소 경계를
검증한 뒤 원자적으로 교체한다. 고객 미러는 `idx·Case_idx·name·phone·living_place·
name_search·phone_search·create/update_dt`, 사건 미러는 유형·상태·담당자와 사건 표시 필드,
담당자
미러는 `idx·member_id·name·position`과 사무소·시각만 보존한다. 비밀번호·생년월일·
개인 이메일/전화, 계좌 발급기관·계좌번호는 복제하지 않는다. 2026-08-06 스냅샷은
고객·사건 각 60,947건, 담당자 69건이며 전화 검색 오류·타 사무소 행·사건-고객 누락은
0건이다. 원본에 이미 없는 담당자 참조 1건은 자동 오연결하지 않고 미해결로 보존한다.
운영 이관 전 암호화 스냅샷 `lawand-prod-pre-phone-directory-20260806`을 만들었고,
`lawand_migrator`만 관리하며 `lawand_viewer`는 SELECT만 가능하다. `lawand_app`과
`PUBLIC`은 접근할 수 없다. 수신 통화의 고객 해석 API는 이 원천을 직접 브라우저에
노출하지 않고 필요한 읽기 결과만 감사 가능한 서버 경계에서 반환하는 후속 단계다.

이 동기화는 2026-08-07부터 크론 전용 EC2(`/opt/ai-agents`)의 `jobs/lf_phone_directory` 잡이
매일 03:30 Asia/Seoul systemd timer(`lawand-phone-directory-sync.timer`, `Persistent=true`,
랜덤 지연 180초)로 수행한다. 대상 DB URL은 Secrets Manager `lawand/prod/database`의
`migrationDatabaseUrl`만 프로세스 메모리로 읽고 파일·systemd unit·명령행·로그·Git에 남기지 않으며,
원본 MySQL은 ERP 서버 SSH 경유 자격증명으로만 읽는다. 단일 실행 잠금, 원본↔staging 행별 논리 digest
일치, 사건-고객 1:1, 사무소 경계, 전화 검색 정규화, 이전 성공 대비 급격한 행 감소(기본 10%) 차단을
모두 통과해야 교체하고, 교체 후 소유자·`PUBLIC`·`lawand_app` 차단과 `lawand_viewer` SELECT,
gateway security-definer 함수 EXECUTE 유지를 같은 트랜잭션에서 확인한다. 실패하면 롤백해 기존 세
테이블을 그대로 유지한다. 감시는 실패 즉시 팀 텔레그램 통지와 25시간 dead-man's-switch 워치독이
담당하며, CloudWatch 구조화 로그·metric·경보는 크론 EC2 역할에 최소권한 정책을 부착한 뒤
`run_sync.py setup-cloudwatch`로 켠다. 실행·검증 절차와 함정은 크론 EC2의
`jobs/lf_phone_directory/AGENTS.md`에 있다.

ERP `/clients`는 이 비공개 동기화 원천을 고객 찾기 읽기 모델로 사용한다. 검색은 gateway의
`search_legalfriends_client_directory` security-definer 함수만 통과하며
`CB.TblCSClient.Case_idx = CB.TblCase.idx`를 항상 조인하고
`COALESCE(CB.TblCase.del_flag, 0) <> 1`을 강제한다. 고객명은 공백을 제외한 2자 이상,
전화번호는 숫자 4자리 이상만 허용하고 한 번에 최대 50건만 반환한다. 브라우저와
`lawand_app`에는 `CB` 테이블 직접 권한을 주지 않으며, 감사로그에는 검색어 원문 대신
검색 종류·길이·결과 건수만 남긴다. 수신전화의 리걸프렌즈 차선 조회에도 같은 삭제 사건
제외 조건을 적용한다. ERP 상담 목록과 상세는 전화번호가 있는 접수에 한해 같은 원천의
완전일치 resolver를 한 번 더 적용한다. 상담별 조회에서는 그 상담의
`legalfriends_case_links.case_idx`와 같은 사건을 제외하고도 과거 사건이 남는 경우에만
`기존고객` 배지를 표시한다. 자기 사건만 일치하는 신규 고객은 `기존고객`으로 바뀌지 않고,
신건 생성 성공을 뜻하는 `리걸프렌즈 등록 완료`를 별도 표시한다. 실제 기존 고객이 새 사건을
만든 경우에는 과거 사건과 이번 등록이 각각 남으므로 두 상태를 함께 표시한다. 목록은 현재
페이지 상담 ID·전화번호·자기 사건 ID를 단일 묶음 조회하며 전화번호와 사건 ID를 응답·로그에
추가하지 않는다.

고객 찾기의 `전화 걸기`는 브라우저가 전화번호를 다시 보내지 않고 고객·사건 식별자만
gateway에 보낸다. gateway는 `resolve_legalfriends_directory_call_target` 함수로 같은 조인과
삭제 조건을 다시 검증한 뒤, 직원의 활성 센트릭스 주 회선으로 기존 click-to-call 명령을
만든다. 발신 시점 고객명·전화번호는 별도 대상 원장에 AES-GCM 암호화하고 outbox·SSE·로그에는
원문을 넣지 않는다. 이 통화는 기존 상담 클릭투콜과 함께 통합 전화데스크에 표시되며 통화
종료 뒤 같은 후처리·재통화 흐름을 사용한다.

2026-08-10에는 migration `0040_wandering_lenny_balinger.sql`과 gateway·ERP를 통합 릴리스
`20260810T082342Z-client-directory-v1`로 운영 배포했다. 운영 앱 역할의 함수 실행과 `CB`
직접 조회 차단, `PUBLIC` 함수 실행 차단, 삭제 사건 발신 대상 제외를 확인했다. 인증된 ERP
`/clients` 렌더와 잘못된 한 글자 검색의 400·감사 미생성을 no-call canary로 검증했으며,
실제 고객 찾기 발신·알림톡은 만들지 않았다. 배포 중 진행된 업무 통화는 Windows bridge를
재시작하거나 강제 종료하지 않았고, gateway 복구 뒤 DPAPI 큐 0·회선별 활성 중복 0을 확인했다.

2026-08-11 고객 찾기에도 상담 상세와 같은 개인 템플릿·직접 입력·SMS/LMS·JPG MMS 문자
작성 흐름을 추가했다. 브라우저는 전화번호를 보내지 않고 고객·사건 식별자만 전달하며,
gateway가 기존 `resolve_legalfriends_directory_call_target` 경계에서 삭제 사건과 전화번호를
다시 검증한다. migration `0043_famous_rafael_vega.sql`은 문자 원장에 상담/리걸프렌즈 대상
종류를 추가하고, 고객명·전화번호를 AES-GCM 스냅샷으로 보존하는
`telephony_message_directory_targets`를 만든다. outbox에는 고객·사건 ID와 암호화 원장
참조만 남기며 직원 개인 템플릿 소유권·회선·MMS 설정 검증과 기존 단일 문자 워커를 그대로
사용한다. 전화데스크는 통화 사실·후처리 원장이라는 역할을 유지하고, 고객 대상이 명시되지
않은 직접 수·발신 통화 행에서 원시 상대번호로 새 문자를 보내는 버튼은 추가하지 않는다.
`/messages`의 새 메시지는 위 `0058` 직접 대상 원장과 별도 감사 경계를 사용하며,
전화데스크 문자가 필요하면 상담 또는 리걸프렌즈 고객으로 해석된 상세에서 기존 대상
재검증 경계를 재사용한다. 이 변경은 암호화 RDS 스냅샷 뒤 migration `0043`과
gateway·ERP 릴리스 `20260810T231946Z-client-directory-messaging-v1`로 운영 배포했다. 운영
migration은 44개이며 최신 해시가 Git과 일치하고, 기존 상담 문자 2건 보존·신규 대상 원장·
역할별 권한을 확인했다. 인증 `/clients`의 문자·전화 및 개인 템플릿 UI를 확인한 뒤 직원이
실제로 요청한 고객 찾기 LMS 1건도 Centrex `succeeded`·outbox `published`로 완료됐다.
같은 날 직원의 실제 JPG MMS 네 건이 SOLAPI 등록 단계에서 모두 `1010(필수 입력 값 미입력)`으로
거절된 것을 확인했다. 수신번호·발신번호·본문·이미지 ID는 모두 존재했고, `strict: true`인
그룹의 MMS 제목 검사만 빠져 있었다. 고정 제목 `법무법인 로앤 안내`를 추가한 동일 요청을
발송하지 않는 임시 그룹에 넣어 `2000` 정상 접수를 확인하고 그룹을 삭제한 뒤, gateway 전용
릴리스 `20260811T001012Z-solapi-mms-subject-v1`을 운영 배포했다. 기존 실패 네 건은 감사
원장과 dead outbox에 보존하고 자동 재시도하지 않았다. 실제 단말 수신 canary는 여전히 별도다.

2026-08-12 고객 찾기 결과에 `신건상담에 등록` 흐름을 추가하고 운영 반영했다. 이름·휴대전화·거주
시·도·사건 유형은 선택한 리걸프렌즈 고객·사건을 기본값으로 채우되 등록 전에 모두
수정할 수 있다. 원천 `living_place`는 비공개 전화 디렉터리 동기화에 포함하고 gateway에서
광역 시·도 enum으로만 정규화해 브라우저에는 상세 주소를 노출하지 않는다. 등록 요청은
선택한 `client_idx·case_idx`를 security-definer 함수로 다시 검증한 뒤 기존 상담 요청과
같은 AES-GCM 고객정보·intake 원장을 만들며, 직원이 기록한 전화 상호작용 근거를 사용한다.
`소개건`을 선택하면 화면의 수정된 이름·전화번호는 소개받은 새 고객으로 저장하고, 선택한
기존 고객은 소개자로 구분한다. 소개자/기존 고객의 이름·전화·기존 사건·기존 담당자 snapshot은
`consultation_directory_sources`에 별도 AES-GCM 암호문으로 보존해 일반 intake와 외부 이벤트에
섞지 않는다. 상담 상세는 소개 여부, 기존 담당, 사건번호·Case ID와 리걸프렌즈 확인 링크를
먼저 보여 주고 수임료·계약 범위·기존 상담 메모를 확인하도록 안내한다. 이후 담당자가 기존
`상담하기`를 실행해야 본인 배정·리걸프렌즈 신건 등록·담당 배정 알림톡이 시작된다. 직원
신규등록 자체는 최초 접수 알림톡을 만들되 외부 사건은 만들지 않고, 같은 고객 반복 요청에는
접수 알림톡을 다시 보내지 않는다. migration `0050`, 전화 디렉터리 `living_place`
재동기화, gateway·ERP는 릴리스 `20260812T063358Z-integrated-worktrees-v1`로 함께 적용했다.
동기화 뒤 사건·고객 각 61,188건과 지역 null 0건, 관계·권한 경계를 검증했으며 실제 소개 상담·
기존 고객 신건과 이후 `상담하기` 외부 실행은 별도 승인 canary로 남긴다.

### 2-4. 기존 시스템은 어댑터 뒤로 (`packages/integrations`)
리걸플로·리걸프렌즈·기존 Laravel ERP 호출을 앱 코드에 흩뿌리지 말고 어댑터 패키지에
가둔다. 전환기 동안 기존 Laravel ERP는 계속 돌 테니(**strangler 방식**), 나중에 어느
시스템을 흡수하든 어댑터만 교체하면 된다. 새 스키마는 `packages/db`가 단일 소스로 갖고,
기존 `law_firm_db`와는 동기화 잡으로 잇는 게 현실적이다(이미 여러 개 운영 중인 패턴).

### 2-5. 계약 후 고객 접점은 리걸프렌즈

홈페이지에서 회원가입을 유도하거나 자체 고객 포털을 먼저 만들지 않는다. 홈페이지의
상담 접수와 ERP 목록 유입만으로는 리걸프렌즈에 고객·상담 데이터를 만들지 않는다.
새 상담이 최초 생성될 때는
`alimtalk.consultation.request_notification.requested`를 상담 접수 트랜잭션에 함께
남긴다. 정상 멱등 재시도·동일내용 중복·익명→실명 보강·동일 고객 재요청에는 고객 접수
알림을 추가하지 않는다. 동일 고객 재요청은 기존 상담 요청 이력으로 보존하며, 담당 배정
뒤라면 현재 담당자에게만 ERP 실시간 알림을 보낸다.
직원이 ERP에서 상담을 확인한 뒤 `상담하기`를 누르고 확인하면 그 직원을 담당자로
배정한다. 같은 DB 트랜잭션에서 `consultation.assigned` 업무 이벤트,
`legalfriends.consultation.registration.requested` 등록 실행 요청,
`alimtalk.consultation.assignment_notification.requested` 알림 실행 요청을 각각
outbox에 만든다. 외부 API 호출은 commit 뒤 독립 워커가 수행하며, 각 실행 요청의
재시도·중복 방지·실패 확인은 ERP 원장에서 따로 관리한다.
상담 묶음의 외부 실행 aggregate와 correlation은 대표 상담 ID를 사용하더라도,
`requestId`가 가리키는 요청과 선호 이름 암호문은 그 요청이 실제로 속한 구성 상담 ID를
AAD 문맥으로 복호화한다. 대표 ID를 구성 상담 암호문의 문맥으로 대신 사용하지 않는다.
다만 홈페이지 상담 연락처가 리걸프렌즈 고객·사건과 정확히 일치하면 `상담하기` 전에
`기존 사건 문의/기존 고객의 새 사건/공유 연락처`를 반드시 선택한다. 기존 사건 문의는
현재 조회 결과의 고객·사건 ID를 다시 검증해 상담 출처 snapshot과 결정 감사 원장을
저장하고 `legalfriends.consultation.registration.requested`를 만들지 않는다. 새 사건과
공유 연락처 선택은 구분을 보존한 뒤 기존 신건 등록을 계속한다.
리걸프렌즈 신건 등록은 `POST /api/bankruptcy/case/createForLawnV2`를 사용하고,
body에는 배정 직원의 숫자형 `member_idx`를 보낸다. 성공 응답의 사건 식별자는
`case_idx`·`case_id` 또는 숫자형 `data`를 허용해 연결 원장에 먼저 보존한다.
이 연결 원장이 존재하면 ERP에는 `리걸프렌즈 등록 완료` 상태를 표시하되, 이후 전화
디렉터리 동기화에 같은 사건이 들어와도 자기 사건만으로 `기존고객`을 판정하지 않는다.
`case_type`은 빠른 상담과 상세 상담 모두 기본 `1`로 보내며, 상세 상담의
`어떤 도움이 가장 필요하신가요?`에서 `개인파산·면책`을 선택한 경우만 `2`,
`기타`를 선택한 경우만 `3`으로 보낸다.
V2 실제 canary에서 최초 담당자 반영까지 확인했으므로 새 사건을 만든 직후
`changeManager`를 중복 호출하지 않는다. 담당자가 나중에 바뀌면 보존한 사건 ID와
직원의 리걸프렌즈 로그인 ID를 `member_id`로 사용해 `changeManager`만 호출한다.
ERP 상담 상세의 담당자 카드에서는 현재 담당자 또는 관리자가 활성 리걸프렌즈 계정과
숫자형 `member_idx`가 연결된 다른 직원을 선택하고 변경 사유를 남길 수 있다. 요청은
`consultation_assignment_transfers` 감사 원장과 개인정보 없는
`legalfriends.consultation.manager_change.requested` outbox를 같은 트랜잭션에 저장한다.
리걸프렌즈 워커가 저장된 사건 ID로 `changeManager`에 성공한 뒤에만 기존 상담 배정과
사건 연결 원장의 담당자를 한 트랜잭션으로 새 직원에게 바꾸고
`consultation.assignment.transferred`를 발행한다. 외부 변경이 실패하면 ERP 담당자는
기존 직원으로 유지하며, 응답 유실처럼 결과가 불명확하면 자동 확정하지 않고 두 시스템의
현재 담당자 확인 상태로 남긴다. 처리 중에는 전화·무효 처리를 잠그고, 성공 이벤트는 새
담당자의 ERP 탭에만 실시간으로 알린다. 담당자 변경 고객 알림톡은 이번 계약에 포함하지
않으며 승인 템플릿·발송 시점이 정해진 뒤 별도 이벤트로 추가한다.

사내 `무효` 분류도 새 사건을 만들거나 리걸프렌즈 원장을 삭제하지 않고 담당자 변경으로
처리한다. ERP 상담 상세에서 현재 상담 담당자 또는 관리자만 등록 완료 사건에 무효 처리를
요청할 수 있다. 개인정보 없는 `legalfriends.consultation.invalidation.requested` outbox가
저장된 사건 연결을 가리키고, 기존 리걸프렌즈 직렬 워커가 `changeManager`의 `case_idx`
헤더와 고정 `member_id=lawandfirm_s999`를 사용한다. `TblMember_idx=1824`는 고정 계약과
감사 이벤트에 함께 보존하지만 이 API의 body에는 보내지 않는다. 성공한 뒤에만 사건 연결
원장의 현재 담당자를 무효 계정으로 갱신하며, 상담·배정·외부 실행 원장과 기존 상담 상태는
삭제하거나 꾸며 바꾸지 않는다. 같은 상담의 대기 요청은 재사용하고 응답이 불명확한 실행은
자동 재시도하지 않은 채 외부 연동 원장에서 현재 담당자 확인을 요구한다.

알림톡은 Solapi `POST /messages/v4/send-many/detail`과 승인된 법무법인 로앤 채널을
사용한다. 상담 접수 템플릿에는 접수번호·한국시간 접수시각·연락예정, 담당 배정
템플릿에는 접수번호·담당자명·연락예정만 치환한다. 빠른 연락은 `가능한 빠른 시간`,
예약 연락은 한국시간 날짜와 시작·종료 시각으로 표시한다. `disableSms=true`로 문자
대체발송을 명시적으로 끈다. HTTP 성공 안에서도 개별 메시지 `statusCode=2000`과
메시지 ID가 있어야 outbox를 완료하고, 솔라피 접수 원장은 전화번호와 본문을 저장하지
않는다. 상세 계약은
[`docs/SOLAPI_ALIMTALK_V1.md`](docs/SOLAPI_ALIMTALK_V1.md)를 따른다.

네이버 예약은 공개 예약 조회 API 대신 공식 IMAP 메일함의 예약 확정 메일을 접수
트리거로 사용한다. gateway가 평일 08:00~19:00에는 5분, 나머지 시간과 주말에는
30분마다 `네이버예약` 폴더를 폴링한다. 첫 활성화 시 현재 UID를 기준점으로 저장해
기존 메일을 소급 등록하지 않고, 이후 확정 메일의 `business_id + booking_number`로
상담 생성을 멱등 처리한다. 메일에서 예약번호·마스킹 이름·예약 상품·상담 시각·
요청사항과 상세 URL을 암호화 대기 원장에 연결한다.

메일 단계의 전화는 `NULL`이며 ERP에는 `010-0000-0000 · 미수집`으로 표시한다. 상담
목록과 상세에는 마스킹 이름·예약 시각·상품·요청사항을 표시하고, 운영자가 필요할 때
`네이버 예약 상세 열기`로 직접 확인한다. 로그인 세션·2단계 인증·CAPTCHA에 의존하는
브라우저 자동화와 텔레그램 재인증 알림은 운영 복잡도와 실패 가능성 때문에 범위에서
제외한다. 현재 `naver_booking` 경로의 접수·담당 배정에는 리걸프렌즈·알림톡 실행을
만들지 않는다. 상세 계약은
[`docs/NAVER_BOOKING_IMAP_V1.md`](docs/NAVER_BOOKING_IMAP_V1.md)를 따른다.

이 동작은 직원 인증·담당 배정 권한·감사로그가 갖춰진 뒤에만 활성화한다. 이후 상담·계약
전환이 확인되면 ERP 이벤트를 통해 리걸프렌즈 초대·사건 인계가 이어지고, 사건 진행 중
정보 공유·할 일·서류·상태 확인은 리걸프렌즈가 담당한다. 홈페이지에는 공개 정보와 상담
접수 확인만 남긴다. 향후 리걸프렌즈로 해결되지 않는 명확한 고객 요구가 검증될 때만
별도 포털을 다시 검토한다.

### 2-6. 공개 AI와 자동 진단은 출시 게이트로 관리

대한변협의 현행 광고 규칙을 고려해 공개 LLM 법률상담 챗봇은 v1 범위에서 제외한다.
결정형 상황 정리·계산 도구도 단순히 "AI가 아니다"라고 전제하지 않고 책임 변호사의
광고 규정 검토를 통과한 범위만 공개한다. 우선순위는 내부 상담·문서·콘텐츠 보조 AI이며,
법률 판단·고객 발송·콘텐츠 발행은 사람이 책임진다.

자가진단 v1은 LLM 답변이 아니라 고정 입력·제약·가중 거리로 과거 사건을 비교한다.
그렇더라도 `개인회생 유사사례`와 `개인파산·면책 검토` 방향, 월 변제금과 주요 절차일은
법률서비스 선택에 영향을 주므로 자동 진단 출시 게이트를 그대로 적용한다. 기술 구현,
로컬 데이터 검증과 운영 공개 승인을 분리하고 상세 기준은
[`docs/SELF_DIAGNOSIS_V1.md`](docs/SELF_DIAGNOSIS_V1.md)를 따른다.

자가진단은 고객에게 예상 관할법원을 먼저 묻지 않고 상담 요청과 같은 현재 거주 시·도를
받는다. 거주지역 기준 법원이 하나면 즉시 표시하고, 경기·강원처럼 시·도만으로 하나를
정할 수 없거나 채무자회생법 제3조제10항부터 제13항까지의 추가 관할이 있는 충북·울산·
경남·전북·제주만 근거 설명과 함께 선택하게 한다. 제출 시 서버가 지역과 법원의 허용
조합을 다시 검증한다. 주소 외 근무지·영업소·재산 소재와 관련 사건도 실제 관할에 영향을
줄 수 있으므로 화면에서는 거주지역 기준 1차 안내임을 명시한다.

금액 입력은 원 단위를 유지하면서 각 입력 바로 아래에 현재 금액의 읽기 쉬운 만원·억원
표현을 함께 표시한다. 월평균 소득·담보 없는 채무·담보부 채무·예상 청산가치가 양수이면서
10만원 미만이면 다음 단계 이동 전에 만원 단위 누락 가능성을 확인한다. 변환 가능한 값은
`입력값 × 10,000`의 후보와 정확한 원 금액을 함께 제시하고, 고객이 변환·원 단위 유지·
재입력을 직접 선택한다. 자동 변환은 하지 않으며 `0원`은 확인 대상에서 제외한다. 공유
제출 계약은 원 단위를 그대로 확정한 의심 필드 목록을 검증해 클라이언트 확인 없이 저액
입력이 접수되는 것을 막는다.

### 2-7. 상담 요청과 광고 귀속은 분리 저장하고 요청 시점에 연결

`consultation`은 채널별 운영 원장, `consultation_request`는 실제 제출 이력으로 분리한다.
정규화 전화번호가 같고 마지막 요청이 7일 이내이며 기존 상담이 종결되지 않았으면 채널·
입력 이름과 관계없이 같은 상담 흐름으로 자동 묶는다. 일반 상담·자가진단·직원 신규등록은
대표 `consultation`에 요청을 붙이고, 별도 상담 원장이 필요한 카카오 선택 전화 접수는
`consultation_groups`와 `consultation_group_members`로 대표 아래 연결한다. 목록은 대표 한
건만 반환하고 상세는 모든 구성 상담과 요청을 반환한다. 이름 불일치는 자동 연결을 막는
조건이 아니라 가족·공용 번호 확인 경고이며 직원이 수동 분리한다. 전화가 없는 플친·네이버
접수는 자동 묶지 않고 고객 확인 뒤 접수번호로 수동 연결한다. 서로 다른 전화번호·담당자·
리걸프렌즈 사건은 수동 연결하지 않는다. 기존 상담이 미배정이면 `repeat_unassigned`, 담당
배정 이후이면 `repeat_assigned`로 남기고 담당 배정·리걸프렌즈 신건·최초 알림톡은 대표
경계에서 한 번만 실행한다.

최초 랜딩, 내부 이동, 상담 CTA와 제출 위치는 별도의 가명 여정으로 저장하고 각
`request_id`에 귀속한다. URL 전체가 아니라 내부 pathname, referrer host와 허용 목록의
AdPilot click ID·UTM·campaign/ad group/keyword/creative ID만 받는다. 이름·전화번호·상담
원문은 여정이나 이벤트에 넣지 않는다. 랜딩 문구가 바뀌어도 과거 성과가 변하지 않도록
page key·version과 광고 출처를 제출 시점에 스냅샷으로 고정한다.

모든 빠른·상세 상담 요청은 정확한 주소가 아닌 거주 시·도 하나를 필수로 받는다. 이는
관할 법원과 지역별 업무 처리 특성의 1차 확인, 담당 상담 배정과 지역별 상담·캠페인
운영 분석에 사용한다. 시·도만으로 관할이 확정되는 것처럼 안내하지 않는다. 거주 시·도는
상담 intake와 함께 인증 암호화하고 이벤트·귀속 스냅샷에는 복제하지 않는다. 지역별
성과는 권한 있는 내부 집계기가 `request_id`의 암호화 intake와 attribution을 결합해
시·도×캠페인·키워드·랜딩 단위로 만들며 이름·전화·상담 원문은 분석 결과에 포함하지 않는다.

상세 계약과 중복 판정 순서, 이벤트 예시는
[`docs/CONSULTATION_INTAKE_V1.md`](docs/CONSULTATION_INTAKE_V1.md)를 단일 기준으로 사용한다.
방문 귀속은 모든 상담 요청에 자동 저장한다. 이 first-party 운영 귀속만을 위한 별도의
동의 UI나 동의 시각은 현재 수집하지 않는다. 전체 URL이 아닌 내부 pathname, referrer
host와 허용 목록의 광고 식별자만 저장하고, 이름·전화·상담 원문은 귀속 데이터에 넣지
않는다. 처리 근거·목적·항목·보유기간은 전체 개인정보처리방침에 반영하고 출시 전
책임 변호사·개인정보 담당자의 승인을 받아야 한다.

GA4는 이 기존 first-party 운영 귀속을 대체하지 않고, 분석에 명시적으로 동의한 방문만
별도로 관측한다. 맞춤형 광고 기능과 광고 관련 동의는 항상 비활성화하고, 전체 URL을
그대로 보내지 않으며 실제 검색어·상담 식별자·연락처·상담 내용·진단값을 GA에 넣지 않는다.
직접 상담과 상담을 생성한 자가진단의 실제 신규 성공만 `generate_lead`로 보내며, GA 합계는
ERP 전체 상담이 아니라 `GA4 관측 리드`로 표시한다. 페이지 정제, 동의, 중복 판정,
AdPilot 경계와 출시 게이트는 [`docs/GA4_MEASUREMENT_V1.md`](docs/GA4_MEASUREMENT_V1.md)를
단일 계약으로 따른다. 네이버 광고 계정 연결은 이 계약과 홈페이지 구현의 선행 조건이
아니며, 연결 뒤 실제 자동 추적 파라미터를 캡처해 계약 적합성을 검증한다.

홈페이지 구현 후보는 `LAWAND_GA4_MEASUREMENT_ID`를 서버 런타임에서만 읽고 같은 origin의
비캐시 설정 API로 유효한 `G-` 값 또는 `null`만 브라우저에 제공한다. 이를 공개 빌드 변수로
넣지 않아 commit SHA별 ECR 이미지의 불변성을 유지한다. 루트 분석 컴포넌트는 모든 Next.js
공개 페이지의 동의·수동 페이지뷰를 담당하고, `JourneyTracker`도 `/bank` 밖의 새 랜딩에서
허용 목록으로 일반화한 pathname과 허용 귀속을 시작한다. 사례 상세 slug는 공통 상세 경로,
임의·404 경로는 `/_not-found`로 접어 경로에 섞인 식별자가 분석·운영 여정에 남지 않게 한다.
Cafe24 fallback URL은 같은 루트가 없으므로 미적용이며,
활성 광고 연결 뒤 그 랜딩이 0건인지 확인한다. 직접 상담·자가진단은 같은 idempotency key의
성공 재시도 동안 안정적인 브라우저 마커로 전환 중복을 막는다. 이 후보에는 DB migration이
없다. GA가 방문·세션 통계에 수반해 만드는 `first_visit`·`session_start`·`user_engagement`
기본 이벤트는 개인정보처리방침에 함께 고지하고, 향상된 측정은 전체 비활성화해 스크롤·
검색·폼 등 추가 자동 이벤트를 막는다. `_ga` 쿠키는 갱신하지 않고 최초 생성부터 최대
14개월로 제한한다.

공개 상담 POST는 브라우저가 gateway를 직접 호출하지 않고 홈페이지의 same-origin
서버 경계를 거친다. 홈페이지 서버와 gateway 사이에는 접수 전용 API 키를 요구하며, 클라이언트
주소는 원문을 전달·저장·로그하지 않고 홈페이지 서버에서 날짜별 HMAC 가명 키로 바꾼다.
gateway는 새 접수키 기준으로 같은 전화번호 6회/30분·12회/24시간, 같은 네트워크
60회/10분·300회/24시간을 허용한다. 같은 idempotency key의 네트워크 재시도는 별도
30회/10분까지 허용해 모바일 통신 오류나 응답 유실이 새 접수 차단으로 이어지지 않게
한다. 네트워크 한도는 공유망 오탐을 줄이기 위한 높은 보조 한도이고 전화번호 지문이
주 방어선이다. 한도 상태는 현재 단일 gateway 프로세스 메모리에만 보관하며 재시작 시
초기화된다. gateway를 여러 인스턴스로 확장할 때는 같은 계약을 Redis 원자 카운터로
옮긴다.

### 2-8. ERP는 초대 전용 직원 계정과 서버 세션을 사용

ERP에는 공개 회원가입을 두지 않는다. 최초 관리자는 운영 CLI의 1회용 초대로 만들고,
이후에는 `admin` 역할 직원이 초대 링크를 발급한다. 초대 생성의 필수값은 고정 식별자인
이름·이메일뿐이다. 가입 시에는 `법무법인 로앤`·`서울`·`미입력` 부서·`미입력` 직책·
`정규직`을 기본 멤버십으로 만들고, 직원이 로그인 뒤 우측 상단의 `/profile`에서 소속·
지역·부서·직책과 업무 연동을 완성한다. 관리자는 직원관리에서 같은 필드와 역할을 수정할
수 있다. 브라우저가 보낸 이메일·이름을 계정 생성에 사용하지 않고 gateway가 초대 원장을
다시 읽어 적용한다.

로그인 계정, 개인 프로필, 소속 멤버십, 초대, 세션, 감사로그를 분리 저장하며 퇴사 계정은
삭제보다 비활성화를 기본으로 한다. 소속은 `법무법인 로앤`, `리걸플로`, 지역은 `서울`,
`대전`, `부산`을 초기 기준으로 둔다. 한 직원이 추후 여러 소속·지역을 겸할 수 있도록
계정에 멤버십을 복수로 연결하고 초대 가입 시 첫 주 멤버십 하나를 만든다.

비밀번호는 가입 시 12자 이상이며 영문 대문자·소문자·숫자·특수문자를 각각 하나 이상
포함해야 하고, scrypt 단방향 해시만 DB에 보관한다. 초대·세션 토큰은 SHA-256 지문만
DB에 보관한다. 브라우저는
HttpOnly·SameSite=Strict 쿠키만 가지며 gateway의 상담 목록·상세는 ERP 내부 API 키와
유효한 직원 세션을 모두 요구한다. 상담 목록과 상세 PII 조회는 직원 ID 기준으로 감사
기록을 남긴다. 이름과 로그인 이메일은 고정 식별자로 유지한다. 소속·지역·부서·직책은
본인과 관리자가 수정하지만 역할·권한은 자기 승격을 막기 위해 관리자만 변경한다. 일반
직원이 URL이나 폼의 대상 ID를 바꿔도 서버는 본인 정보와 본인 업무 연동만 허용한다.
센트릭스 회선·내선·현재 비밀번호 검증과 Windows bridge 자동 배정, 리걸프렌즈 ID·
`member_idx` 연결도 본인이 직접 수행할 수 있다.

비밀번호 변경은 인증된 세션에서 현재 비밀번호를 다시 확인하고 같은 강도 정책을 적용한
뒤 모든 기존 세션을 폐기한다. 이메일 발송 인프라가 없는 현재 단계에서는 이메일 링크를
가장한 수동 절차를 만들지 않으며, 분실 계정의 이메일 기반 재설정은 발송 채널과 만료·
재사용 방지 원장을 함께 도입할 때 별도로 구현한다. 현재 역할·권한 그룹은 `관리자`,
`정규직`, `아르바이트`, `별산`, `민원업체`이고 관리자만 직원 초대를 만들 수 있다. 향후 페이지를 직원별로 일일이
설정하지 않고 역할 프리셋과 소속·지역·담당 범위를 결합해 모듈 단위 보기·처리·관리
권한을 부여한다. 세부 기준과 운영 전 게이트는
[`docs/ERP_AUTH_V1.md`](docs/ERP_AUTH_V1.md)를 따른다.

### 2-9. 고객후기는 원본성·맥락·공개 안전을 함께 보존한다

기존 Cafe24/WordPress KBoard의 `board_id=22` 중 회생·파산으로 연결된 후기 전량을
`review_import_batches`와 `customer_reviews`로 이관한다. 원본 UID·연결 UID·원문·작성일·
수정일·익명 표시명·분야·작성 당시 단계·고객 경험 키워드·원본 해시를 보존하며, 원본의
비밀번호·전화번호와 추가정보의 전화번호·성별·나이·유입경로는 조회·저장하지 않는다.
원본 계정정보도 저장소나 환경파일에 보존하지 않는다.

이관기는 삭제 상태를 공개 제외로, 전화·이메일·주민등록번호·사건번호·계좌·상세주소
패턴을 찾은 글을 검수 대기로 분리한다. 원본 해시가 같은 글을 다시 이관할 때 사람이
확정한 개인정보 검수 상태는 덮어쓰지 않고, 원문이나 상태가 바뀌었을 때만 다시 판정한다.
공개 페이지는 `published`이면서 개인정보 상태가 `clear/reviewed`인 행만 읽는다.

후기 허브는 별점·베스트 후기 중심이 아니라 분야, 작성 당시 단계, 고객이 직접 선택한
경험 키워드로 자신과 가까운 맥락을 찾게 한다. 원문·작성일·단계를 함께 표시하고
개별 결과가 다른 사건의 결과를 보장하지 않음을 가까이 둔다. 필터 조합은
`noindex, follow`, canonical은 `/bank/reviews`로 고정하며, 자사 후기의
`Review/AggregateRating` 리치결과 마크업은 사용하지 않는다. 기존
`/bank/successioncase_epilogue`는 새 후기 허브로 영구 이동한다.

신규 후기는 `/bank/reviews/write`에서 별점 없이 분야, 작성 당시 단계, 경험 키워드
1~3개, 공개 이름, 원문과 작성자 확인용 휴대전화를 받는다. 공개 이름은 입력값과 관계없이
첫 글자만 남긴 `김○○ 고객` 형식으로 gateway 접수 시 정규화하고, 공개 원장 승격과 홈페이지
조회 경계에서도 같은 규칙을 다시 적용한다. 따라서 변경 전에 접수·공개된 신규 후기까지
홈페이지에서 실명이 노출되지 않는다. 주민등록번호·계좌번호·사건번호·상세주소와 제3자
개인정보를 원문에 쓰지 않도록 안내한다. 제출 즉시 공개하지 않고
`customer_review_submissions`의 `pending_review`로 접수한다.

휴대전화와 후기 원문은 AES-256-GCM으로 각각 인증 암호화하고 전화·payload HMAC만
중복 판정에 쓴다. 홈페이지 same-origin 서버와 gateway 전용 키 경계를 거치며 기존
전화·네트워크 rate limit, UUID 멱등키, 10분 동일내용 중복 재생을 적용한다. 검수 원장은
접수일부터 1년을 보관하고 작성자 확인·개인정보 검수·수정·철회 대응에만 쓴다. 공개
동의와 개인정보 수집 동의를 분리해 버전·시각을 보존한다. 운영자가 실제 이용 여부,
개인정보와 광고 표현을 확인한 뒤에만 공개 원장으로 승격하며 개인정보 가림과 명백한
오탈자 외에는 좋은 표현을 임의로 덧붙이지 않는다.

ERP 후기관리는 이전 공개 원장과 신규 검수 대기를 함께 보여준다. 상세 열람은 직원 감사
원장을 남기고, 신규 제출의 암호화 원문·확인 전화와 연결된 리걸프렌즈 고객의 전체 이름·
전화·사건 종류/상태/법원/번호/담당자 정보를 표시한다. 공개 제한은 개인정보, 이용 사실
미확인, 악성·조작 의심, 고객 철회, 중복, 기타 중 사유를 필수로 남긴다. 공개 후기에는 직원
누구나 하나의 공식 답글을 작성·수정할 수 있고 홈페이지는 현재 공식 답글을 함께 읽는다.

개별 후기 요청 문자는 직원 본인의 전용 템플릿과 기존 센트릭스 문자 원장을 사용한다.
템플릿은 `{{고객명}}`, `{{담당자명}}`, `{{사건번호}}`, `{{후기작성링크}}`만 허용하며 전용
링크를 필수로 한다. 각 직원에게 상담 후·개시절차 진행 중·면책결정 이후·그 밖의 시점의
자동화 키가 고정된 기본 템플릿 네 개를 보장한다. 기본 문구는 개인별로 수정할 수 있지만
슬롯은 이름 변경·삭제하지 않는다. 개인이 만든 추가 템플릿은 이름·문구·기본 작성 시점을
자유롭게 관리하고 발송 이력이 있어도 ERP에서 삭제할 수 있다. 삭제한 템플릿은 활성 목록과
새 발송 선택에서 제외하지만 과거 요청 원장의 참조는 소프트 삭제로 보존하며, 같은 이름의 새
템플릿을 다시 만들 수 있다.
ERP 후기 요청 화면은 고정 높이 카드 목록 대신 기본 템플릿과 개인 추가 템플릿을 구분한
선택 드롭다운을 사용한다. 선택한 템플릿은 왼쪽의 실제 문자 형태 미리보기와 오른쪽 편집기에
동시에 채우고, 긴 본문도 잘리지 않은 전체 내용과 변수 치환 예시·SMS/LMS 예상 구분으로
확인한다. 기본 템플릿은 명시적인 배지를 표시하고 이름·기본 시점·삭제 동작은 잠그되 본문은
수정·저장할 수 있다. 개인 템플릿은 이름·시점·본문 수정과 삭제를 제공하고, 새 템플릿은
별도 `새 템플릿 만들기` 동작으로 빈 편집 상태를 연다. 저장하지 않은 내용이 있으면 템플릿
전환과 페이지 이탈 전에 확인하고, 서버에 저장된 본문과 문자 미리보기가 달라진 상태에서는
후기 요청 발송을 막는다. 좁은 화면에서는 미리보기와 편집기를 한 열로 쌓는다.
한 번에 최대 30개 고객 사건을 선택하고 선택 목록은 고객명·전화·사건명·진행 상태를 계속
표시하며, 발송 응답 유실 시 같은 멱등키로 재시도한다. 링크 토큰은 요청 UUID와 서버 HMAC을
묶고 90일·1회 사용으로 제한한다. 토큰은 URL fragment로 전달하고 홈페이지가 same-origin
POST로 검증하므로 서버 URL 로그에 남기지 않는다. 발송 당시 사건 유형과 템플릿 시점을 요청
원장에 snapshot으로 남겨 고객 작성 화면의 사건 분야와 작성 시점을 기본 선택한다. 유효한
전용 링크에서는 gateway가 연결된 고객 사건의 이름을 가린 공개 이름과 휴대전화를 사용하므로
고객이 이름·전화번호를 다시 입력하지 않는다. 링크가 없는 신규 후기는 전화번호 완전일치
결과가 정확히 한 사건일 때만 자동 연결하고, 복수 후보·이전 후기는 직원이 직접 선택한다.
이름 기반 유사 매칭은 하지 않는다. 연결 당시 리걸프렌즈 담당자 중 활성 ERP 계정이 있는
전원을 답글 의무자로 기록하며, 비식별 실시간 이벤트 뒤 담당자에게만 브라우저 알림을 보낸다.
답글 또는 공개 제한이 끝나면 지속 배지의 의무 건수에서 빠진다.

기프티콘은 같은 후기 상세에서 고객·사건·담당자 확인, 승인 상품 선택, 발송 사유, 고객 안내,
긍정 후기의 대가가 아니라는 최종 확인까지 디자인을 적용했다. 기프티쇼 비즈 상용 Key와
개인정보 처리위탁·상품/발송/결과 계약이 승인되기 전에는 외부 API·과금·실제 발송을 전혀
수행하지 않는다. 승인 후에도 API 인증값은 gateway 서버 비밀로만 두고 한 건 단위 발송,
중복 방지, 결과·취소·감사 원장을 추가한 뒤 활성화한다.

### 2-10. 로앤 소개는 주장보다 확인 가능한 기록을 먼저 보여준다

`/about`은 연혁·수임 건수·수상 경력을 나열하는 소개서가 아니라 방문자가 법률대리인을
선택할 때 중요하게 보는 기준을 스스로 확인하는 대화형 페이지다. 방문 목적, 궁금한
업무 방식, 변호사 선택 기준, 로앤을 처음 알게 된 경로의 네 질문에 답하면 선택한
기준에 맞춰 로앤의 원칙을 한 문장으로 정리한다. 답은 브라우저 상태에서만 사용하고
서버·DB·분석 이벤트로 전송하거나 저장하지 않는다.

선택 직후 다음 질문으로 자동 이동하지 않는다. 선택한 답에 대한 로앤의 설명을 먼저
화면 중심에 보여주고, 사용자가 `답을 읽었어요` 버튼을 눌렀을 때만 다음 질문으로
이동한다. 답변은 추상적인 신뢰·친절 선언보다 실제 비교 질문, 확인할 자료, 자동화와
사람 판단의 경계, 공개 후기 숫자가 뜻하지 않는 범위를 구체적으로 말한다. 유리한
가능성만 강조하거나 결과를 장담하지 않고, 사용자가 상담·계약 전 스스로 확인할 한
가지 행동을 각 답변에 함께 제공한다.

신뢰 지표는 산출 근거를 설명할 수 있는 값만 사용한다. 현재 PostgreSQL 공개 후기 원장에서
공개 건수, 최초·최근 작성일, 작성 당시 단계와 경험 키워드를 동적으로 집계한다. 서울·
대전·부산 사무소 주소·전화·사업자번호, 대표변호사와 법인등록번호는 공식 공개정보와
일치시킨다. 기존 사이트의 큰 누적 자문·사건해결 수처럼 산출 기준과 갱신 이력을 현재
검증할 수 없는 숫자는 새 소개 페이지에 사용하지 않는다.

소개 페이지는 결론 전 사실 확인, 범위와 변수 설명, 사람의 판단 책임, 다음 단계까지
이어지는 기록을 네 가지 일하는 기준으로 공개한다. 자동화가 돕는 접수·분류·누락 확인과
사람이 책임지는 법률 판단·상담·제출·중요 안내를 나누어 공개 LLM 상담이나 자동 성공
판정을 제공하는 것처럼 보이지 않게 한다. 실제 인물·사무소 사진은 승인된 촬영 자산이
준비되기 전까지 가짜 재연·스톡 이미지로 대신하지 않는다.

공개 설명·근거·연락처와 대화형 질문은 검색과 초기 표시를 위해 서버 렌더링을 유지한다.
SSR되는 Client Component의 최초 렌더에는 현재 시간·난수·브라우저 저장소·화면 크기·
locale 의존값을 직접 사용하지 않고 서버와 같은 초깃값을 먼저 렌더한 뒤 effect에서
갱신한다. iOS Chrome을 포함한 WebKit 브라우저가 서버 HTML의 전화번호·날짜·이메일·
주소를 hydration 전에 링크로 바꾸지 않도록 루트 metadata의 `formatDetection`에서
네 항목을 모두 끈다. 페이지 전체 `suppressHydrationWarning`, console 오류 필터,
브라우저 내부 속성을 지우는 DOM 변경 스크립트로 실제 애플리케이션 불일치를 숨기지
않는다. 자체 렌더를 결정적으로 만들 수 없는 입력 UI에 한해서만 client-only 경계를
사용한다.

iPhone Chrome은 이 설정과 별개로 React가 실행되기 전에 문서 루트 `<html>`에
`__gcrremoteframetoken` 속성을 삽입한다. 서버에는 없는 이 외부 속성이 실제 오류 diff에서
확인됐으므로, 공식 React/Next.js escape hatch인 `suppressHydrationWarning`을 정확한
`<html>` 요소에만 적용한다. 이 옵션은 한 단계만 작동하므로 `body` 이하의 날짜·난수·
브라우저 분기·태그 중첩 등 실제 hydration 오류는 계속 드러난다. 외부 속성을 임의로
삭제하거나 console 오류를 필터링하지 않는다.

### 2-11. 홈페이지 카카오 CTA는 이름·거주지역과 선택 전화 입력 후 접수·채팅 확인으로 확정한다

카카오톡 1:1 채팅은 홈페이지 전화 상담 폼의 세 번째 모드가 아니라 독립 접점으로
유지한다. 홈페이지의 카카오 CTA는 외부 채팅 URL을 바로 열지 않고 client-only 모달에서
`이름 또는 카카오톡 표시명`과 광역 거주지역을 모두 필수로 받는다. 휴대전화 번호는
`선택 · 입력하지 않으셔도 돼요`라고 명확히 표시하고 비워 둘 수 있으며, 입력한 경우에만 `010`
휴대전화 형식을 검증한다. 고객이 확인할 때 브라우저가 same-origin
`POST /api/kakao-entry`를 새 탭으로 제출하고, 홈페이지 서버가 gateway의
`POST /v1/kakao/homepage-entries`에 이름·거주지역과 선택 전화번호가 포함된 접수를 저장한 뒤
`https://pf.kakao.com/_AeGxoxl/chat`으로 303 이동시킨다. gateway가 일시적으로
실패하더라도 고객의 채팅 진입을 막지는 않는 fail-open 이동을 사용하고, 실패 로그에는
개인정보를 넣지 않는다. 빈 문자열·공백뿐인 이름, 선택하지 않은 거주지역 또는 입력한 전화번호의
형식 오류는 확인 버튼과 홈페이지 POST 양쪽에서 거부하고 카카오 채팅방도 열지 않는다. 필수 두 필드와
선택 전화번호를 유효하게 제출한 뒤 gateway가 일시적으로 실패한
경우에만 기존 fail-open 이동을 유지한다. SSR 첫 렌더에는 모달 input·button을 두지 않고
mount 뒤 portal로 열어 hydration 결정성을 지킨다.

일반 채널 링크는 고객이 채팅방을 연 사실만으로 상담원 화면에 대화를 만들지 못한다. 따라서
제출 버튼 바로 앞의 닫을 수 없는 노란 안내에서 `채팅방이 열리기만 하면 접수되지 않는다`,
`홈페이지에 입력한 이름은 {입력 이름}입니다. 상담 요청합니다.`라고 메시지를 한 번 보내야
한다는 사실을 설명한다. 입력 중인 이름을 예시 문장에 실시간으로 반영해 실제 채팅방 이름이
다르더라도 직원이 ERP 접수와 대조할 수 있게 한다. 제출 버튼은
`카카오톡 열고 상담 메시지 보내기`로 표시하고 버튼 아래에도 메시지를 꼭 보내 달라는 문장을
반복한다. 별도 필수 확인 체크박스는 추가하지 않는다. 이 안내는 향후 카카오 채널에 검증된
원탭 상담원 연결 기능을 붙이기 전까지 첫 메시지 누락을 줄이는 현재 운영 기준이다.

일반 카카오 채널 링크는 채팅방 진입 성공, 고객의 카카오 식별자, 실제 채널 표시명이나
첫 메시지를 서버에 반환하지 않는다. 고객 입력 이름은 채팅방을 찾는 검색 단서이지
카카오가 보증한 식별자가 아니다. 따라서 확인 제출은 실제 채팅 완료가 아니라
`pending(이름·지역 입력 완료·메시지 확인 대기)` 접수다. 같은 브라우저 탭의 30분 이내 반복
제출은 같은 UUID 멱등키를 재사용해 상담·요청을 중복 생성하지 않고 `click_count`와 최근 클릭 시각만
갱신한다. 직접 채널 검색·친구 목록 등 홈페이지 CTA를 거치지 않은 극소수 상담은
자동 식별할 수 없으므로 기존처럼 직원이 수동 등록한다.

선택 입력한 전화번호는 정규화해 요청 암호문과 상담 HMAC 지문으로 보존하고 ERP에 실제 번호를 표시한다.
미입력 접수만 DB 전화번호를 `NULL`로 두고 ERP에서 `010-0000-0000 · 미수집`으로 표시한다.
고객 입력 이름·거주지역과 `표시명_<접수번호 8자리>_플친` ERP 이름도 AES-256-GCM intake에 보존하며,
개인정보 처리 근거는 실제 메시지 수신과 구분한 `customer_initiated_channel_entry`다.
ERP 목록은 복호화한 입력 이름과 `카톡 이름 입력` 배지를 즉시 표시한다. 직원이 채널
관리자에서 같은 이름의 메시지를 찾고 `상담하기`를 누르면 entry를
`confirmed(채팅 확인)`로 바꾸는 일과 본인 담당 배정을 같은 트랜잭션에서 처리한다.
오입력이나 실제 표시명 차이는 상세에서 감사로그와 함께 고친다. 배포 전 생성된 이름 없는
기존 pending만 표시명을 먼저 확정하기 전 배정을 계속 차단한다. 메시지가 없는 진입은
배정 전에 직원이 상세 우측 상단의 다른 상담과 같은 `무효 처리` 액션으로
`invalid(미진입·무효)`로 바꾸고, 무효 담당자 `lawandfirm_s999`·`member_idx=1824`로
리걸프렌즈 신건 등록한 뒤 상담 상태를 종결한다. 표시명 수정 영역 안에는 무효 버튼을
중복 배치하지 않는다. 배정 뒤에는 같은 우측 상단 버튼이 리걸프렌즈 사건 담당자를 무효로
변경한다. 이때 상담 작업 큐는 저장 상태 `closed`를 일반 `종결`로 표시하지 않고
카카오 진입의 `invalid` 원장을 근거로 상태와 우측 담당 영역을 모두 `무효`로 표시한다.
이름·전화번호는 목록에서 블러 처리하되 `무효` 상태·접수번호·처리 배지는 읽을 수 있게
남겨 일반 종결 상담과 혼동하지 않게 한다.

카카오 이름 접수 트랜잭션은 선택 전화번호 유무와 관계없이 `consultation.requested`만 남기고
접수 알림톡을 만들지 않는다. 채팅 확인 후 직원이 배정하면
`consultation.kakao_chat.confirmed`, `consultation.assigned`,
`legalfriends.consultation.registration.requested`를 남긴다. 담당 배정 알림톡은 만들지
않는다. 리걸프렌즈에는 선택 입력한 실제 번호를 전달하고, 없을 때만 외부 신건 payload에
`010-0000-0000`을 합성해 고객이 선택한 지역과 함께 사용한다. 상담 DB와 이벤트 payload에는 가짜번호를
저장하지 않는다. 선택 번호가 리걸프렌즈 기존 연락처와 일치하면 홈페이지·직원 신규등록과 같은
`기존 사건 문의/기존 고객의 새 사건/공유 연락처` 게이트를 담당 배정 전에 적용한다.
배포 전 거주지역이 없던 기존 접수만 외부 등록 시 `미수집`으로 보완한다.
상담하기 직후 무효를 눌러 사건 연결이 아직 없어도 신건 등록을 먼저
완료하고 무효 담당자 변경을 재시도한다.

운영 채널에 연결했던 챗봇 v1.0~v1.2 실험은 고객 자유 메시지가 자동 읽음 처리되고
상담원 채팅 목록에 정상 노출되지 않는 것을 확인해 종료했다. 현재 `법무법인 로앤`
채널은 챗봇 연결을 해제하고 기존 채팅방 리스트 메뉴를 사용한다. 당시 스킬 계약과
코드는 비활성 이력으로만 보존하며 현행 접수 기준으로 사용하지 않는다. 현재 계약과
운영 절차는
[`docs/KAKAO_HOMEPAGE_ENTRY_V1.md`](docs/KAKAO_HOMEPAGE_ENTRY_V1.md)를 따른다.

### 2-12. 리걸프렌즈 성공사례 원천은 비공개 복제본에서 정제한다

리걸프렌즈 MySQL의 `CB.TblCBCase`는 성공사례와 향후 자가진단의 원천 후보로 사용한다.
2026-07-31 기준 35개 컬럼·9,598행을 로컬 MySQL의 별도 `CB` 스키마로 복제했고,
원본과 행 수·컬럼·인덱스·기본 필드·JSON·날짜·정규화 숫자 해시가 일치함을 확인했다.
2026-08-03에는 이 원천을 로컬 PostgreSQL `lawand_dev`의 별도 `CB.TblCBCase`로도
이관했다. 원본의 35개 컬럼, 기본키·고유키와 5개 검색 인덱스는 유지하되, 대상에 없는
`ACCOUNT.TblOffice`·`CONTENT.TblCase` 참조 외래키는 만들지 않았다. 원본 MySQL 복제본과
Git 밖 압축 백업은 검증·복구 기준으로 유지한다. PostgreSQL 원천 테이블은 `PUBLIC`과
홈페이지 앱 역할에는 권한을 주지 않는다. 로컬 단독 운영자의 DBeaver 계정인
`lawand_viewer`에는 2026-08-03 사용자 요청으로 `CB` 스키마의 현재·향후 객체 전체
권한을 부여하고 `lawand_dev`에서 강제 읽기 전용 설정을 해제했다. 이 예외는 로컬
개발 DB에만 적용하며 운영 환경의 원천 테이블 접근 정책으로 승격하지 않는다.

2026-08-05에는 운영 크론의 원천을 같은 운영 RDS에서 읽을 수 있도록 로컬에서 검증한
`CB.TblCBCase`, `CB.TblCaseMemo`, `CB.TblMoClientStatement`를 운영 RDS의 동일한
비공개 스키마로 이관했다. 이관 전 암호화 수동 스냅샷을 만들고, 단일 트랜잭션 복원 뒤
세 테이블의 행 수·키 범위·최종 수정시각과 행 순서 기반 논리 해시가 로컬과 일치함을
확인했다. 운영 권한은 별도 동기화·생성 역할을 추가하지 않고 기존 역할로 단순화한다.
`lawand_migrator`가 `CB` 생성·동기화와 공개 사례 초안 저장을 담당하고,
`lawand_viewer`는 기본 읽기 전용 상태로 `CB`를 조회한다. 공개 홈페이지·gateway의
`lawand_app`과 `PUBLIC`은 계속 `CB` 스키마에 접근하지 않는다.

이 테이블에는 의뢰인 이름·전화번호·사건번호와 채무·소득·변제 관련 정보가 함께 있어
그 자체는 공개 사례 저장소가 아니다. 홈페이지, 공개 API와 자가진단 런타임은 원본
MySQL이나 PostgreSQL `CB.TblCBCase`를 직접 조회하지 않는다. 2026-08-03 구현한
`self_diagnosis_case_profiles`는 동기화 때부터 직접 식별자와 원본 사건 ID를 SELECT하지
않고, `Office_idx=56` 제약과 새 무작위 UUID를 사용한다. 고객이 실제 진행 순서를 읽을
수 있도록 신청서 접수·금지·개시·인가·파산선고·면책허가 날짜는 읽기 모델에 포함한다.
원천 1,773건 중 실제 신청서 접수일과 필수 변제·결정일 기록을 갖춘 1,759건(개인회생
1,342·개인파산면책 417)을 로컬 런타임 모델로 구축했고, `lawand_app`은 이 모델만
SELECT할 수 있다.

같은 리걸프렌즈 RDS의 `CONTENT.TblCaseMemo`는 사건 메모 원천이다. 로컬 PostgreSQL
`lawand_dev`의 `CB.TblCaseMemo`에 `Case_idx` 유일키, `update_dt`, `memo` 3개 컬럼을
보존하며, 2026-08-04 11:44:44 스냅샷 기준 202,772행·메모 25,487,570자다.
`scripts/import-legalfriends-casememo.mjs`는 RDS EC2의 Git 제외 환경파일에서 접속정보를
읽고 원본을 직접 저장하지 않은 채 스트리밍한다. `--replace` 실행 시 임시 테이블에
먼저 적재·검증하고 행 수·키 범위·NULL 수·문자 수·두 부분 해시·최종 수정시각이 모두
맞을 때만 기존 `CB.TblCaseMemo`와 원자적으로 교체한다. 원천 메모는 사건 내용과
개인정보가 포함될 수 있어 `PUBLIC`·`lawand_app`에는 권한을 주지 않고 로컬
`lawand_viewer`만 확인한다. 홈페이지·자가진단 런타임은 이 테이블을 직접 조회하지
않는다. RDS가 계속 갱신되므로 문서의 행 수·시각은 마지막으로 검증한 스냅샷 기준이다.

같은 RDS의 `CONTENT.TblMoClientStatement`는 사건별 상담·주소·채무 상황 원천이다. 로컬
PostgreSQL `lawand_dev`의 비공개 `CB.TblMoClientStatement`에 18개 컬럼과 `idx` 기본키,
`Case_idx`·`phone` 인덱스를 보존했으며, 2026-08-04 12:10:24 스냅샷 기준 9,402행이다.
`scripts/import-legalfriends-client-statement.mjs`는 bigint 식별자를 문자열로 스트리밍해
정밀도 손실을 막고, JSON·텍스트·NULL·범위·최종 수정시각·두 부분 해시를 원본과 대상에서
대조한다. `--replace`는 새 임시 테이블을 먼저 검증한 뒤 기존 `CB.TblMoClientStatement`와
원자적으로 교체한다. 원천에는 전화번호·주소 상세·채무 사유·상담 희망이 포함될 수 있어
`PUBLIC`·`lawand_app`에는 권한을 주지 않고 로컬 `lawand_viewer`만 확인한다. 홈페이지와
gateway 런타임은 이 원천을 직접 조회하지 않는다.

2026-08-04에는 세 비공개 원천을 `Case_idx`로 조인해 공개 후보 한 건을 만드는 v1
파이프라인을 구현했다. `scripts/generate-public-case-study.mjs`는 `Office_idx=56` 사건 중
필수 절차 결과가 있는 후보를 고르고, 메모·진술 자료는 있을 때만 필요한 범주를 보탠다.
소득·채무·청산가치·소득형태·
거주형태·혼인·자녀·인정 가구원 조합이 같은 사건이 최소 5건인 경우에만 진행한다.
이름·전화·사건번호·주소·직장명·학교·금융기관·달력 날짜는 제거하고 월 금액은 10만원,
총액은 100만원 단위로 반올림한다. 절차에는 달력 날짜 대신 신청서 접수일부터 실제로 지난
일수만 표시한다. 직원 메모와 의뢰인 진술 원문은 식별자 제거만 거쳐
모델에 보내지 않고, 코드가 감지한 넓은 사실 범주만 안전한 편집 스냅샷에 넣는다.

Codex CLI에는 이 안전한 스냅샷만 표준입력으로 전달한다. 기본값은 `gpt-5.6-luna`·`xhigh`이고,
모델·추론 강도는 생성 명령에서 명시적으로 바꿔 비교할 수 있다. 첫 preview는 사용자 요청으로
`gpt-5.6-terra`·`medium`으로 다시 작성했고, 두 번째 preview는 `gpt-5.6-luna`·`medium`으로
추가생계비가 기록된 다른 개인회생 사건을 사용했다. 추가생계비가 있는 사례는 핵심 쟁점·
계산에서 별도 반영 항목과 자동 인정이 아닌 필요성·계속성·증빙 심사를 설명하도록 강제한다.
청산가치 설명에는 개인회생의 명목 총변제액과 현재가치 비교의 차이를 반드시 포함한다.
파산·면책 후보는 파산선고와 면책허가 기록이 모두 있는 사건만 사용하고, 재산·청산가치는
파산절차의 처분·배당 관점으로 설명한다. 세 번째 preview는 `gpt-5.6-luna`·`medium`으로
생성한 파산·면책 사례다. 결과는 신규 `public_case_studies`에 자가진단 연결용 내부 키 `source_case_idx`,
HMAC 지문과 스냅샷 해시, 익명화·프롬프트·모델 버전, 공개용 본문·재무 요약·절차·태그로
저장한다. `source_case_idx`는 홈페이지 응답에 선택하지 않는다. 생성기는
항상 `preview`, 개인정보·법률 검수 `pending`으로 기록하고 공개·철회된 행은 덮어쓰지
않는다. 새 slug로 실행하면 이미 사용한 사건을 제외해 다음 후보를 고르고, 같은 slug는
모델 호출 전에 거부한다. DB 제약은 최소 집단 5건과 태그 수를 강제하며, 공개 근거·개인정보 승인일·책임
변호사 법률 승인일·공개일이 모두 없으면 `published` 전환을 거부한다.

`/bank/cases`와 `/bank/cases/[slug]`는 승인된 사례를 보여주고, 개발 환경에서만 로컬
피드백용 `preview`를 함께 읽는다. 운영 모드에서는 preview를 SQL 단계에서 제외하고
상세 경로도 404로 처리한다. preview에는 `noindex, nofollow`를 표시하고 sitemap과
Article 구조화 데이터에 넣지 않는다. 개인회생 목록·상세는 월소득·월변제금·변제기간,
가용소득 배분·청산가치 현재가치·인가 절차를 중심으로 구성한다. 파산·면책 목록·상세는
월변제금과 변제기간을 제거하고 현재 지급능력·총채무·처분·배당 관점의 청산가치,
파산선고와 별도 면책심사를 중심으로 구성한다. 파산 사례의 실제 파산선고·면책허가
경과일과 제305조·제564조·제566조 및 대한민국 법원 파산·면책 안내를 분야별 공식
근거로 연결한다. 현재 초안 세 건은 모두 정식 공개가 아니다. `lawand_app`은 공개 사례
원장을 SELECT만 할 수 있고, 생성·검수·발행 변경은
마이그레이션 역할 또는 향후 분리할 전용 발행 역할만 수행한다. 운영 발행 전에 사건 재이용 근거와 철회 범위, 개인정보 검수, 책임 변호사의
청산가치 현재가치·가용소득·절차 문구 검토를 완료해야 한다. 상세 계약은
[`docs/PUBLIC_CASE_STUDIES_V1.md`](docs/PUBLIC_CASE_STUDIES_V1.md)를 따른다.

자가진단은 2026년 기준 중위소득 60%를 생계비 참고값으로 쓰고, 최소 총변제액을
`max(청산가치, 담보 없는 채무의 3%)`로 계산한다. 고객에게 부양가족 수를 별도로 묻지
않고 미성년 자녀 수를 최초 비교값으로 사용하며, 60개월 안에 제약을 맞추도록 인정 가정
인원을 한 명씩 줄인 비교 시나리오를 만든다. 1인 참고 생계비 이하이거나 0명
시나리오도 제약을 못 맞출 때만 파산·면책 사례로 분기한다. 유사한 회생사건 수가
부족하다는 이유만으로 파산 방향을 표시하지 않는다. 회생 후보는 우선권채권 여부를
정확히 맞추고 원천 변제율 3% 이상·1~60개월·청산가치 이상 변제 조건을 통과해야 한다.
그 뒤 미성년 자녀 수가 같은 사건을 우선 후보군으로 고정하고 월소득 정확 일치 또는
5%·10만원 이내 사건을 먼저 확보한다. 그 안에서 월소득에 가장 큰 연속형 가중치를 두고
법원·소득형태·혼인·원천 인정 부양가족·총채무·청산가치를 함께 계산하며 거주형태는
보조 가중치로 비교한다. `debt_reasons`는 원천 정의 오류 가능성 때문에 v1에서
제외한다. `living_cost_type`과 `living_cost_cost`는 고객 입력이나 가중치에는 사용하지
않고 선택된 원천 사건의 추가생계비 인정 내용을 설명하는 결과 필드로만 사용한다.
`estimated_spend`는 예상 지출, `total_payment`와 `repayment_rate`는 총변제금과 변제율로
표시하며 월소득 배분 막대와 함께 제공한다. 주요 결정일 옆에는 신청서 접수일부터의
날짜 차이를 함께 표시한다.

직접 식별자가 없더라도 법원·가족·채무·재산·예상 지출·추가생계비·변제 결과·원본
절차일의 다차원 조합에는
재식별 위험이 남는다. 따라서 이 읽기 모델과 다섯 개별 결과 카드는 아직 승인된 공개
사례 원장이 아니다. 운영 공개 전 과거 사건 이용 근거와 동의·철회 범위, 날짜를 포함한
최소 집단 크기와 희소 조합 일반화, 책임 변호사의 결과 문구 심사를 마쳐야 한다.

원천의 채무액·소득·변제금 일부는 MySQL `FLOAT`다. 큰 금액에서는 인접 표현 간격이
커질 수 있으므로 정제 원장에서는 금액을 원 단위 `BIGINT` 또는 업무상 필요한 소수
자릿수를 고정한 `DECIMAL`로 변환하고, 원천값·변환 규칙·검증 결과를 함께 보존한다.
공개 사례와 자가진단 문구는 책임 변호사의 법률·광고 검토 전에는 배포하지 않는다.

### 2-13. 홈페이지 법적 고지와 광고책임자는 전역 단일 원장으로 표시한다

홈페이지 운영 법인은 법무법인 로앤, 대표변호사와 광고책임변호사는 김충환 변호사,
변호사등록번호는 15977로 확정한다. 전역 푸터와 콘텐츠별 작성정보는
`apps/homepage/lib/legal-identity.ts`의 같은 식별정보를 사용해 페이지별 표기 불일치를
막는다. 광고책임변호사 지정과 개별 콘텐츠의 법률 검토 완료는 같은 의미가 아니므로,
검토 사실이 없는 콘텐츠를 `법률 검토 완료`로 표시하지 않고 `광고책임변호사`로 정확히
구분한다.

`/privacy`는 상담 요청, 홈페이지 카카오톡 진입, 네이버 예약 메일, 후기 접수·기존 공개
후기, 가명 방문 여정과 보안 로그의 목적·항목·보유기간을 공개한다. 홈페이지 상담·후기
원장은 기본 1년, 인프라 보안 로그는 최대 3개월, 공개 후기는 철회 또는 공개 목적 종료
시까지를 기준으로 한다. AWS 인프라, 주식회사 누리고(SOLAPI) 알림톡,
주식회사 리걸플로(리걸프렌즈) 사건 관리 연계를 처리위탁으로 공개하고, 별도 광고
플랫폼·생성형 AI에는 상담·후기 접수 개인정보를 제공하지 않는 현재 경계를 명시한다.
개인정보 보호책임자와 권리행사 창구는 김충환 대표변호사와 로앤 대표전화·전자우편으로
공개한다.

`/terms`는 공개 콘텐츠가 일반 정보이며 개별 법률의견이나 결과 보장이 아니라는 점,
온라인 접수·접수번호·담당 배정만으로 위임계약이 체결되지 않는 점, 후기 원본성과
공개·철회 기준, 외부 카카오·네이버 서비스의 책임 경계를 설명한다. 두 문서는 푸터와
sitemap에 연결하고 상담·후기 동의 상세에서도 전체 처리방침으로 바로 이동할 수 있게
한다.

### 2-14. 개인회생 비용 랜딩은 총액보다 항목·기준일·계약 범위를 공개한다

`/bank/guides/costs`를 `개인회생 비용`, `개인회생 변호사 비용`, `개인회생 법원비용`
검색 의도에 대응하는 canonical 랜딩으로 사용한다. 비용은 변호사 보수, 인지대, 송달료,
사건별 실비·예납금으로 분리하고 매달 채권자에게 내는 변제금과 명확히 구분한다.

법원 기본비용은 정적 숫자만 노출하지 않고 기준일과 공식 계산식을 함께 표시한다.
2026-07-31 기준 개인회생 개시신청의 기본 인지액은 30,000원, 전자문서 제출 인지액은
27,000원이다. 송달료는 `1회 기준금액 × (10 + 채권자 수 × 8)`이며, 2026-07-01부터
e-Post 적용 사건의 1회 기준금액 5,640원을 사용한다. 예시 합계는 전자 인지대와
송달료만 더하고 변호사 보수·외부회생위원 예납금·서류 실비가 제외됐음을 바로 붙인다.
실제 납부액은 접수 당시 법원 납부서와 명령을 우선한다.

로앤의 확정 보수표와 계약 조건이 없는 상태에서는 업계 평균이나 최저 시작가, 분납
가능 기간을 임의로 광고하지 않는다. 대신 부가가치세, 법원비용 분리, 채무증명서,
보정 대응, 개시·인가·면책 범위, 중도 해지·기각 정산, 분납 일정과 추가비용 사전 승인
여부를 견적서·위임계약서에서 확인하도록 한다. 실제 금액 공개는 김충환 광고책임변호사가
승인한 로앤 보수표와 계약 범위가 확정된 뒤 같은 페이지에 반영한다.

### 2-15. 구성원 소개는 사람의 수보다 자격·역할·확인 책임을 먼저 공개한다

`/people`은 김충환·허남관·우종현·박혜성 변호사의 이름, 직위, 서울·부산·대전 소속
사무소, 변호사등록번호, 법학전문대학원과 변호사시험 자격을 공개한다. 자격과 전문분야는
대한변호사협회 공개 프로필을 기준일과 함께 확인하고 각 공식 프로필로 연결한다. 협회에
등록된 전문분야만 검증 표시를 사용하며, 일반 취급 분야와 법원 위촉 경력은 별도 항목으로
구분한다. 개인 전화번호·개인 이메일·생년 정보는 공개하지 않는다.

회생·파산의 담당변호사는 김충환 대표변호사로 표시한다. 법률컨설팅팀은 최초 상담,
정보 정리, 가능한 진행 방향의 설명, 서류 발급과 부채증명서 발급 의뢰 지원을 맡는다.
기본 서류와 부채증명서가 일정 수준 모이면 사건관리팀이 배정되어 변호사의 지휘 아래
신청서 초안을 준비한다. 제출 전에는 법률컨설팅팀의 사실관계 확인, 담당변호사의 법률
검토, 의뢰인의 최종 확인을 거친다. 법원의 보정권고 이후에도 사건관리팀의 자료 안내와
초안 준비, 내부 논의와 변호사 검토를 거친다. 역할 분담이 비변호사의 독립적인 법률
판단이나 결과 보장으로 오인되지 않도록 이 책임 경계를 페이지 본문에 직접 설명한다.

실무진의 실명·사진 공개는 신뢰에 도움이 되지만 고정된 장식용 조직도로 운영하지 않는다.
현재 재직, 소속팀, 고객에게 담당하는 단계와 공개 범위를 확인한 구성원부터 성명·사진·
소속팀·담당 단계까지만 게시한다. 2026-08-03 공개 페이지는 ERP
`members`의 재직 상태와 NAS에서 제공받은 원본 사진의 존재를 대조한 정적 스냅샷이며,
개인 연락처는 게시하지 않고 공식 사무소 채널로 연결한다. 휴직·퇴사 사진은 공개하지
않으며, 부서 이동이나 퇴사 시 즉시 갱신할 수 있는 원장을 마련하기 전까지는 페이지
배포 전 수동 대조를 거친다.

제공받은 사무실 합성·연출 이미지는 로앤의 실제 시설이나 규모를 입증하는 자료로 쓰지
않는다. 잘못 생성된 표기가 없는 회의·응대 공간 부분만 잘라 보조 시각자료로 사용하고,
화면에 `공간 연출 이미지`를 직접 표시한다. 실제 방문 장소의 단일 진실원천은
`/about#offices`의 서울·대전·부산 주소와 공식 연락처다.

### 2-16. ERP의 내부 제품명은 `LAW& OS`로 통일한다

직원이 사용하는 ERP의 전면 제품명은 `LAW& OS`, 영문 설명은 정확히
`AI Operating System for LAW&`로 쓴다. 기존 `ERP` 용어는 저장소 앱 이름, 시스템 간
출처 구분, 업무 문맥처럼 기술적으로 필요한 곳에는 유지하되 브라우저 기본 제목, 헤더,
로그인·초대 화면의 주 브랜드에는 사용하지 않는다. 하위 페이지 문서 제목은
`{페이지명} | LAW& OS` 템플릿을 따른다.

브랜드 심볼은 연결을 뜻하는 앰퍼샌드와 운영체제의 코어·궤도를 결합한 기하학 형태로,
법률 서비스의 저울·망치·법원 기둥을 쓰지 않는다. 짙은 그래파이트 타일과 민트·오프화이트
심볼을 라이트·다크 테마에서 동일하게 사용하고, 같은 원본에서 512px 앱 아이콘, 180px
Apple 아이콘, 16/32/48px 파비콘과 브라우저 알림 아이콘을 파생한다. 워드마크 문자열은
HTML 텍스트로 렌더해 작은 화면과 보조기기에서도 제품명을 정확히 유지한다.

---

## 3. 인프라·데이터 보관 결정

### 3-1. 초기 배포 — 앱별 EC2 분리, 관리형 데이터 계층

홈페이지·ERP·gateway는 처음부터 **각각 별도 EC2**로 배포한다. 같은 AWS 계정과 VPC 안에
두되, 앱별 배포·장애 격리·증설 단위를 독립시킨다. RDS PostgreSQL과 배포 S3는 앱과
분리된 관리형 서비스로 사용한다. ElastiCache Redis는 실제 독립 큐·실시간 부하가 생기기
전에는 만들지 않고 현재 PostgreSQL outbox를 유지한다.

```
인터넷 / Route 53
  ├─ lawandfirm.com          → homepage EC2
  ├─ erp.[플랫폼 도메인]    → erp EC2
  └─ api.[플랫폼 도메인]    → gateway EC2
                              ├─ RDS PostgreSQL
                              ├─ ElastiCache Redis
                              └─ 사무실 NAS (Site-to-Site VPN)
```

정식 홈페이지 도메인은 기존 검색 자산이 있는 `lawandfirm.com`으로 확정한다.
`lawand.co.kr`은 초기 인프라 예시일 뿐 본 사이트의 canonical 도메인으로 사용하지 않는다.

초기 권장 인스턴스는 homepage `t4g.small`, ERP `t4g.small` 이상, gateway `t4g.medium`
이상이다. 각 앱은 독립 Docker 이미지·CI/CD 배포 단위를 유지하며, EC2 디스크나 다른 앱의
로컬 포트/파일시스템에 의존하지 않는다.

Linux 앱의 운영 배포 단일 경로는 `main` 검증 → GitHub Actions/Buildx `linux/arm64` 빌드 →
앱별 private ECR → EC2의 immutable digest pull이다. GitHub는 장기 AWS 키 대신 immutable
owner/repository 숫자 ID가 포함된 정확한 `main` subject로 제한한 OIDC 역할을 사용하고, ECR은
tag 변경을 막고 push scan과 최근 10개 lifecycle을 적용한다. EC2 systemd는
`repository@sha256:digest`를 직접 실행하므로 commit tag 재지정이 운영 바이너리를 바꾸지
못한다. migration이 있는 릴리스는 gateway digest로 migration을 먼저 적용한 뒤 gateway와
영향 앱을 같은 릴리스 ID로 전환한다. 기존 S3 소스 전송·EC2 네이티브 빌드는 CI/ECR 장애의
명시적 긴급 fallback일 뿐이다. 모든 배포는 내부·외부 health 성공 뒤 현재+rollback 2개
이미지만 남기고 BuildKit cache 4 GiB 상한, 오래된 source release 정리, 정리 전후 가용 용량과
이미지 ID 기록을 수행한다.

2026-08-13 첫 불변 이미지 운영 릴리스로 이 기준을 실제 전환했다. 완료 worktree 7개와 원격
`origin/worktree/*` 38개를 모두 `main`에 포함한 commit `133262a`를 GitHub Actions에서 검증하고
앱별 ARM64 OCI image로 게시했으며, EC2 세 대는 ECR digest를 직접 실행한다. ECR은 tag immutable,
push scan, 최근 10개 lifecycle이고 GitHub OIDC는 immutable owner/repository 숫자 ID가 포함된
정확한 `main` subject만 신뢰한다. migration `0057`은 같은 gateway digest와 암호화 snapshot으로
운영 반영했다. health 뒤 current+rollback 2개와 release directory 2개만 남겼고 BuildKit의
record 단위 soft limit가 4 GiB를 넘는 경우 남은 회수 가능 cache를 비워 hard cap을 검증한다.
첫 정리에서 세 서버 합계 73,839,296,512 bytes를 회수하고 최종 cache를 1.381~1.429GB로 낮췄다.

2026-08-04 이 토폴로지를 서울 리전 `lawand-prod` 스택으로 실제 구성했다. 세 EC2는
각각 EIP와 암호화 gp3를 사용하고 SSH 대신 SSM으로 관리한다. RDS는 두 private DB
subnet 안의 단일 AZ `db.t4g.xlarge`를 사용하며 public access를 차단했다. 최초에는
`db.t4g.small`로 시작했으나 2026-08-13 실제 동시 조회의 CPU 포화 뒤 상향했다. 80/443만
edge에 공개하고 gateway 3022와 PostgreSQL 5432는 보안 그룹 간 통신만 허용한다.
배포 아티팩트 버킷은 public access를 전부 차단하고 버전 관리·TLS 강제·30일 만료를
적용했으며 스택·EC2·RDS에 삭제·종료 방지를 설정했다. 정식 DNS 전까지 EIP HTTP를
`sslip.io` 임시 HTTPS로 301 전환해 검증하며, 상세 기준은
[`docs/PRODUCTION_DEPLOYMENT_V1.md`](docs/PRODUCTION_DEPLOYMENT_V1.md)를 따른다.
운영 EC2의 ARM64 AMI는 검토된 현재 이미지 ID를 CloudFormation에 명시적으로 고정한다.
AWS의 최신 AMI 별칭 갱신이 경보 같은 무관한 스택 변경에서 세 서버 교체로 번지지 않게 하고,
AMI 교체는 별도 변경 세트에서 교체 영향과 복구 계획을 확인한 릴리스로만 수행한다.

### 3-2. 녹취 보관 — NAS 원본 + 클라우드 재해 복구 사본

사무실 NAS를 녹취 원본의 주 저장소로 사용한다. gateway는 녹취 완료 파일을 암호화된
임시 EBS 영역에 두고, WireGuard 기반 Site-to-Site VPN을 통해 NAS로 **비동기 전송**한다.
전송 실패 시 재시도하며, 전송 성공 전 원본을 삭제하지 않는다. NAS의 SMB/NFS를 인터넷에
공개하지 않는다.

DB에는 녹취 파일 자체가 아니라 상담/사건 ID, 저장 경로, SHA-256 해시, 시각·길이·접근
이력을 기록한다. NAS RAID만으로는 백업을 대신하지 않으므로 NAS 스냅샷과 S3 Glacier 계열의
암호화된 재해 복구 사본을 운영한다. 원본은 불변으로 취급하고, 편집본·STT 결과·요약은
별도 객체와 레코드로 관리한다.

### 3-3. 로컬 개발 DB — PostgreSQL 역할 분리

WSL의 PostgreSQL 16 `127.0.0.1:5432`에 `lawand_dev`를 지속 개발 DB로 사용한다.
`lawand_migrator`는 DB 소유와 마이그레이션, `lawand_app`은 gateway가 필요한 public
테이블 CRUD를 담당한다. `lawand_viewer`는 public 테이블의 DBeaver SELECT를 기본으로
하되, 로컬 단독 운영 편의를 위해 `lawand_dev`의 강제 읽기 전용 설정을 해제하고
`CB` 스키마에 한해 현재·향후 객체 전체 권한을 가진다. 운영 DB는 별도 역할을 더 늘리지
않고 `lawand_migrator`가 CB 관리, 기본 읽기 전용인 `lawand_viewer`가 CB 조회를 맡는다.
`lawand_app`은 public 런타임 테이블만 사용하고 CB에는 접근하지 않는다.

로컬 비밀번호와 연결 문자열은 Git에서 제외된 권한 600의 `.env.development.local`에
보관한다. 로컬 역할·비밀번호를 운영 RDS에 재사용하지 않으며, 운영에서는 Secrets
Manager와 별도 역할·보안그룹·TLS 기준을 적용한다.

## 4. 두 가지 실무 메모

- **기존 `ai-agents-mono`(운영콘솔)와는 별도 repo로.** 그건 내부 인프라 모니터링이고
  이건 비즈니스 플랫폼이라 수명주기가 다르다. 대신 **GitHub 비공개 origin은 첫 커밋부터**
  붙일 것 — 과거 루트 repo 통합하며 겪은 히스토리 폐기 같은 수고를 반복하지 않도록.
- **네트워크 전제를 초기에 못 박아야 한다.** 공개 도메인·HTTPS와 VPC/서브넷·보안 그룹,
  사무실 NAS와의 Site-to-Site VPN을 스캐폴딩 뒤 인프라 구축 전에 확정한다.

---

## 5. 스캐폴딩 전 확정할 오픈 이슈

- [x] 배포 토폴로지: homepage·ERP·gateway 앱별 EC2 분리, RDS·배포 S3 관리형 분리,
  ElastiCache는 실제 큐 부하 전까지 보류
- [x] GitHub Actions OIDC → ARM64 앱별 immutable ECR → EC2 digest pull 배포 기준선,
  health 후 rollback 2개·BuildKit 4 GiB·source release 정리와 용량/이미지 원장
- [x] GitHub origin 연결: `https://github.com/attorneychkim-ctrl/lawand.git`
- [x] 개인회생·파산 홈페이지 제품·UX·통합 설계 v1 작성
- [x] VPC/2AZ 서브넷·보안 그룹·EC2 3대·EIP 3개·비공개 RDS·배포 S3·임시 HTTPS 준비
- [x] `lawandfirm.com`·ERP/API Route 53 전환, 정식 HTTPS 발급과 DNS record rollback 준비
- [ ] 사무실 NAS ↔ AWS Site-to-Site VPN(WireGuard)·암호화·백업·보존 정책 설계
- [x] 정식 홈페이지 도메인: `lawandfirm.com` 유지
- [ ] 출시 전 기존 핵심 검색 유입·백링크 URL의 유지/단일 301 보호 목록
- [x] 기존 개인회생·파산 고객후기 전량 추출·PII 점검·이관 검증
- [x] 신규 고객후기 공개 작성 → 암호화 검수 대기 원장 수직 흐름
- [x] 검증 가능한 후기·법인·사무소 정보 기반 대화형 로앤 소개 페이지
- [x] `/privacy` 개인정보처리방침·`/terms` 이용약관과 푸터·상담/후기 동의 연결
- [x] GA4·AdPilot 측정 계약 v1: 동의 기반 정제 페이지뷰·웹 상담 생성 이벤트·
  운영 귀속/GA 관측치 분리·맞춤형 광고 비활성화·계정 연결 후 검증 게이트
- [x] GA4 홈페이지 구현 후보와 AdPilot 범용 결손: 런타임 ID·basic consent·전역 정제
  페이지뷰·직접 상담/자가진단 `generate_lead`·설정/철회·개인정보처리방침 초안 →
  상세 목표 선택/역할 분리·범용 연결 문구·키워드 충돌 미귀속 진단
- [ ] GA4 운영 속성·웹 스트림 생성과 관리 설정·개인정보 최종 검토 → 운영 ID 설정·
  통합 배포 → 동의/철회·DebugView·실제 쿠키·전환 검증 → AdPilot 읽기 전용 운영 스트림·
  네이버 광고 연결·랜딩/키워드 기여 검증
- [x] 대표변호사·광고책임변호사 김충환(변호사등록번호 15977) 전역·콘텐츠 표시
- [x] 개인회생 비용 랜딩: 법원비용 현재 계산식·변호사 보수 분리·계약 확인표
- [x] 변호사·구성원 소개: 대한변협 자격 검증·지역별 변호사·실제 사건 처리 책임 구조·재직 실무진 원본 사진
- [ ] 실무진 공개 원장: 현재 재직·소속팀·담당 단계·사진 사용 동의와 즉시 갱신 절차
- [x] ERP 후기관리: 원문 복호화 열람 감사·고객/사건/담당자 연결·사유 필수 공개 제한·
  누구나 공식 답글·담당자 알림/지속 배지·개인 템플릿 다중 후기 요청 문자
- [x] 기프티쇼 비즈 상용 Key 승인 상품 3종의 상품 상세 재검증·단건 MMS 발송·응답 유실
  취소와 중복·암호화·감사 원장 구현(운영 migration·배포 전 후보)
- [x] 리걸프렌즈 `CB.TblCBCase` 성공사례 원천의 로컬 비공개 복제·무결성 검증
- [x] 리걸프렌즈 `CONTENT.TblCaseMemo` 원천의 로컬 `CB.TblCaseMemo` 복제·무결성 검증
- [x] 리걸프렌즈 `CONTENT.TblMoClientStatement` 원천의 로컬 `CB.TblMoClientStatement` 복제·무결성 검증
- [x] 운영 RDS 비공개 `CB` 3개 원천 이관·논리 해시 검증·migrator/viewer 권한 적용
- [x] 수신전화 리걸프렌즈 차선 원천: `Office_idx=56`의 `CB.TblCSClient`·`CB.TblCase`·
  `CB.TblMember` 최소필드 로컬·운영 동기화, 행별 논리 해시·권한·암호화 스냅샷 검증
- [x] ERP 고객 찾기: `CB.TblCSClient.Case_idx = CB.TblCase.idx` 조인·삭제 사건 제외를
  강제한 고객명/전화번호 검색과 식별자 재검증 기반 센트릭스 클릭투콜·통합 후처리
- [x] ERP 고객 찾기 문자: 고객·사건 식별자 재검증 → 암호화 대상 스냅샷 → 개인 템플릿·
  SMS/LMS·JPG MMS 발송, 개인정보 없는 outbox와 기존 단일 문자 워커 재사용
- [x] ERP 고객 찾기 신건·소개 상담: 수정 가능한 이름·휴대전화·광역 거주지·사건 유형 →
  소개자/기존 고객과 기존 사건·담당 snapshot 분리 암호화 → 상담 상세의 리걸프렌즈 확인 문맥
- [x] 자가진단 런타임 읽기 모델: `Office_idx=56`·직접 식별자/원본 사건 ID 제외·금액 정규화·변제계획·주요 절차일
- [x] 성공사례 로컬 preview 정제 원장: 내부 `source_case_idx` 연결키·HMAC 원천 지문·비식별 스냅샷·최소 집단 5건·검수/철회 상태
- [x] 내부 사건 3개 원천 조인 → 코드 선비식별화 → Luna xhigh 사례 초안 생성
- [x] `/bank/cases` 목록·상세와 개발 preview 전용 표시·noindex·운영 조회 차단
- [ ] ERP 개인정보 검수·책임 변호사 승인 → 공개·철회 운영 화면과 감사로그
- [x] `/bank/self-diagnosis` 입력 → 유사사건 5건 → 암호화 ERP 상담 접수 수직 구현
- [x] 자가진단 결과 카드 5건의 비식별 스냅샷을 상담 intake에 저장하고 ERP 고객 상세에 표시
- [x] 자가진단 10만원 미만 금액의 원·만원 단위 확인·명시적 변환·서버 재검증
- [ ] 자가진단 과거 사건 이용 근거·희소 조합 일반화·공개 결과 문구의 책임 변호사 심사
- [ ] 상황 정리/계산 도구·AI·광고 카피의 김충환 책임변호사 출시 심사
- [ ] 공개 사례 동의·익명화·승인·철회 정책 확정
- [ ] 상담 연락 SLA의 예외·공휴일·수용량, 외부 채널·사무소·비용 설명 범위 확정
- [x] pnpm 11.17.0 Corepack 준비와 workspace 의존성 설치
- [x] `packages/core` 상담 이벤트 계약 v1 + 중복 판정 + 최소 상담 상태 초안
- [x] 새 PostgreSQL 상담·귀속·outbox 스키마와 기존 시스템 분리 경계
- [x] 홈페이지 POST → gateway 암호화·중복·DB/outbox → ERP 목록·상세 수직 흐름
- [x] ERP 초대 전용 가입·로그인·서버 세션·기본 역할·PII 조회 감사 v1
- [x] ERP 상담 데스크·상세 프로덕션 UX: 작업 큐·검색·필터·다크/라이트·모바일 전화/배정
- [x] ERP 상담 작업 큐 신규등록: 일반 신규·기존고객·소개건 모달 → 고객찾기 선택 원천 재검증·
  7일 중복 묶음·리걸프렌즈 처리 게이트·최초 접수 알림톡 → 관리자 소프트삭제와 개인정보 블러
- [x] ERP 상담데스크·고객찾기·전화 후처리 신건의 사내 고객명 구분 자동화: 선택 중 입력칸에
  `_기존`·`_소개` 고정 표시 → 서버에서 기존 수동·반복 접미사 제거 후 선택값을 정확히 한 번 저장
- [x] 소개건 담당 문맥: 선택한 소개자의 암호화 사건 snapshot → 상담 목록·상세의 `소개자 담당`
  표시, 전화 후처리 소개건의 필수 소개자 검색·서버 재검증·동일 트랜잭션 원장 보존
- [x] 정규화 전화번호+최근 7일 상담 묶음: 채널·이름 무관 자동 연결 → 목록 대표 1건·상세
  전체 요청·이름 불일치 경고 → 전화 없는 접수번호 수동 연결·구성원 분리·감사 원장 →
  담당 배정·리걸프렌즈 신건·최초 알림톡 묶음당 1회
- [x] PostgreSQL outbox `LISTEN/NOTIFY` → gateway SSE → ERP same-origin 프록시의
  실시간 상담 목록 갱신과 재연결 동기화
- [x] gateway DB 연결 풀 안정화: 요청/LISTEN 분리·풀 대기 CloudWatch 지표/경보·bridge 유휴
  DB 폴링 억제·ERP 동시 조회 single-flight·전화 고객/명령 일괄 조회·안전한 VALUES 후보 SQL
- [x] 전화 실시간 E2E 지연 계측 후보: 전화 SSE의 단기 비식별 delivery ID → 다중 탭 leader의
  snapshot·전화데스크 paint/Notification ACK → 원천·gateway·브라우저 구간별 CloudWatch
  원시 표본과 1분 p50·p95·max 구조화 로그, ACK의 DB 세션 재조회 fan-out 차단
- [x] 상담 최초 등록 전 직원 알림: 전역 `consultation.requested` SSE → 보이는 페이지
  단일 공유 스트림 → 인증 후 고객명·지역·채널·배정 가능 여부 조회 → 20초 핵심 카드·브라우저
  Notification·브랜드 아이콘/채널 배지·단일 상담하기·배정 이벤트 즉시 닫힘·다중 탭 중복
  방지. 표시 성공 뒤에만 브라우저별 완료 키를 남기고 실패는 최근 2분 outbox snapshot에서
  재시도한다. 보이는 탭은 항상 처리하며 백그라운드 대표는 `BroadcastChannel`의 visibility
  상태로 선출한다. SSE·서비스 워커·표시 결과는 고객 정보 없는 진단 로그로 남긴다.
  탭 종료 전달용 Web Push 구독·서버 발송은 후속 범위
- [x] 센트릭스 A타입 클릭투콜 로컬 출시 후보: ERP 인증 직원 명령 → 개인정보 없는 outbox →
  직렬 gateway 워커 → 상태·실패 원장, 불명확 결과 자동 재시도 금지
- [x] 센트릭스 `userinfo` 실제 회선·내선 일치 검증을 강제하는 직원 주 회선 연결 도구
- [x] 요청 회선의 올바른 센트릭스 API 로그인 확보 → `userinfo` 일치 검증 → 로컬 직원
  주 회선 연결
- [ ] 운영 secret·RDS 회선 연결 → 본인 소유 번호 실제 clickdial canary → 워커 활성화
- [x] 센트릭스 발신 `callhistory` 사후 대사 → 연결·시간 자동 기록 → ERP 종료 결과 자동
  팝업 → 담당자 7분류 확정·감사 원장 → 부재·재상담 필요 목록 작업 큐
- [x] 고객 문자 로컬 출시 후보: 센트릭스 SMS/LMS, SOLAPI JPG MMS, 담당자 개인 템플릿·
  변수 치환·휴대전화 미리보기·상담별 암호화 발송 원장
- [x] 운영 migration `0041`·gateway·ERP 통합 배포 → 통제 센트릭스 SMS 실제 발송 canary
- [x] RID를 제거한 0588 대표 계정 통제 SMS → 단말에 `070-4607-0588` 표시 → 단말 미연결
  상태의 회신이 같은 mailbox로 유입 → 정확한 수동 발송 원장·최신 발송 직원 알림 연결 canary
- [x] 운영 migration `0042`·gateway·ERP 통합 배포 → 기본 템플릿/활성화 제거, 개인 템플릿
  실삭제·과거 발송 snapshot 보존, ERP 내 정보·본인 업무 연결 운영 활성화
- [x] 운영 migration `0043`·gateway·ERP 통합 배포 → 고객 찾기 대상 재검증·암호화 snapshot·
  기존 단일 문자 worker 기반 SMS/LMS/MMS 흐름
- [x] 대표 문자 수신함 로컬 출시 후보: 직원 회선과 분리된 대표 endpoint 7개 metadata,
  `getrecvsmslist` 직렬 polling·암호화 수신 원장·최신 발신 Case_idx 매칭, `/messages`
  수·발신 통합 화면과 개인 템플릿 모달
- [x] `/messages` 대화형 작성 로컬 출시 후보: 대화·말풍선 각 50건 cursor 무한 로딩,
  최신 말풍선 기본 정렬·과거 열람 중 스크롤 보존, 선택 대화 하단 직접 입력/JPG 작성창,
  ERP·리걸프렌즈·직접 번호 새 메시지 모달, `0059` 암호화 직접 대상 원장·비식별 outbox
- [x] 운영 migration `0058..0060`·homepage/gateway/ERP 통합 배포 → 기존 Case_idx/상담 대화 보존,
  직접 대상 권한·암호화, 후기관리·답글·요청 경계와 인증 화면/API 운영 smoke
- [ ] `/messages` 통제 SMS/JPG MMS·대표 수신 회신 연결·50건 cursor canary
- [x] 운영 migration `0044`·gateway/ERP 통합 배포 → `/messages`와 대표 mailbox 7개 비활성
  상태·권한·기존 대화 운영 검증
- [x] 대표 계정 7개 TTY `userinfo` 검증 연결
- [x] 활성·인증 대표 endpoint 7개를 직원 binding 없이 전화 callback·24시간 수신이력
  관찰 대상으로 포함하되 개인 회선의 직원 binding 조건과 문자/Windows bridge 분리 유지
- [x] U+ 비표준 `SRC` 격리 gateway 수정 운영 반영 → 대표 mailbox 7개 오류 0·최근 동기화
- [x] 0588 공용 SMS/LMS 발신 설정·발신 당시 회신 mailbox snapshot·mailbox별 수신 매칭
  migration `0069` 및 gateway 후보 구현
- [ ] `0069` main 통합·운영 반영 뒤 상담 또는 Case_idx 대상 통제 회신의 ERP 대화·담당자
  알림 canary
- [x] SOLAPI 등록 MMS 발신번호를 운영 Secrets Manager·gateway에 적용
- [ ] 200KB 이하 명함 JPG MMS 실제 발송·단말 수신 canary
- [x] 임시 Windows Server 2022 x64 + 32비트 OpenAPI OCX 수신 canary:
  `RINGEVENT → Answer() → CHANNELLIST → CHANNELOUT`, 물리 전화기 스피커폰 양방향 통화
- [x] x86 STA ActiveX host 기반 Windows bridge 1단계: Windows 자격 증명 관리자 →
  회선 검증 로그인 → 상시 프로세스·재접속 → 실제 `RING_EVENT`·`CHANNEL_OUT` 수신
- [x] bridge→gateway 2단계: DPAPI 내구 큐 → HTTPS/HMAC 인증 → 발신번호 즉시 암호화·
  지문화 → provider/event/nonce 멱등 수신 통화·이벤트 원장, 운영 배포
- [x] 실제 신규 수신 end-to-end 원장 canary: 약 12.9초 무응답 통화의
  `inbound.ringing → inbound.ended(HCAUSE=16)`, DPAPI 재전송·암호화/지문 무결성 확인
- [x] ERP 전역 수신전화 표시: DB commit `NOTIFY` → gateway 인증 SSE → ERP same-origin
  프록시·재연결 스냅샷 → 전체 번호·내선·`내 전화/담당자 전화` 통화별 상단 카드, 운영 배포
- [x] 실제 새 수신전화로 ERP `수신전화 → 통화 종료` 표시와 bridge v0.2.1 자연 종료 확인:
  약 13.6초 무응답, `ringing → ended(HCAUSE=16)`, 프로세스 1·큐 0
- [x] 상담데스크 우선 고객 해석 → 리걸프렌즈 동기화 원천 차선 조회 → 수신 카드 고객·사건·담당자 표시
- [x] 크론 전용 EC2에서 `Office_idx=56` 전화 디렉터리의 일일 consistent snapshot·원자 교체·경보 활성화:
  매일 03:30 Asia/Seoul systemd timer, 실패 canary로 기존 데이터 보존 확인, 접근 경계 canary 통과,
  실패 통지 실발송 검증(CloudWatch 로그·metric·경보는 크론 EC2 역할에 IAM 정책 부착 후 활성화)
- [x] ERP 명시적 받기 → 활성 ring의 Windows bridge `Answer()` → 물리 전화기 스피커폰
  양방향 통화 → 연결 channel ID 추적 종료까지 운영 canary
- [x] 직원관리 프로덕션 UX와 센트릭스 전체 회선·내선·비밀번호 검증: U+ `userinfo`
  정확 일치 → endpoint·암호화 인증값·주 회선 자동 생성/배정, 기존 secret fallback 유지
- [x] 단일 Windows 서버의 센트릭스 다중 인스턴스 풀: 기존 회선 1개 + 논리 슬롯 50개,
  배정 회선 전부 + 유휴 warm 5개만 실행, 신규 직원 원클릭 자동 점유·실패 자동 반환,
  1208 실제 OCX 로그인과 ERP connected 상태 운영 canary
- [x] 수신·ERP 클릭투콜·센트릭스 직접 발신 통합 전화데스크 운영 배포: 원본 원장 분리
  보존 → 회선·번호 지문·최소 시각차 1:1 연결 → 중복 없는 목록·검색·필터·SSE
- [x] 비즈콜·망 수신 종료 원장 운영 배포: 15초 `getinboundcall` 보정 → ERP
  `U+ 앱/망 수신` 표시와 전화데스크 기록
- [x] 비즈콜 앱 실시간 ring 추가 개발 보류: 공개 REST 미노출을 확인했고, 사용자 결정으로
  앱 직접 발신·앱 수신의 종료 후 전화데스크 기록까지만 현재 제품 범위로 확정
- [x] 전화데스크 상세·열 가지 통화 결과·암호화 메모·기존 상담 연결/신건상담 생성·30분 단위 담당자 재통화 큐
  - [x] 후처리 모달 상단 고정, 기존 상담 등록일·담당자와 리걸프렌즈 사건 확장 정보,
    홈페이지형 날짜·30분 시간 카드, 저장 뒤 목록 이동과 상세 목록 버튼 복구
  - [x] 신규 후처리의 `상담완료` 기본값, 후처리 문자 작성, 재통화 큐 고객명·전화번호와
    안전한 상담/리걸프렌즈 대상 문자·센트릭스 즉시 발신
  - [x] 통화 `connected`부터 전 직원 후처리 화면·메모/다음 일정 저장, 종료 뒤 결과 수정,
    담당자와 무관한 상담 문자·발신 및 카카오 선택 전화번호/묶음 구성원 재검증
  - [x] 연결된 외부 수·발신의 실제 회선 소유자/참여자에게만 현재 화면 위 후처리 모달 1회
    자동 표시, 강제 상세 이동·다중 탭 중복 방지, 닫은 뒤 통화 카드에서 수동 재열기
  - [x] 고객 종료 근거·2분 무신호를 만족한 미확정 외부 root의 활성 직원 수동 확정,
    root-only 상세, 상태바 시작/마지막 신호·확인 동선, 대표 공용 회선 표기
- [x] 전화 고객 해석에 활성 직원 센트릭스 전체 회선 추가: 상담 → 직원 회선 → 리걸프렌즈
  우선순위, 직원명·내선 표시, 비활성/미연결 제외
- [x] 외부 수신 브라우저 알림 발신번호 상시 표시와 직원 회선 요약 분기 복구, 직원 프로필·활성
  endpoint 양쪽 번호 일치 해석
- [x] 암호화 공용 전화번호부 출시 후보: 원번호·연결번호 동시 지문 해석, 후처리 저장·수정,
  `/phonebook` 검색·등록·수정·소프트삭제, migration `0061`
- [x] 운영 migration `0061..0063`과 홈페이지·ERP·gateway 통합 배포: 암호화 snapshot,
  migration 원장 64개, 직원 43명 기본 후기 슬롯 172개, 대상 리걸프렌즈 실패 1건의 보존형
  2차 성공과 새 dead 0 확인
- [x] Windows 다중 bridge 운영 안정화: 감독기·비정상 종료 재시작, SYSTEM health metric·
  CloudWatch 5종 경보, DPAPI dead-letter, 직원관리 4단계 상태, 관리자 유휴 슬롯 재배정
- [x] bridge 재접속 전 활성 수·발신 종료 보정 + gateway endpoint lock 기반 회선당 활성
  통화 단일화 + 내부 4자리 leg 고아 이벤트 차단을 v0.7.1로 운영 배포
- [x] 통합 통화 활동 v2 제품 정책 확정: 외부 수·발신 전 직원 카드, 내선 참여자 알림,
  `Member_idx` 기반 수신 담당자 라우팅, 고객 통화 root/호전환 leg/최종 통화자 후처리
- [x] bridge v0.7.2 비식별 관측 로그 통제 배포와 시나리오별 상관 가능 범위 확정
  - [x] 4591·1208 일반 내선과 통화 후 호전환 양쪽 endpoint 실제 이벤트 canary
  - [x] 무조건 호전환·실패 복귀 canary와 암호화 DLQ 3건 archive·active queue 정상화
  - [x] 활성 외부 root 문맥의 A/B consultation attempt와 미연결 복귀 correlation 구현
  - [ ] 통화 후 호전환 B/고객 final leg의 추가 provider 증거 확보·결정적 상관 구현
- [x] 통합 통화 활동 구현: root/leg 원장·외부 수발신 공용 snapshot/card·정확한 알림 대상·
  토스트/Notification API·최종 고객 통화자 1회 후처리
- [x] 통합 통화 활동 migration `0045`·gateway·ERP·bridge v0.8.0 운영 통합 배포
- [x] v0.8.2 운영 통합 배포: v2 수신 필수 회선 직렬화·이벤트별 영구 오류 큐 비차단·
  종료된 U+ 이력과 지연 bridge ring 엄격 상관·늦은 `전화 받기` 차단·0048 중복 원장 복구,
  암호화 dead-letter 32건 SHA archive와 queue/dead-letter 분리 경보
- [x] v0.8.3·0049 운영 통합 배포: U+ sentinel provider ID 폐기·유일한 인접 sequence 종료 상관·
  v1→v2 terminal state 동기화·전체 v2 재접속/3분 ring 보정·snapshot 3분 안전망·08:19 정확
  종료 복구 fixture와 운영 읽기 dry-run
- [ ] v0.8.0 일반 내선·무조건/통화 후 호전환·실패 복귀 운영 acceptance 재수행
- [x] 실제 업무 통화를 포함한 10→25→50 프로세스 CPU·메모리 canary와 warm 5개 원복
- [ ] bridge 조직용 Authenticode 인증서 발급·서명 배포
- [x] Windows 재부팅 canary: 평문 자동 로그온 없이 SYSTEM 경보 확인 → 관리자 RDP 로그인 →
  배정 6개·warm 5개 복구 확인
- [x] ERP 내 정보: 이름·이메일 고정, 소속·지역·부서·직책 본인/관리자 수정,
  본인 센트릭스·리걸프렌즈 연결, 현재 비밀번호 확인 기반 변경·전체 세션 폐기
- [ ] ERP 인증 운영 보강: 분실 계정 이메일 재설정·계정 비활성화 UI·MFA/SSO·외부 rate limit
- [x] ERP `상담하기` 명령: 본인 담당 배정 → 업무 이벤트·리걸프렌즈·알림톡 실행 요청 outbox
- [x] 외부 실행 워커 기반: 선점 임대·개별 재시도·시도 원장·ERP 실패 상태 표시
- [x] 리걸프렌즈 워커: 직원 로그인 ID·`member_idx` 관리 →
  `createForLawnV2` 최초 담당자 포함 신건 등록 → 사건 ID 보존 → 추후 담당자 변경
- [x] ERP 상담 상세 무효 처리: 등록 사건 연결 재검증 → 개인정보 없는 outbox →
  `changeManager(case_idx, lawandfirm_s999)` → 담당자/관리자 권한·중복 방지·감사 원장
- [x] ERP 상담 담당자 변경: 현재 담당자/관리자 → 활성 리걸프렌즈 직원·사유 선택 →
  `changeManager` 성공 뒤 ERP 배정 원자 확정·실패 시 기존 담당 유지·실시간 새 담당 알림
- [ ] 상담 담당자 변경 고객 알림톡: 승인 템플릿·발송 대상과 시점 확정 뒤 별도 이벤트 연결
- [x] ERP 상담 전화번호의 고객찾기 완전일치 조회 → 자기 상담 생성 사건 제외 뒤 과거
  사건이 남을 때만 목록·상세 `기존고객`, 신건 성공은 별도 `리걸프렌즈 등록 완료` 배지
- [x] 리걸프렌즈 로컬 개발 워커 사용자 승인 활성화
- [ ] 리걸프렌즈 운영 안전성 확인: 외부 멱등성 계약·응답 유실 건 운영 절차
- [x] Solapi 알림톡 워커: 승인 채널·상담 접수/담당 배정 템플릿·API 자격증명 연결,
  문자 대체발송 비활성, ERP 실행 원장 표시
- [x] 로컬·운영 gateway의 리걸프렌즈·알림톡·네이버 IMAP 워커 시작과 빈 대기열·health 확인
- [x] 운영 gateway EIP `3.36.255.226` 확정과 운영 전용 Secrets Manager 경계 분리
- [ ] Solapi 키의 `0.0.0.0/0` 허용을 운영 gateway EIP 하나로 제한
- [ ] Solapi 최종 발송결과 웹훅 또는 조회 소비자와 실패 운영 알림 연결
- [x] 공개 상담 POST의 IP 비저장형 rate limit·gateway 직접 호출 차단·과다 접수 경고 로그
- [ ] 과다 접수 구조화 로그의 CloudWatch 경보 연결·필요 시 단계형 CAPTCHA
- [ ] OpenAI Realtime STT API 연동 PoC (gateway)
- [x] 상담 접수·담당 배정은 카카오 알림톡, 실패 시 문자 대체발송 없음으로 확정
- [x] 홈페이지 카카오 CTA → 공백 불허 필수 이름/표시명·광역 거주지역과 부담 없는 선택 전화번호 모달 → 선택 번호 암호화 ERP 접수 → 1:1 채팅 303 이동
- [x] ERP 입력 이름 즉시 표시 → `상담하기`의 채팅 확인·본인 배정 원자 처리 → 우측 상단 미진입 무효
- [x] 카카오 상담하기 → 선택 실제번호 또는 미입력 가상번호·고객 선택 지역으로 로그인 직원 리걸프렌즈 신건 등록(기존 무지역 접수만 `미수집`),
  배정 전 무효는 무효 담당자 신건 등록·배정 후 무효는 담당자 변경, 알림톡 차단
- [x] 챗봇 자동 읽음·상담원 미노출 위험 확인 후 운영 연결 해제·기존 채팅방 메뉴 복구
- [x] 네이버 예약 확정 메일 → IMAP 폴링 → 예약번호 멱등 ERP 무전화 상담 접수
- [x] 네이버 IMAP 애플리케이션 비밀번호 인증·기존 메일 기준점·로컬 워커 활성화
- [x] 네이버 브라우저 자동화 PoC 철회·관련 스키마 역적용·IMAP 전용 구조 복구
- [ ] 기준점 이후 실제 신규 네이버 예약 확정 메일 → ERP IMAP 접수 canary

---

## 6. 다음 단계 후보

GA4·AdPilot 코드 후보는 [`docs/GA4_MEASUREMENT_V1.md`](docs/GA4_MEASUREMENT_V1.md)의
계정 독립 작업까지 완료했다. 다음 순서는 실제 Google 계약 주체·처리 위치·국외 처리와
14개월 보유 문구를 책임 변호사·개인정보 담당자가 확정하고, 로앤 소유 운영 속성·웹 스트림을
`Asia/Seoul`·`KRW`로 만든 뒤 향상된 측정 전체를 끄는 것이다.
그 다음 메인 통합 세션이 런타임 ID를 운영 secret에 설정해 홈페이지를 배포하고 동의 전·
허용·철회·DebugView를 검증한다. 마지막으로 AdPilot의 읽기 전용 GA4와 네이버 광고 계정을
연결해 활성 랜딩 전수, 실제 `nkw-...` 형식, 충돌·미귀속과 관측 누락률을 확인한다. 이 결과가
안정되기 전에는 `generate_lead`를 primary나 자동 광고 피드백 입력으로 승격하지 않는다.

1. `/bank`와 개인회생 신청자격 랜딩페이지의 실제 콘텐츠·운영정보 검수
2. 개인정보처리방침의 운영 위탁계약·자동 파기 일치 점검과 ERP 인증 운영 보강
3. 법률컨설팅팀·사건관리팀의 공개 대상 실무진 명단·현재 역할·사진 사용 동의 확정
4. 기프티쇼 비즈 상용 Key·처리위탁 승인 뒤 현재 잠긴 ERP 화면에 상품 조회·단건 발송·
   결과 조회·재전송/취소·잔액과 감사 원장 연결
5. 자가진단 읽기 모델의 과거 사건 이용 근거·희소 조합 일반화·책임 변호사 출시 심사
6. 출시 전 핵심 기존 URL만 검색 성과를 확인해 유지/단일 301
7. 활성화된 리걸프렌즈 워커의 외부 멱등성 계약과 응답 유실 건 운영 절차 확정
8. SOLAPI 명함 JPG MMS 1건 canary 뒤, Solapi 최종 발송결과 자동 수신과 전체 상담
   상태머신 확정
9. 구현된 0588 공용 센트릭스 SMS/LMS 발신·회신 mailbox snapshot·mailbox별 최신 발신 매칭
   후보를 main에 통합하고 migration `0069`와 gateway를 같은 릴리스로 운영 반영한 뒤,
   상담/Case_idx 통합 대화 canary를 수행한다. ERP 대화를 고객번호 하나로 합칠지는 동일 번호
   공유 고객과 복수 사건 표시 정책을 먼저 확정한다. 대표 문자 수신함의 U+ 비표준 `SRC` 격리는
   배포 완료 상태를 유지하고, 대표전화 근무/휴무·담당자 조건 라우팅은 별도 설계한다.
10. 홈페이지 카카오 버튼 → 이름/표시명·광역 거주지역과 선택 전화번호 입력 → ERP 즉시 표시 → 실제 채팅을 찾아
    `상담하기`로 확인·배정하는 운영자 canary
11. 실제 수신·ERP 발신·직접 발신 한 건씩 통화 종료 후 공용 후처리 자동 열림과 담당자·
    `상담완료` 기본값, 후처리 문자와 재통화 큐 문자·센트릭스 즉시 발신 UX 확인
12. 센트릭스 조직용 코드 서명 인증서 배포; 실제 배정이 50개에 가까워지기 전
    t3.medium의 메모리 여유를 재확인하고 t3.large 상향 검토
13. 배포된 bridge v0.8.2에서 외부 수신 순서를 통제 관측해 지연 수신 비차단과 새
    queue/dead-letter 분리 경보를 재확인한다. 이어 일반 내선·무조건/통화 후 호전환·실패
    복귀를 다시 canary한다. 통화 후 호전환의
    B/customer final leg는 추가 provider 증거가 없으면 계속 `호전환 확인 필요`로 남기고,
    공용 카드·담당자 알림·최종 통화자 후처리와 녹취 매핑용 provider 식별자를 대조한다.
14. 비즈콜 앱 직접 발신 1건이 종료 후 전화데스크에 들어오는지만 운영 acceptance로 확인;
    실시간 모바일 ring·ERP 받기·원격 발신은 현재 범위에서 제외
15. 전화 담당자의 실시간 근무현황과 회선 통화 중 여부를 전화데스크·수신 카드에 결합
16. `lawandfirm.com` DNS 전파 관찰, Solapi IP 제한·승인된 운영 canary·경보 통지 연결
17. `0058`·gateway·ERP 통합 배포 뒤 실제 일반 내선과 고객 통화 후 호전환을 통제 관측해
    양쪽 leg 종료 동기화, 늦은 channel의 terminal 재개방 0, 한쪽짜리 3분 확인 필요 전환,
    실제 활성/호전환 보존과 상단 ghost 0을 재대조한다. 운영 connected root는 provider 종료
    증거 없이 추정 종료하지 않는다.
18. 기준점 이후 신규 네이버 예약 확정 메일 → IMAP 전용 ERP 접수 실제 canary
19. 호전환 미확정 수동 확정 후보를 gateway·ERP 같은 릴리스로 배포한 뒤 `***9684`의 실제
    최종 통화자를 업무 담당자가 선택해 root·모든 leg 종료, `staff_resolved` 감사 원장,
    상태바 제거와 후처리 저장까지 확인한다. 등록되지 않은 내선 9971만으로 직원을 추정하지 않는다.
20. 운영 고객찾기에서 소개자 1건과 기존 고객 본인 신건 1건을 통제 등록해 지역 기본값 수정·
    상세 문맥을 확인한다. 실제 `상담하기`와 리걸프렌즈 신건 생성은 별도 승인 canary로 분리한다.
21. 전화 실시간 E2E 지연 계측을 gateway·ERP 같은 릴리스로 운영 반영한 뒤 실제 수신·발신
    표본을 1~2시간 모아 event→gateway, gateway→browser, browser processing의 p50·p95·max를
    확인한다. IQ200 삭제 이후에도 원천→gateway 지연이 반복될 때만 bridge 계정 상태를 다시 본다.
22. `0059`를 암호화 RDS 스냅샷 뒤 적용하고 gateway·ERP를 같은 릴리스 ID로 전환한다.
    기존 상담/Case_idx 대화 보존과 50건 cursor 경계, 직접 번호 SMS·JPG MMS 한 건씩 및
    해당 번호의 대표 수신 회신이 같은 직접 대화로 이어지는지 통제 검증한다.
