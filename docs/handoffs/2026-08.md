# 2026-08 작업 인수인계 원장

> 2026-08-20 컨텍스트 문서 구조 개편 이후의 append-only 원장이다. 새 항목은 이 파일 맨
> 아래에 시간순으로 추가한다. 개편 전 전체 이력은
> [`../archive/context-pre-compact/AGENTS_LEGACY_THROUGH_2026-08-20.md`](../archive/context-pre-compact/AGENTS_LEGACY_THROUGH_2026-08-20.md)에
> 원문 그대로 보존돼 있다.

## 2026-08-20 — 컨텍스트 문서 무손실 분리·현행 기준선 축약 후보

- 기존 `AGENTS.md` 5,928줄과 `PROJECT_PLAN.md` v1.70 2,950줄을
  `docs/archive/context-pre-compact/`로 Git 이동했다. 원문 SHA-256은 각각
  `f016affdbc35157bb9ae7d5716f91aedfaed03c414a0830d53fc2ed64bde6488`·
  `6e1babda44fa56d9526fdd4636931e009d4fe52a9927f51d0e2fa02fda12e068`이며 이동 전후가
  일치한다.
- 공용 핵심 문서는 `AGENTS.md` 115줄, `PROJECT_PLAN.md` v2.0 369줄,
  `docs/handoffs/CURRENT.md` 60줄로 재구성했다. 세 문서 합계는 8,878줄·858,733 bytes에서
  544줄·37,631 bytes로 줄어 줄 수 약 93.9%, byte 수 약 95.6%를 절감했다. Claude 안내판까지
  포함한 자동 검사 합계도 39,126 bytes다. 과거 운영 원문은 삭제하지 않았다.
- `PROJECT_PLAN.md`는 실제 앱·패키지 구조, 최신 운영 릴리스·migration `0071`, 현행
  아키텍처와 살아 있는 승인/acceptance만 남긴다. 완료 체크리스트와 후보·배포 연대기는
  다시 누적하지 않는다. GA4 분야 문서의 `무팝업 hotfix 배포 전` 상태도 실제 배포·24시간
  수신·`generate_lead` 주요 이벤트 확인 상태로 교정했다.
- 새 인수인계는 `CURRENT.md`의 가변 현재 상태와 `docs/handoffs/YYYY-MM.md`의 오래된 순
  append-only 원장으로 분리했다. `CLAUDE.md`와 리걸프렌즈 크론 인수인계도 같은 읽기·기록
  순서로 맞췄다.
- `pnpm docs:context:check`는 공용 핵심 세 문서와 Claude 안내판의 줄/byte 합계, 필수 참조,
  금지된 연대기 패턴, 로컬 링크와 두 archive 해시를 검사한다. 별도 GitHub Actions workflow와
  production image 검증 단계에도 연결했다.
- `pnpm docs:context:check`, 전체 5패키지 lint·typecheck·test, package JSON·workflow shape,
  로컬 링크·archive hash와 `git diff --check`를 통과했다. 애플리케이션 코드·DB schema·
  migration·운영 데이터·외부 서비스·main·운영 배포는 변경하지 않았다. `HERDR_ENV`가 비어
  있고 HERDR 서버도 실행 중이 아니어서 Git worktree·원격 상태만 읽기 전용으로 대조했다.

## 2026-08-20 — 컨텍스트 문서 main·CI 단독 반영 완료

- HERDR 서버가 없어 Git worktree와 모든 `origin/LegalFlow/*`·`origin/worktree/*` HEAD를 다시
  대조했다. 미병합 원격은 `project_md_compact`·`clientname_sCRiPt`·`phone_alarm` 세 개였다.
  사용자의 “이것만” 지시에 따라 첫 브랜치만 병합하고 나머지 두 기능 브랜치는 명시적 제외했다.
  `web_hook`의 원격 HEAD는 이미 `main` ancestor이고 로컬 추적 변경 4건은 진행 중으로 보존했다.
- `LegalFlow/project_md_compact`를 `main` merge
  `38fc1dc6476ea7108ee5846e45d23618741a47fa`로 반영했다. 통합 상태에서 컨텍스트 budget·archive
  해시, 전체 5패키지 test·typecheck·lint·production build, DB schema check와
  `git diff --check`가 성공했다. 원격 Actions `32337680968`과 `32337680942`도 성공했다.
- workflow 경로 변경으로 같은 SHA의 ARM64 이미지를 ECR에 게시했다. parent digest는 홈페이지
  `sha256:7479ea3281d9473af3ecd7f3c3d9b3632b4e983322e3115965ccdc714dfd373b`, gateway
  `sha256:612be751033c093be527f3e960bb628c3567270970f264bda12cc5015de77074`, ERP
  `sha256:4ac67bc6bbe20eed11a12e4673d17053e4de9bb98e5450077cd7a6fb861422b9`다.
  애플리케이션 코드·DB·migration·운영 설정 변화가 없어 이 digest들은 EC2에 전환하지 않았다.
- 따라서 최신 운영 릴리스 `20260820T034100Z-three-worktrees-v1`, 운영 DB 72개 migration과 현재
  rollback 경계는 그대로다. snapshot·migration·서버 재기동·이미지/cache 정리·운영 데이터·
  외부 발송은 수행하지 않았다. 정식 홈페이지·ERP 로그인·gateway health는 각각 3회 연속
  200이었다. Actions의 Node 20 deprecation 안내는 비차단 경고이며 이번 실행 결과는 성공이다.

## 2026-08-20 — 기존 사건 문의 담당자 변경 버튼 미노출 운영 진단

- 전 직원 담당자 변경 허용 커밋 `6aff040635911f2d220ed0f0ca6e63cf8f96da87`은 8월 19일
  릴리스 `20260819T035353Z-orca-integrated-ops-v3`에 ERP·gateway로 운영 반영됐고, 현재
  릴리스 `20260820T034100Z-three-worktrees-v1`의 실행 중 ERP·gateway immutable digest도
  배포 문서와 일치함을 SSM에서 읽기 전용으로 재확인했다.
- 신고된 운영 상담 `LA-260820-LDPT4NBP`는 `assigned`, 미삭제, 현재 담당자와 변경 가능
  대체 직원 43명, pending 변경 0건으로 일반 표시 조건은 충족했다. 신고 직원 계정도 활성
  정규직·활성 주 멤버십 상태여서 직원 권한이나 브라우저 cache 문제가 아니다.
- 이 상담은 `existing_case`로 기존 사건 `case_idx=190463`을 선택해 신건 등록 이벤트를
  만들지 않았고 `consultation_legalfriends_handlings`·directory source만 존재한다.
  `legalfriends_case_links`가 없기 때문에 ERP의 `Boolean(consultation.legalFriendsCase)`에서
  변경 버튼이 숨겨지고, 버튼만 노출해도 gateway `requestAssigneeTransfer`가
  `case_not_registered`로 거부하는 구조임을 확인했다.
- 후속 수정은 검증된 기존 사건 `case_idx`를 담당자 변경 원장에 포함하고 ERP 표시 조건과
  gateway 명령을 함께 보강해야 한다. `changeManager` 외부 성공 뒤 ERP 담당자를 확정하는
  계약, pending 멱등성, 감사 원장은 유지한다. 이번 진단은 운영 DB·외부 리걸프렌즈·배포와
  애플리케이션 코드를 변경하지 않았다.

## 2026-08-20 — 후기 요청 템플릿 저장 실패 피드백 운영 진단

- 신고된 두 직원의 후기 요청 템플릿을 본문 원문 없이 운영 DB 메타데이터로 대조했다. 두
  템플릿 모두 당일 `2026-08-20 15:44:24`·`16:05:11 KST`에 실제 UPDATE가 성공했고 현재
  저장값은 링크 변수와 500바이트 제한을 만족한다. 뒤쪽 저장값은 센트릭스 기준 468/500
  바이트로 여유가 작다.
- gateway·ERP application journal, 같은 시간대 Caddy의 해당 경로 오류와 RDS PostgreSQL
  error log를 확인했으나 후기 요청 템플릿 관련 5xx·DB constraint·중복 오류는 없었다. 두 앱은
  active, container restart 0이고 CloudWatch ALARM도 0이다.
- 원인은 서버 저장 장애가 아니라 클라이언트의 무응답형 검증 UX로 좁혔다. 편집 본문이
  500바이트를 넘거나 `{{후기작성링크}}`를 지우거나 허용되지 않은 변수를 넣으면 저장 버튼을
  비활성화하고 `saveTemplate`이 fetch 전에 반환한다. 비활성 기본 버튼은 `cursor: wait`를 써서
  사용자는 저장 처리 중 또는 저장 실패로 오인할 수 있고, 서버 요청이 없어 운영 로그에도
  남지 않는다. 정확한 실패 초안과 검증 사유는 현재 보존하지 않으므로 두 신고 각각이 어느
  조건을 위반했는지는 사후 확정할 수 없다.
- 이번 세션은 진단만 수행했다. 애플리케이션 코드·DB·운영 데이터·배포·외부 발송은 변경하지
  않았다. `HERDR_ENV`가 비어 있고 HERDR 서버도 실행 중이 아니어서 관리 워크트리 목록은
  확인할 수 없었다.

## 2026-08-20 — Orca 운영 기준 교정·후기 템플릿 저장 지연 재진단

- 당시 세션에서 사용자가 작업 관리자를 Orca로 교정해 `orca-ide` 상태와 해당 워크트리를
  확인하고 그 브랜치의 문서·검사 기준을 바꿨다. 이후 현재 저장소의 authoritative 지침은
  HERDR 단일 기준으로 다시 확정됐으므로 이 세션별 교정은 역사로만 보존한다.
- 공서아의 정확한 피드백은 저장 버튼 비활성화가 아니라 “이전 발송 내용이 남은 채 로딩이
  계속됨”이었다. 운영 DB에는 공서아·김지안 템플릿 UPDATE가 각각 `15:44:24`·`16:05:11 KST`에
  최종 반영됐지만, 같은 구간 gateway request DB pool은 max 20을 모두 사용한 채 대기했다.
  분 단위 waiting maximum은 공서아 반영 분 3, 바로 다음 분 11, 김지안 직전 분 17, 반영 분
  1이었고 주변 구간 최고 97까지 반복됐다. 같은 구간 RDS CPU는 대체로 낮아 DB CPU 포화나
  constraint 오류 정황은 없다.
- 따라서 일차 원인은 순간적인 gateway request pool 적체와 저장 응답 지연·유실 뒤 브라우저가
  서버 값을 재조회하지 않는 관측 공백으로 좁혔다. 이 교정에서는 문서·검사 기준만 변경했고
  애플리케이션 코드·DB·운영 데이터·배포·외부 발송은 변경하지 않았다.

## 2026-08-20 — 후기 요청 비활성 버튼 가짜 로딩 커서 수정

- 사용자가 실제 상태를 재확인했다. 템플릿 저장은 정상 완료됐고 수정 초안이 없어
  `변경사항 저장` 버튼이 비활성인 상태였으며, 수신 고객을 선택하지 않은
  `0명에게 후기 요청` 버튼도 같은 대기 커서를 보였다. 두 버튼 모두 공통
  `.primary-button:disabled { cursor: wait; }`를 적용받은 것이 직접 원인이었다. 따라서 위
  항목의 gateway request pool 적체는 별도 운영 정황일 뿐 이 UI 신고와 인과가 확인되지 않았다.
- 템플릿 저장 버튼에는 `aria-busy={templateBusy}`, 후기 요청 버튼에는
  `aria-busy={sending}`을 추가했다. 후기 화면의 두 버튼은 비동기 작업 중이 아닌 비활성
  상태에서 `cursor: not-allowed`를 사용하고, 실제 저장·발송 중에는 기존 대기 커서를 유지한다.
  저장 API·발송 조건·데이터 흐름은 변경하지 않았다.
- ERP lint·typecheck·의존 패키지 포함 production build와 `git diff --check`를 통과했다.
  DB·migration·운영 데이터·외부 발송·`main`·운영 배포는 변경하지 않았다.

## 2026-08-20 — 상담완료 문자·후기 요청 독립 발송 후보

- 문자 템플릿 관리 모달의 자동발송 드롭다운과 JPG 파일 선택 버튼을 기존 입력 체계에 맞게
  스타일링하고, 자동발송 목록에서 `담당자배정` 바로 아래에 `상담완료`를 추가했다.
- 일반 문자와 후기 요청 문자의 공통 `{{고객명}}` 치환은 내부 고객명 원장을 수정하지 않고
  발송 시점에 유니코드 기준 앞 3글자만 사용한다. `홍길동_기존`·`홍길동_소개` fixture가 모두
  `홍길동`으로 렌더되는 core 회귀 테스트를 고정했다.
- 통화 후처리의 `상담완료` 결과에는 `상담완료 문자발송`과 `후기 요청 문자발송`을 독립 체크로
  표시한다. 각 대상·템플릿이 있으면 미발송 상태에서 둘 다 기본 체크하고, 성공·처리 중·결과
  불명 이력이 있으면 해당 체크만 기본 해제한다. gateway는 두 플래그를 같은 저장 요청에서
  함께 실행할 수 있으며 API 회귀 테스트로 계약을 고정했다.
- migration `0072_consultation_completed_message.sql`은 자동발송 CHECK 허용 목록에
  `consultation_completed`만 추가한다. 후보 SHA-256은
  `dca900abf3b0298920774d2548e82059663c411c1a99490838fc34a647982f0d`다.
- 전체 5패키지 test·typecheck·lint·production build, Drizzle schema check,
  `pnpm docs:context:check`, `git diff --check`를 통과했다. core 95개·gateway 194개 테스트가
  성공했다. `main` 병합·운영 migration·gateway/ERP 배포·외부 발송은 수행하지 않았다.
- 당시 진행 중이던 PC 알림 브랜치의 `0071`·`0072` 후보는 현재 `main`과 이 브랜치의 번호에
  겹치므로 메인 통합에서 최신 journal 위에 재생성해야 한다고 인계했다.

## 2026-08-20 — 완료 작업 6개 통합 병합 후보

- Orca 앱 `1.4.185`와 ready/reachable runtime에서 `main` 및 기능 워크트리 6개를 전수
  확인했다. 원격과 일치하는 `clientname_sCRiPt`·`phone_alarm`·`change_manager`·
  `review_save_error`·`sms_consult_complete`·`web_hook` HEAD를 `main`에 병합했고, 다른 모든
  `origin/LegalFlow/*`·`origin/worktree/*`를 포함해 각 HEAD가 통합 `main`의 ancestor임을
  확인했다. `change_manager`는 기존 사건 문의의 담당자 변경 누락 원인 진단만 포함하며 기능
  수정은 없다.
- 공개 intake의 markup·제어문자 고객명 격리, 재통화 업무와 호전환 확인 업무를 합친 전화
  배지, 후기 화면 비활성 버튼의 정확한 커서·busy 상태, 상담완료 문자와 후기 요청의 독립
  발송, 직원 개인 Windows PC 알림을 통합했다. 개인 알림은 5분 일회용 pairing, 개인별 9개
  설정, 암호화 payload·token hash·재생 방지·정리 원장을 사용한다. ERP 화면은 관리자 전용이고
  unsigned Windows ZIP은 운영 다운로드로 제공하지 않는다. 외부 URL 웹훅 후보는 폐기했다.
- migration은 `0072_consultation_completed_message.sql`과 최신 journal 위에서 다시 생성한
  `0073_desktop_notifications.sql` 두 개다. SHA-256은 각각
  `dca900abf3b0298920774d2548e82059663c411c1a99490838fc34a647982f0d`·
  `0448f39922e61c360cd0417a867c194be0812d6c60254da04b06ee47675b3c36`다. schema-only `CB`
  정의를 갖춘 빈 임시 PostgreSQL에 `0000..0073`을 두 번 적용해 74개·재실행 no-op을 확인했고,
  개인 알림 테이블 5개·상담완료 템플릿 제약·cleanup 함수·PUBLIC grant 0을 검증했다. 아무
  `CB` schema도 없는 완전 빈 DB의 첫 검증은 과거 migration이 참조하는 `CB` 함수 때문에
  중단됐으며 새 migration 실패가 아니다.
- 전체 5패키지 test·typecheck·lint·production build, Drizzle schema check,
  `pnpm docs:context:check`, `git diff --check`를 통과했다. core 102개·gateway 217개·홈페이지
  9개 테스트가 성공했다. 최초 ERP typecheck는 삭제된 과거 웹훅 route를 가리키는 `.next`
  생성물 때문에 실패했지만 production build가 현재 route를 재생성한 뒤 단독·전체 검증이
  모두 성공했다. 이 시점에는 원격 `main` 푸시·운영 snapshot·migration·digest 전환·운영
  데이터 변경·외부 전화/문자/알림 발송을 수행하지 않았다.

## 2026-08-20 — 완료 작업 6개 통합 운영 배포 완료

- 애플리케이션 소스 `34fc13d1e1a42126613749cfc295baea4b9885c3`를 main에 푸시했고 Actions
  `32354715262`의 전체 검증·세 ARM64 이미지 게시와 `32354715274`의 문서 검사가 성공했다.
  모든 Orca·원격 작업 HEAD는 최종 main ancestor다.
- 새 gateway digest 선행 pull과 암호화 snapshot
  `lawand-prod-pre-six-worktrees-20260820t094820z` available·100% 뒤 migration `0072`·`0073`을
  적용하고 재실행 no-op을 확인했다. 운영 migration은 74개이고 최신 해시
  `dca900abf3b0298920774d2548e82059663c411c1a99490838fc34a647982f0d`·
  `0448f39922e61c360cd0417a867c194be0812d6c60254da04b06ee47675b3c36`가 소스와 일치한다.
  PC 알림 테이블 5·FK 6·index 10, PUBLIC/viewer grant 0, 앱 최소 권한·cleanup 함수와 자동 생성
  행 0을 확인했다.
- 릴리스 `20260820T094820Z-six-worktrees-v1`로 gateway→ERP→홈페이지를 digest 전환했다.
  parent digest/image ID는 홈페이지
  `sha256:26c2969026cd618d44bbfb0d97462e0da8d98092155b10fb87c4f6993d1e7a16`·
  `sha256:a93a38cfbc437f77f59920561c83b8b5d2fe4f75defd02b10d7f9de967eec362`, ERP
  `sha256:527c57b3007e4e381a746961bd1fbd721e633e02bf4ae69d998a0d0f0ed70c74`·
  `sha256:b0343f7ed1accb411d78dbb23f0869f00b445a2c80ace4232a3d7ec1d67b94af`, gateway
  `sha256:c2ca002dca9462459323a0030418ff13552abbe66087bff779748fa5f1e8a99d`·
  `sha256:60bf3cccd010444ad68f7318512415943094fa15a5d35b9f426a991897dc072c`다. 세 scan은 기존과 같은
  CRITICAL 3·HIGH 11·MEDIUM 11·LOW 1이다.
- health 뒤 홈페이지·ERP·gateway cache는 1,421,000,000·1,429,000,000·1,381,000,000 bytes,
  회수량은 882,593,792·895,746,048·854,675,456 bytes다. 앱별 현재+rollback 2개와 source
  release 2개만 보존하고 서버 배포 로그에 기록했다.
- 정식·EIP 세 앱 endpoint는 각각 3회 연속 200, 앱·Caddy active, restart·error journal 0,
  env 600이다. request/LISTEN waiting은 네 표본 모두 `0/20`·`0/5`, CloudWatch는 OK 14·
  ALARM 0·INSUFFICIENT_DATA 0이다. 세 EC2 status/system ok·SSM Online, RDS available·암호화·
  삭제 방지다. outbox는 published 1,282·dead 29·pending 618·locked 0, 전화·문자·받기 명령과
  최근 통화·PC 알림 자동 생성 행은 0이다.
- Orca 운영 canary의 console·hydration·가로 overflow·제출 요청은 0이고 단계 전환 어텐션이
  정상이다. unsigned Windows client artifact는 배포하지 않았으며 실제 상담·전화·문자·PC
  알림·외부 웹훅 발송이나 기존 데이터 backfill을 만들지 않았다.

## 2026-08-21 — Windows PowerShell 5.1 설치 인코딩 hotfix main 반영

- Orca 앱 `1.4.185`와 ready/reachable runtime에서 `main`·완료 상태 `web_hook` 워크트리를
  확인하고 모든 `origin/LegalFlow/*`·`origin/worktree/*`를 다시 대조했다. 유일한 미병합 HEAD는
  clean·원격 일치 상태의 `457d04f64d4e7717a57a2a283050a8d60446470d`였고, 통합 뒤 미병합
  원격 수는 0이다.
- Windows PowerShell 5.1이 BOM 없는 UTF-8의 한글 시작 메뉴 바로가기 이름을 ANSI로 잘못
  해석하던 문제를 고쳤다. `install.ps1`·`uninstall.ps1`에 UTF-8 BOM을 유지하고 `build.ps1`이
  패키징 전에 두 BOM을 강제 검사한다. 브랜치에서 Windows PowerShell 5.1 parser, Debug·Release
  x64 compile·self-test와 통제 사용자 1인 설치를 통과했다. WSL 통합에서는 두 파일의 선두
  `EF BB BF`, UTF-8 유효성, `pnpm docs:context:check`와 `git diff --check`를 재확인했다.
- 과거 대형 `AGENTS.md`와의 병합 충돌은 현재 압축 문서 체계를 보존하고 이 원장에 이력을
  옮겨 해소했다. merge main은 `b95621a6506fad02f8d825e3462d221415782e27`이며 원격 main에
  푸시했다. Actions `32432686543`의 전체 5개 workspace task 검증과 세 ARM64 이미지 게시가
  성공했다.
- 경로 필터로 게시된 digest는 홈페이지
  `sha256:4f501ad60b2acaf55609c5b050cf91b2a2b27740627450b914d3d51c05d1fd34`, ERP
  `sha256:6737d31bb4c9745640aff498ab2c747f2f4497f7b1887262ee10b8ec8b636c6c`, gateway
  `sha256:6bdc4237277a5b81e3b299731996fddd816ff74fddd3fe03b768e61e5006204b`다. 운영 중인 Linux
  앱·migration에는 변화가 없어 세 이미지를 EC2에 적용하지 않았고 릴리스
  `20260820T094820Z-six-worktrees-v1`을 유지했다. snapshot·migration·SSM·서버 재기동·
  cache 정리·운영 데이터·외부 발송은 수행하지 않았다.
- 로컬 `lawand-prod` AWS 세션은 확인 시 만료돼 다음 AWS 작업 전에 재로그인이 필요하다.
  이번 main 검증·ECR 게시는 GitHub OIDC로 완료됐으며 사용자 AWS 세션을 사용하지 않았다.

## 2026-08-21 — Windows 개인 PC 자체 업무 카드 후보

- Windows `NotifyIcon.ShowBalloonTip` 표시를 없애고 화면 우측 상단의 LAW& OS 자체 카드로
  교체했다. 상담·문자·후기·고객전화·내선·호전환은 서로 다른 색·크기·배지와 실제 업무 필드를
  사용한다. 짧은 필드는 같은 폭의 항목 배지 오른쪽에 값을 정렬하고, 상담·문자·후기 원문은
  별도 본문 필드로 분리했다. 사용자가 실제 Windows 미리보기에서 전화 카드 정렬을 최종 기준으로
  승인했다.
- 일반 상담·문자·후기는 25~30초 뒤 자동 정리하고 마우스를 올리면 멈춘다. 실시간 고객전화·
  내선·호전환은 직접 닫거나 서버 만료 시각 기준 최대 2분 유지한다. 현재 커서가 있는 모니터에
  최대 3장만 배치하며 화면 높이 또는 장수 초과분은 종류별 건수 한 장으로 묶는다. 개별 카드의
  `displayed` ACK와 정확한 same-origin ERP 화면을 연 `opened` ACK는 분리했다.
- 공식 Windows `SessionSwitch` 잠금과 `GetLastInputInfo`의 10분 키보드·마우스 부재를 함께
  감지한다. 잠금·부재가 시작되면 고객정보 카드를 닫고 종류별 건수만 누적하며 복귀 시 요약 카드
  한 장을 유지한다. 커서 위치는 모니터 선택에만 쓰고 잠금 상태로 추정하지 않는다.
- gateway 생산자는 상담·문자·후기·외부전화·내선·호전환 payload를 `항목: 값` 구조로 확장해
  고객명·전화번호·실제 본문·채널·지역·회선·전화 상태를 암호화 payload 안에 포함한다. 테스트
  알림도 같은 구조를 사용한다. ERP 설정 화면의 알림 센터·잠금 제목 표현은 실제 자체 카드와
  부재 요약 동작에 맞췄다.
- 기존 브라우저 알림 생산·서비스 워커 표시·화면 토스트는 제거하지 않았다. ERP 내비게이션의
  `알림 켜짐/꺼짐/차단됨` 버튼과 별도 로컬 설정만 제거해, 사용자가 브라우저 사이트 알림 권한을
  허용하거나 차단한 결과만 표시 기준으로 사용한다. 브라우저 설정을 바꾸고 ERP 창으로 돌아오면
  현재 권한을 다시 읽는다.
- Windows Debug·Release x64 compile과 self-test가 성공했고 Release unsigned ZIP SHA-256은
  `2D34E0453FF55A0A7BBBB4A21467812E26694A6EF453660D4299C693F284943F`다. 개발용 미리보기로 여섯
  업무 종류와 부재중 요약을 실제 Windows에서 확인하고 버튼 클릭 시 창이 닫히는 경로를 검증했다.
  최종 4필드 테스트 카드도 두 열 요약·한 줄 알림 목적·독립 본문이 겹침 없이 표시됨을 확인했다.
  core 선행 build 뒤 gateway 217개 테스트, gateway·ERP lint·typecheck·production build와
  `git diff --check`를 통과했다. 첫 전체 gateway 실행은 오래된 core dist export 때문에 6개
  파일이 로딩 전에 중단됐고 core 재빌드 뒤 전부 통과했다.
- Orca 앱 `1.4.185`와 ready/reachable runtime에서 `main`·`web_hook` 두 워크트리를 대조했다.
  구현 commit `48b234552bb279635e8020b2189ab3f731a1643f`을 원격 `LegalFlow/web_hook`에 푸시하고 Orca
  상태를 `completed`로 전환했다. DB schema·migration·운영 데이터·외부 발송·기존 통제 PC
  설치본·main·운영 배포는 변경하지 않았다. 조직 Authenticode 서명과 정식 artifact 공개,
  실제 이벤트·잠금/부재 acceptance는 main 통합 뒤 남아 있다.
